fn main(r1: u64, r7: u64, r8: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let stack_ptr: ptr
    let frame_ptr: ptr
    let tmp_idx_104
    let tmp_idx_112
    let tmp_idx_144
    let tmp_idx_168
    let tmp_idx_264
    let tmp_idx_40
    let tmp_idx_48
    let tmp_idx_552
    let tmp_idx_56
    let tmp_idx_600
    let tmp_idx_64
    let tmp_idx_704
    let tmp_idx_760
    let tmp_idx_80
    let buffer_ptr_15: ptr
    let ptr_157_144
    let data_ptr_16: ptr
    let state_ptr_181: ptr
    let state_40
    let state_233_40
    let io_buffer_ptr: ptr
    let io_40
    let io_48
    let io_64
    let config_ptr: ptr
    let ptr_487_112
    let ptr_487_160
    let ptr_487_184
    let ptr_487_40
    let ptr_516_416
    let ptr_516_80
    let ptr_601: ptr
    let ptr_602: ptr
    let ptr_603: ptr
    let ptr_604: ptr
    let ptr_605: ptr
    let ptr_605_40
    let ptr_605_680
    let ptr_605_80
    let ptr_617_40
    let var_null: u64
    let var_zero: u64
    let arg_idx_883: u64
    let arg_idx_884: u64
    let arg_idx_885: u64

    // Check bounds for stack access
    if (0xFEFD0000 >=u r1 - 16 - 256) {
        stack_ptr = ptr_992 - 256
    }

    if (0xFEFD0000 >=u stack_ptr - 3760) {
        // Initial setup for heap memory block
        let target_ptr = stack_ptr - 3760
        RESULT_PTR = 0x4DFC
        arg_idx_883 = 1856
        arg_idx_884 = 0
        arg_idx_885 = 0
        goto block_399c;
    }

    return

    block_399c:
    if (!(0xFEFD0000 >=u frame_ptr - 192)) {
        return
    }
    frame_ptr = ptr_13 - 192
    tmp_idx_40 = arg_idx_883
    tmp_idx_48 = arg_idx_884
    tmp_idx_56 = arg_idx_885
    func_13(frame_ptr, r7)

    // Offset memory write: 0x60000 is base memory segment
    u32[12 + tmp_idx_40 + 0x60000] = tmp_idx_272

    tmp_idx_104 = var_null
    tmp_idx_112 = tmp_idx_64
    if (!(tmp_idx_64 <u 0)) {
        buffer_ptr_15 = tmp_idx_104 + 0x60000
        data_ptr_16 = tmp_idx_64 + 0x60000
        if (!(tmp_idx_96 == 0)) {
        while (tmp_idx_96 - 1 != 0) {
        r2 = buffer_ptr_15 + 1
        r3 = data_ptr_16 + 1
        }
        }
        goto block_3d16;
    }

    block_3d16:
    func_16(r1, r7)

    block_3aa2:
    while (0xFEFD0000 <u frame_ptr - 256) {
        tmp_idx_40 = var_null
        tmp_idx_48 = var_null
        let cond_56 = !(var_null)
        tmp_idx_56 = cond_56
        tmp_idx_64 = cond_56
        let cond_57 = cond_56 <u 0
        tmp_idx_80 = cond_57
        break
    }

    // Reset heap object fields
    io_buffer_ptr->field_393216 = 0
    u8[1 + io_buffer_ptr + 0x60000] = 0
    u32[4 + io_buffer_ptr + 0x60000] = 0

    return

    block_27170:
    let var_9423 = 0x1000 + tmp_idx_96
    tmp_idx_168 = var_9423
    tmp_idx_176 = 32 >>u (32 << var_9423) // Bitmask shift

    tmp_idx_104 = tmp_idx_96
    tmp_idx_112 = tmp_idx_88
    let cond_349 = tmp_idx_88 <u tmp_idx_96 <u 0
    tmp_idx_144 = cond_349
    if (tmp_idx_88 <u tmp_idx_96 <u 0) goto block_c074;

    r10 = 2
    r0 = 976

    if (!(0xFEFD0000 >=u frame_ptr - 904)) {
        return
    }
    ptr_605 = frame_ptr - 904
    ptr_605_40 = ptr_617_40
    let var_8960 = 32 >>u (32 << (12 >>u ptr_617_80))
    ptr_605_80 = var_8960
    if (!(var_8960 >u 16 <u 0)) {
        r0 = 954
        goto block_27185;
    }

    // Cryptographic/Obfuscated loop
    for (let ptr_609 = var_9135; ptr_605_648 >=u 0; ptr_609 = 32 >>u (32 << (0xFFFFFFFE & ptr_605_680))) {
        ptr_605_680 = u32[8 + ptr_605_648 + 0x60000]
        let var_9167 = 32
        if (var_9167 >>u (var_9167 << (1 & u32[8 + ptr_605_648 + 0x60000])) <u 0) goto block_2a332;
        goto block_2a371;
        break
    }

    block_2a470:
    func_193(r1, r7)

    block_2636d:
    return
}
fn func_1(r0: u64, input_len: u64, heap_ptr: u64, r10: u64, r11: u64, r12: u64) {
    // Check if input_len - 136 is within the valid memory boundary (0xFEFD0000)
    if (0xFEFD0000 >=u input_len - 136) {
        // @001f
        // Zero out a 16-byte block in the heap memory at heap_ptr + 0x60000
        u32[heap_ptr + 0x60000] = 0
        u32[4 + heap_ptr + 0x60000] = 0
        u32[8 + heap_ptr + 0x60000] = 0
        u32[12 + heap_ptr + 0x60000] = 0
    }

    // @008f
    return
}
fn func_2(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let loop_limit // Holds the computed shifting/loop bound

    // Memory bounds check: verify stack/heap range requirement
    if (0xFEFD0000 >=u r1 - 856) {
        if (r12 <u r11 <u 0) {
            // @01ca
            goto block_01cf;
        } else {
            // @011e
            goto block_013a;
        }
    }

    block_013a:
    // @013a
    // Calculate initial shift constraint based on relative pointers/offsets
    let shift_mask = 32 >>u (32 << ptr_0_56 - ptr_0_64)
    loop_limit = shift_mask

    if (shift_mask <=u 4 >=u 0) {
        // @01c5
        goto block_0228;
    } else {
        // @01bd: Clamp loop_limit to 4 for bounds safety
        loop_limit = 4
    }

    // @01bd
    loop_limit = 4

    // @01c5
    block_0228:
    // Iterate while loop_limit maintains integrity against iterator
    while (loop_limit <u ptr_0_272 >=u 0) {
        // @0402: Complex bitwise extraction from persistent memory arrays
        let bit_val_1 = 32 >>u (32 << (32 >>u (32 << (32 >>u (32 << (3 << ptr_0_272)) << u8[32 >>u (32 << (32 >>u (32 << ptr_0_272 + ptr_0_56)) + u32[4 + ptr_0_48 + 0x60000]) + 0x60000])) | ptr_0_280))
        let bit_val_2 = 32 >>u (32 << 1 + ptr_0_272)
    }

    // @054f

    // @0554

    // @027e: Initialize bit-width constants
    let bit_width_1 = 32
    let bit_width_2 = 32
    let bit_width_3 = 32
    let bit_width_4 = 32
    let bit_width_5 = 32

    // Conditional check on memory-mapped flag structure
    if (bit_width_5 >>u (bit_width_5 << (loop_limit >u 4 & bit_width_4 >>u (bit_width_4 << (128 & u8[bit_width_3 >>u (bit_width_3 << (bit_width_2 >>u (bit_width_2 << 1 - (bit_width_1 >>u (bit_width_1 << loop_limit + ptr_0_56)))) + u32[4 + ptr_0_48 + 0x60000]) + 0x60000])) <u 0)) <u 0) {
        // @05eb
        goto block_05f0;
    } else {
        // @03fd
    }
    // @0559: Compute secondary state mask
    let state_mask = 32 >>u (32 << (32 >>u (32 << (32 >>u (32 << (3 << loop_limit)) << 0xFFFFFFFF)) | ptr_0_280))

    // @05eb
    block_05f0:
    // @05f0
    block_01cf:
    // @01cf
    // Clear status fields at global memory offset 0x60000
    ptr_0_40->field_393216 = ptr_0_208
    u32[4 + ptr_0_40 + 0x60000] = 0
    u32[8 + ptr_0_40 + 0x60000] = 0
    u32[12 + ptr_0_40 + 0x60000] = 0

    // @0123
    return
}
fn func_3(frame_ptr: u64, stack_ptr: u64, r9: u64, r10: u64, bit_offset: u64, r12: u64) {
    // Check bounds against memory region (0xFEFD0000) for stack-relative offset
    if (0xFEFD0000 >=u stack_ptr - 88) {
        // @1175: Handle stack frame adjustment/spill
        func_4(stack_ptr - 88, r7)
    }

    // Loop until stack_ptr exceeds the allocated bounds minus buffer size
    while (0xFEFD0000 <u stack_ptr - 88 - 1784) {
        // Validate internal structure offset against memory limit
        if (0xFEFD0000 >=u stack_ptr - 88 - 296) {
            // @3171: Extract bit-field from memory buffer at r10 + 0x60000 base
            let bit_field_val = u8[32 >>u (32 << bit_offset + u32[4 + r10 + 0x60000]) + 0x60000]
            let mask_val = 32 >>u (32 << 1 + bit_offset)

            continue
        } else {
            // @3170: Out of bounds; terminate
            return
        }
    }

    // @3170: Function completion
    return

}
fn func_4(object_ptr: u64, value_to_store: u64) {
    // @11ae - Store value in the field at offset 72 relative to object_ptr
    u64[object_ptr + 72] = value_to_store

    // @11b7 - Load value from field at offset 80 and return it
    r7 = u64[object_ptr + 80]
    return
}
fn func_5(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let mask_val_144
    let bit_width_168
    let loop_limit_304

    // Verify system bounds against register 1
    if (0xFEFD0000 >=u r1 - 960) {
        // Calculate dynamic mask based on memory-mapped input at 0x60000
        mask_val_144 = 32 >>u (32 << (15 & u8[32 >>u (32 << r11 + u32[4 + r10 + 0x60000]) + 0x60000]))
        let var_bit_width = 32 >>u (32 << 1 + r11)
        bit_width_168 = var_bit_width

        if (r12 <u var_bit_width <u 0) {
            // @139c
            goto block_13a1;
        } else {
            // @12ee
            goto block_130b;
        }
    }

    block_130b:
    // Determine loop iteration ceiling, clamped to 4
    let calc_limit = 32 >>u (32 << bit_width_168 - ptr_0_64) // ptr_0_64 is external state
    loop_limit_304 = calc_limit

    if (calc_limit <=u 4 >=u 0) {
        // @1397
        goto block_13fc;
    } else {
        // @138f - clamp loop count to 4
        loop_limit_304 = 4
    }

    // @138f
    loop_limit_304 = 4

    // @1397

    block_13fc:
    // Iterative processing of bitmasks based on memory offset array
    while (loop_limit_304 <u ptr_0_376 >=u 0) {
        // @15d7
        let proc_val_241 = 32 >>u (32 << (32 >>u (32 << (32 >>u (32 << (3 << ptr_0_376)) << u8[32 >>u (32 << (32 >>u (32 << ptr_0_376 + bit_width_168)) + u32[4 + ptr_0_48 + 0x60000]) + 0x60000])) | ptr_0_384))
        let var_250 = 32 >>u (32 << 1 + ptr_0_376)
    }

    // @1725

    // @172a

    // @1452
    let w_32 = 32
    
    // Complex bitwise validation logic against memory buffer
    if (w_32 >>u (w_32 << (loop_limit_304 >u 4 & w_32 >>u (w_32 << (128 & u8[w_32 >>u (w_32 << (w_32 >>u (w_32 << 1 - (w_32 >>u (w_32 << loop_limit_304 + bit_width_168)))) + u32[4 + ptr_0_48 + 0x60000]) + 0x60000])) <u 0)) <u 0) {
        // @17c1
        goto block_17c6;
    } else {
        // @15d2
    }
    // @172f
    let final_mask_279 = 32 >>u (32 << (32 >>u (32 << (32 >>u (32 << (3 << loop_limit_304)) << 0xFFFFFFFF)) | ptr_0_384))

    // @17c1

    block_17c6:
    // @17c6

    block_13a1:
    // Finalize state update to memory structure
    ptr_0_40->field_393216 = mask_val_144
    u32[4 + ptr_0_40 + 0x60000] = ptr_0_312
    u32[8 + ptr_0_40 + 0x60000] = 0 // Clear auxiliary fields
    u32[12 + ptr_0_40 + 0x60000] = 0

    // @12f3
    return
}
fn func_6(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    if (0xFEFD0000 >=u r1 - 1064) {
        // @17e0: Perform state transformation based on memory-mapped inputs at 0x60000
        let state_val = u32[4 + r10 + 0x60000]
        let xform_shift_1 = 32 >>u (32 << 1 + r11)
        let xform_shift_5 = 32 >>u (32 << 5 + r11)
        
        // Apply S-box/permutation transformation to the first word
        u32[r9 + 0x60000] = 32 >>u (32 << (15 & u8[32 >>u (32 << state_val + r11) + 0x60000]))
        
        // Apply heavy bitwise transformation (S-box mixing) to the second word
        u32[4 + r9 + 0x60000] = 32 >>u (32 << (32 >>u (32 << (24 << u8[32 >>u (32 << state_val + (32 >>u (32 << 3 + xform_shift_1))) + 0x60000])) | 32 >>u (32 << (32 >>u (32 << (16 << u8[32 >>u (32 << state_val + (32 >>u (32 << 2 + xform_shift_1))) + 0x60000])) | 32 >>u (32 << (32 >>u (32 << (8 << u8[32 >>u (32 << state_val + (32 >>u (32 << 1 + xform_shift_1))) + 0x60000])) | u8[32 >>u (32 << xform_shift_1 + state_val) + 0x60000]))))))
        
        // Apply heavy bitwise transformation (S-box mixing) to the third word
        u32[8 + r9 + 0x60000] = 32 >>u (32 << (32 >>u (32 << (24 << u8[32 >>u (32 << state_val + (32 >>u (32 << 3 + xform_shift_5))) + 0x60000])) | 32 >>u (32 << (32 >>u (32 << (16 << u8[32 >>u (32 << state_val + (32 >>u (32 << 2 + xform_shift_5))) + 0x60000])) | 32 >>u (32 << (32 >>u (32 << (8 << u8[32 >>u (32 << state_val + (32 >>u (32 << 1 + xform_shift_5))) + 0x60000])) | u8[32 >>u (32 << state_val + xform_shift_5) + 0x60000]))))))
        
        // Clear the fourth word of the output buffer
        u32[12 + r9 + 0x60000] = 0
    }

    // @1e42
    return
}
fn func_7(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let loop_idx_active_range_len   // ptr_0_1152
    let base_offset_val             // ptr_0_144
    let range_limit_a               // ptr_0_216
    let range_limit_b               // ptr_0_272
    let range_limit_c               // ptr_0_296
    let active_range_len            // ptr_0_432

    // Bound check on input r1 against a memory-mapped base offset (0x60000 range)
    if (0xFEFD0000 >=u r1 - 1808) {
        // Read byte from memory mapped region indexable by r10
        let entry_byte = u8[32 >>u (32 << r11 + u32[4 + r10 + 0x60000]) + 0x60000]
        base_offset_val = 32 >>u (32 << (15 & entry_byte))
        let var_35 = 32 >>u (32 << (4 >>u entry_byte))
        range_limit_a = var_35

        if (var_35 <u 4 >=u 0) {
            // Calculate slice bounds using bit-shifting logic
            let var_69 = 32 >>u (32 << (32 >>u (32 << 1 + ptr_0_56)) + ptr_0_56)
            range_limit_b = var_69
            let var_78 = 32 >>u (32 << 1 + ptr_0_56)
            range_limit_c = var_78

            if (var_69 <u var_78 <u 0) {
                goto block_210a;
            } else {
                goto block_2073;
            }
        }
    }

    block_2073:
    // Determine active range length based on calculated slice bounds
    let var_110 = 32 >>u (32 << range_limit_c - range_limit_b)
    active_range_len = var_110

    if (var_110 <=u 4 >=u 0) {
        goto block_215f;
    } else {
        active_range_len = 4
    }

    active_range_len = 4

    block_215f:
    // Process main loop over range_len
    while (active_range_len <u ptr_0_496 >=u 0) {
        let var_285 = 32 >>u (32 << (32 >>u (32 << (32 >>u (32 << (3 << ptr_0_496)) << u8[32 >>u (32 << (32 >>u (32 << ptr_0_496 + range_limit_c)) + u32[4 + ptr_0_48 + 0x60000]) + 0x60000])) | ptr_0_504))
        let var_294 = 32 >>u (32 << 1 + ptr_0_496)
    }

    let mask_val_a = 32
    let mask_val_b = 32
    let mask_val_c = 32
    let mask_val_d = 32
    let mask_val_e = 32

    // Bitwise validation logic before branching
    if (mask_val_e >>u (mask_val_e << (active_range_len >u 4 & mask_val_d >>u (mask_val_d << (128 & u8[mask_val_c >>u (mask_val_c << (mask_val_b >>u (mask_val_b << 1 - (mask_val_a >>u (mask_val_a << active_range_len + range_limit_c)))) + u32[4 + ptr_0_48 + 0x60000]) + 0x60000])) <u 0)) <u 0) {
        goto block_2529;
    }
    
    let var_323 = 32 >>u (32 << (32 >>u (32 << (32 >>u (32 << (3 << active_range_len)) << 0xFFFFFFFF)) | ptr_0_504))

    block_2529:
    block_210a:
    // Secondary range validation
    if (ptr_0_64 <u range_limit_b <u 0) {
        goto block_25c4;
    }
    
    let var_334 = 32 >>u (32 << range_limit_b - ptr_0_64)
    loop_idx_active_range_len = var_334

    if (var_334 <=u 4 >=u 0) {
        goto block_2621;
    } else {
        loop_idx_active_range_len = 4
    }

    loop_idx_active_range_len = 4

    block_2621:
    while (loop_idx_active_range_len <u ptr_0_1224 >=u 0) {
        let var_508 = 32 >>u (32 << (32 >>u (32 << (32 >>u (32 << (3 << ptr_0_1224)) << u8[32 >>u (32 << (32 >>u (32 << range_limit_b + ptr_0_1224)) + u32[4 + ptr_0_48 + 0x60000]) + 0x60000])) | ptr_0_1232))
        let var_517 = 32 >>u (32 << 1 + ptr_0_1224)
    }

    let mask_val_f = 32
    let mask_val_g = 32
    let mask_val_h = 32
    let mask_val_i = 32
    let mask_val_j = 32

    if (mask_val_j >>u (mask_val_j << (loop_idx_active_range_len >u 4 & mask_val_i >>u (mask_val_i << (128 & u8[mask_val_h >>u (mask_val_h << (mask_val_g >>u (mask_val_g << 1 - (mask_val_f >>u (mask_val_f << range_limit_b + loop_idx_active_range_len)))) + u32[4 + ptr_0_48 + 0x60000]) + 0x60000])) <u 0)) <u 0) {
        goto block_29eb;
    }
    
    let var_546 = 32 >>u (32 << (32 >>u (32 << (32 >>u (32 << (3 << loop_idx_active_range_len)) << 0xFFFFFFFF)) | ptr_0_1232))

    block_29eb:
    block_25c4:
    // Finalizing structural updates to instance memory @ r40
    ptr_0_40->field_393216 = base_offset_val
    u32[4 + ptr_0_40 + 0x60000] = ptr_0_440
    u32[8 + ptr_0_40 + 0x60000] = ptr_0_1160
    u32[12 + ptr_0_40 + 0x60000] = 0

    return
}
fn func_8(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    if (0xFEFD0000 >=u r1 - 240) {
        // @2a05: Perform bitwise transformation on memory-mapped buffer
        let shift_val = u8[32 >>u (32 << r11 + u32[4 + r10 + 0x60000]) + 0x60000]
        
        // Update state buffer at r9 using computed bitwise results
        u32[r9 + 0x60000] = 32 >>u (32 << (4 >>u shift_val))
        u32[4 + r9 + 0x60000] = 32 >>u (32 << (15 & shift_val))
        
        // Zero out padding/remaining fields in the struct
        u32[8 + r9 + 0x60000] = 0
        u32[12 + r9 + 0x60000] = 0
    }

    // @2b1c
    return
}
fn func_9(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let lookup_val
    let mask_shift_val
    let bit_mask_size
    let loop_counter

    if (0xFEFD0000 >=u r1 - 984) {
        // @2b49: Perform bit-field extraction via lookup table at 0x60000 base offset
        let var_17 = u8[32 >>u (32 << r11 + u32[4 + r10 + 0x60000]) + 0x60000]
        lookup_val = var_17
        mask_shift_val = 32 >>u (32 << (4 >>u var_17))
        let var_35 = 32 >>u (32 << 1 + r11)
        bit_mask_size = var_35

        if (r12 <u var_35 <u 0) {
            // @2d03: Branch to initialization block
            goto block_2d08;
        } else {
            // @2c55: Proceed to bit calculation
            goto block_2c72;
        }
    }

    block_2c72:
    // @2c72: Calculate initial loop counter based on mask size
    let var_67 = 32 >>u (32 << bit_mask_size - ptr_0_64)
    loop_counter = var_67

    if (var_67 <=u 4 >=u 0) {
        // @2cfe
        goto block_2d8d;
    } else {
        // @2cf6: Clamp counter to max 4
        loop_counter = 4
    }

    // @2cf6
    loop_counter = 4

    // @2cfe

    block_2d8d:
    while (loop_counter <u ptr_0_400 >=u 0) {
        // @2f68: Perform multi-stage shift/OR operations on buffer data
        let var_250 = 32 >>u (32 << (32 >>u (32 << (32 >>u (32 << (3 << ptr_0_400)) << u8[32 >>u (32 << (32 >>u (32 << ptr_0_400 + bit_mask_size)) + u32[4 + ptr_0_48 + 0x60000]) + 0x60000])) | ptr_0_408))
        let var_259 = 32 >>u (32 << 1 + loop_counter)
    }

    // @30b6

    // @30bb

    // @2de3: Prepare bit-width operands for complex conditional update
    let var_130 = 32
    let var_139 = 32
    let var_150 = 32
    let var_160 = 32
    let var_183 = 32

    // Conditional bit-masking logic
    if (var_183 >>u (var_183 << (loop_counter >u 4 & var_160 >>u (var_160 << (128 & u8[var_150 >>u (var_150 << (var_139 >>u (var_139 << 1 - (var_130 >>u (var_130 << loop_counter + bit_mask_size)))) + u32[4 + ptr_0_48 + 0x60000]) + 0x60000])) <u 0)) <u 0) {
        // @3152
        goto block_3157;
    } else {
        // @2f63
    }
    // @30c0: Apply overflow/masking correction
    let var_288 = 32 >>u (32 << (32 >>u (32 << (32 >>u (32 << (3 << loop_counter)) << 0xFFFFFFFF)) | ptr_0_408))

    // @3152

    block_3157:
    // @3157

    block_2d08:
    // @2d08: Write state to memory structure
    ptr_0_40->field_393216 = mask_shift_val
    u32[4 + ptr_0_40 + 0x60000] = 32 >>u (32 << (15 & lookup_val))
    u32[8 + ptr_0_40 + 0x60000] = ptr_0_312
    u32[12 + ptr_0_40 + 0x60000] = 0

    // @2c5a
    return
}
fn func_10(r1: u64, r7: u64) {
    // @3221
    u64[r1 + 152] = r7
    let input_header_val = u32[r7 + 0x60000] // Load value at 0x60000 offset from r7
    u64[r1 + 160] = input_header_val
    let addr_offset_ptr = 4 + r7
    u64[r1 + 168] = addr_offset_ptr
    let offset_val = u32[addr_offset_ptr + 0x60000]
    u64[r1 + 176] = offset_val
    let shift_base = u64[r1 + 120]
    u64[r1 + 184] = shift_base
    let shift_amount = 4 >>u shift_base // Perform unsigned right shift
    u64[r1 + 192] = shift_amount
    let mask_val_a = 32 >>u (32 << shift_amount) // Bit manipulation to create mask
    u64[r1 + 200] = mask_val_a
    u32[u64[r1 + 40] + 0x60000] = mask_val_a // Store mask result to memory at dest base
    let mask_base = u64[r1 + 120]
    u64[r1 + 216] = mask_base
    let alignment_four_bits = 15 & mask_base // Extract lower 4 bits (mod 16)
    u64[r1 + 224] = alignment_four_bits
    let mask_val_b = 32 >>u (32 << alignment_four_bits)
    u64[r1 + 232] = mask_val_b
    let dest_ptr_plus_4 = 4 + u64[r1 + 40]
    u64[r1 + 240] = dest_ptr_plus_4
    u32[dest_ptr_plus_4 + 0x60000] = mask_val_b
    let dest_ptr_plus_8 = 8 + u64[r1 + 40]
    u64[r1 + 256] = dest_ptr_plus_8
    u32[dest_ptr_plus_8 + 0x60000] = input_header_val
    let dest_ptr_plus_12 = 12 + u64[r1 + 40]
    u64[r1 + 272] = dest_ptr_plus_12
    u32[dest_ptr_plus_12 + 0x60000] = offset_val

    // @32fd
    r7 = u64[r1 + 288]
    return
}
fn func_11(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    // Check if bounds are within safe memory range (0xFEFD0000 limit)
    if (0xFEFD0000 >=u r1 - 328) {
        // @332a
        // Load index from memory relative to r10
        let data_offset = u32[4 + r10 + 0x60000]
        
        // Calculate bitmask/shift index from memory location
        let shift_val = u8[32 >>u (32 << r11 + data_offset) + 0x60000]
        
        // Apply bitwise transformations to memory relative to r9
        u32[r9 + 0x60000] = 32 >>u (32 << (4 >>u shift_val))
        u32[4 + r9 + 0x60000] = 32 >>u (32 << (15 & shift_val))
        
        // Update memory with nested bitwise calculation from secondary offset
        u32[8 + r9 + 0x60000] = 32 >>u (32 << (15 & u8[32 >>u (32 << data_offset + (32 >>u (32 << 1 + r11))) + 0x60000]))
        
        // Clear word at 12-byte offset
        u32[12 + r9 + 0x60000] = 0
    }

    // @34d4
    return
}
fn func_13(state_ptr: u64, data_ptr: u64) {
    // @39df
    // Initialize structure pointers and reset memory regions
    u64[state_ptr + 64] = data_ptr                                 // Store data pointer in state at offset 64
    u32[data_ptr + 0x60000] = 0                                    // Initialize field at offset 0 (u32)
    let offset_4 = 4 + data_ptr
    u64[state_ptr + 80] = offset_4                                 // Store offset_4 in state at offset 80
    u32[offset_4 + 0x60000] = 0                                    // Initialize field at offset 4 (u32)
    let offset_8 = 8 + data_ptr
    u64[state_ptr + 96] = offset_8                                 // Store offset_8 in state at offset 96
    u64[offset_8 + 0x60000] = 0                                    // Initialize field at offset 8 (u64)
    let offset_16 = 16 + data_ptr
    u64[state_ptr + 112] = offset_16                               // Store offset_16 in state at offset 112
    u8[offset_16 + 0x60000] = 0                                    // Initialize field at offset 16 (u8)

    // Populate fields from state buffer
    u32[data_ptr + 0x60000] = u64[state_ptr + 40]                  // Write value from state[40] to data[0]
    let offset_4_val = 4 + data_ptr
    u64[state_ptr + 136] = offset_4_val
    u32[offset_4_val + 0x60000] = u64[state_ptr + 48]              // Write value from state[48] to data[4]
    let offset_8_val = 8 + data_ptr
    u64[state_ptr + 152] = offset_8_val
    u64[offset_8_val + 0x60000] = 1                                // Set flag at data[8] to 1
    let offset_16_val = 16 + data_ptr
    u64[state_ptr + 168] = offset_16_val
    u8[offset_16_val + 0x60000] = u64[state_ptr + 56]              // Write value from state[56] to data[16]

    // @3a8a
    r7 = u64[state_ptr + 184]                                      // Return context/next pointer
    return
}
fn func_15(ctx_ptr: u64, input_val: u64) {
    // @3b68
    u64[ctx_ptr + 136] = input_val
    u64[ctx_ptr + 144] = input_val
    let offset_delta = 4 - input_val
    u64[ctx_ptr + 152] = offset_delta
    
    // Calculate memory shift mask based on offset_delta
    let bit_mask = 32 >>u (32 << offset_delta)
    u64[ctx_ptr + 160] = bit_mask
    
    // Initialize memory region offsets relative to bit_mask
    let base_ptr_1 = 4 + bit_mask
    u64[ctx_ptr + 168] = base_ptr_1
    u32[base_ptr_1 + 0x60000] = 0 // Clear first buffer slot
    
    let base_ptr_2 = 8 + bit_mask
    u64[ctx_ptr + 184] = base_ptr_2
    u32[base_ptr_2 + 0x60000] = 0 // Clear second buffer slot
    
    let base_ptr_3 = 12 + bit_mask
    u64[ctx_ptr + 200] = base_ptr_3
    u32[base_ptr_3 + 0x60000] = u64[ctx_ptr + 48] // Copy stored value from context
    
    let base_ptr_4 = 16 + bit_mask
    u64[ctx_ptr + 216] = base_ptr_4
    u32[base_ptr_4 + 0x60000] = u64[ctx_ptr + 40] // Copy stored value from context
    
    // Setup auxiliary pointers and shift parameters
    u64[ctx_ptr + 232] = input_val
    let aux_shift_val = 16 + input_val
    u64[ctx_ptr + 240] = aux_shift_val
    u64[ctx_ptr + 248] = 32 >>u (32 << aux_shift_val)

    // @3b13
    r7 = u64[ctx_ptr + 96]
    return
}
fn func_16(state_ptr: u64, input_struct_ptr: u64) {
    // @3d2b
    // Store input struct pointer in state memory
    u64[state_ptr + 144] = input_struct_ptr
    u32[input_struct_ptr + 0x60000] = u64[state_ptr + 104]
    
    // Calculate and store offset for second struct field
    let offset_ptr_2 = 4 + input_struct_ptr
    u64[state_ptr + 160] = offset_ptr_2
    u32[offset_ptr_2 + 0x60000] = u64[state_ptr + 104]
    
    // Calculate and store offset for third struct field
    let offset_ptr_3 = 8 + input_struct_ptr
    u64[state_ptr + 176] = offset_ptr_3
    u32[offset_ptr_3 + 0x60000] = u64[state_ptr + 96]
    
    // Calculate and store offset for fourth struct field
    let offset_ptr_4 = 12 + input_struct_ptr
    u64[state_ptr + 192] = offset_ptr_4
    u32[offset_ptr_4 + 0x60000] = u64[state_ptr + 40]

    // @3cc8
    // Update r7 with value from state memory before return
    r7 = u64[state_ptr + 128]
    return
}
fn func_21(r1: u64) {
    // @77c9
    // Load HEAP_PAGES value from memory offset 56 of the provided state struct
    HEAP_PAGES = u64[r1 + 56]

    // @77d9
    // Load the pointer/value from offset 8 of the provided state struct into r9
    r9 = u64[r1 + 8]
    return
}
fn func_23(state_ptr: u64, input_val: u64) {
    let data_offset: ptr

    // @7950
    u64[state_ptr + 208] = input_val
    data_offset = input_val + 0x60000 // Base address for memory mapping

    if (condition_flag == 0) {
        // @7977
        // Store input_val in the structure at base offset + 0x60000
        u32[u64[state_ptr + 112] + 0x60000] = u64[state_ptr + 208]
        
        let header_field_1 = 4 + u64[state_ptr + 112]
        u64[state_ptr + 232] = header_field_1
        u32[header_field_1 + 0x60000] = u64[state_ptr + 208]
        
        let header_field_2 = 8 + u64[state_ptr + 112]
        u64[state_ptr + 248] = header_field_2
        u32[header_field_2 + 0x60000] = u64[state_ptr + 40]
    } else {
        // @7969
        r2 = data_offset + 1 // Increment memory pointer for alternate path
    }

    // @7873
    r7 = u64[state_ptr + 96] // Load return value from context
    return
}
fn func_24(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let shift_val
    let target_ptr
    let offset_idx

    // Validate memory bounds relative to r1
    if (0xFEFD0000 >=u r1 - 376) {
        // @79d0
        // Calculate shift based on 32-bit value at r10 offset 0x60004
        shift_val = 32 >>u (32 << u32[4 + r10 + 0x60000])

        // Check if value is within indexable range [0, 13]
        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @7a64
            target_ptr = 32 >>u (32 << (32 >>u (32 << (3 << shift_val))) + ptr_0_56)
            offset_idx = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @7b36
                // Population count (number of bits set) at calculated memory address
                target_ptr->field_393216 = popcnt64(u64[32 >>u (32 << (32 >>u (32 << (3 << offset_idx))) + ptr_0_56) + 0x60000])
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0 // Clear related state structure field
                goto block_7bdd;
            } else {
                // @7b2e
                offset_idx = 12
            }
        } else {
            // @7a5c
            shift_val = 12
        }
    }

    block_7bdd:
    // @7bdd
    return
}
fn func_25(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let shift_val_a
    let mem_ptr_b
    let shift_val_c

    if (0xFEFD0000 >=u r1 - 424) {
        // @7c0a: Extract bit shift amount from memory at offset 0x60000 + 4
        shift_val_a = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @7c9e: Calculate pointer offsets based on internal state
            mem_ptr_b = 32 >>u (32 << (32 >>u (32 << (3 << shift_val_a))) + ptr_0_56)
            shift_val_c = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @7d70: Update memory field using population count (popcnt) logic
                mem_ptr_b->field_393216 = 32 >>u (32 << (32 >>u (32 << popcnt32(32 >>u (32 << u64[32 >>u (32 << (32 >>u (32 << (3 << shift_val_c))) + ptr_0_56) + 0x60000])))))
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_7e68;
            } else {
                // @7d68: Fallback shift value for invalid range
                shift_val_c = 12
            }
        } else {
            // @7c96: Fallback shift value for invalid range
            shift_val_a = 12
        }
    }

    block_7e68:
    // @7e68: Exit function
    return
}
fn func_26(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val
    let index_computed
    let offset_val

    // Check if r1 - 376 is within bounds of memory region 0xFEFD0000
    if (0xFEFD0000 >=u r1 - 376) {
        // Prepare bit shift for base memory lookup
        bit_shift_val = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // Compute index for heap/memory access
            index_computed = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val))) + ptr_0_56)
            offset_val = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // Perform CLZ (Count Leading Zeros) update on memory based on calculated offsets
                index_computed->field_393216 = clz64(u64[32 >>u (32 << (32 >>u (32 << (3 << offset_val))) + ptr_0_56) + 0x60000])
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_80a2;
            } else {
                // Default shift value for out-of-bounds index
                offset_val = 12
            }
        } else {
            // Default shift value for out-of-bounds memory pointer
            bit_shift_val = 12
        }
    }

    block_80a2:
    // @80a2
    return
}
fn func_27(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val
    let target_ptr
    let offset_val

    // Check bounds: r1 - 424 must be within valid addressable range
    if (0xFEFD0000 >=u r1 - 424) {
        // @80cf
        // Normalize bit shift value from memory at r10 + 0x60004
        bit_shift_val = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @8163
            // Calculate offsets based on memory inputs and state registers
            target_ptr = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val))) + ptr_0_56)
            offset_val = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @8235
                // Perform complex word-aligned update using count leading zeros
                target_ptr->field_393216 = 32 >>u (32 << (32 >>u (32 << clz32(32 >>u (32 << u64[32 >>u (32 << (32 >>u (32 << (3 << offset_val))) + ptr_0_56) + 0x60000])))))
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_832d;
            } else {
                // @822d
                offset_val = 12
            }
        } else {
            // @815b
            bit_shift_val = 12
        }
    }

    block_832d:
    // @832d
    return
}
fn func_28(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let shift_val
    let addr_base
    let calculated_offset

    if (0xFEFD0000 >=u r1 - 376) {
        // @835a: Extract shift amount from memory at offset 0x60000 + 4
        shift_val = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @83ee: Calculate memory pointer using nested shifts and base offset
            addr_base = 32 >>u (32 << (32 >>u (32 << (3 << shift_val))) + ptr_0_56)
            calculated_offset = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @84c0: Result of Count Trailing Zeros stored in memory field
                addr_base->field_393216 = ctz64(u64[32 >>u (32 << (32 >>u (32 << (3 << calculated_offset))) + ptr_0_56) + 0x60000])
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_8567;
            } else {
                // @84b8: Default fallback index
                calculated_offset = 12
            }
        } else {
            // @83e6: Default fallback shift value
            shift_val = 12
        }
    }

    block_8567:
    // @8567: End of function block
    return
}
fn func_29(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bitmask_shift
    let target_addr_offset
    let target_idx

    // Validate memory boundary: r1 is within reachable range 0xFEFD0000
    if (0xFEFD0000 >=u r1 - 424) {
        // @8594: Calculate bitmask shift based on memory value at state offset 0x60000
        bitmask_shift = 32 >>u (32 << u32[4 + r10 + 0x60000])

        // Verify index is within safe bounds [0, 13]
        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @8628: Calculate base offsets for memory access
            target_addr_offset = 32 >>u (32 << (32 >>u (32 << (3 << bitmask_shift))) + ptr_0_56)
            target_idx = 32 >>u (32 << ptr_0_48->field_393216)

            // Validate write index bounds
            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @86fa: Masked write operation using counted trailing zeros to derive bit position
                target_addr_offset->field_393216 = 32 >>u (32 << (32 >>u (32 << ctz32(32 >>u (32 << u64[32 >>u (32 << (32 >>u (32 << (3 << target_idx))) + ptr_0_56) + 0x60000])))))
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0 // Clear associated status flags
                goto block_87f2;
            } else {
                // @86f2: Fallback index
                target_idx = 12
            }
        } else {
            // @8620: Fallback shift
            bitmask_shift = 12
        }
    }

    block_87f2:
    // @87f2
    return
}
fn func_30(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let shift_val
    let target_ptr
    let offset_val

    if (0xFEFD0000 >=u r1 - 432) {
        // @881f: Read bit-depth/shift factor from memory mapped I/O at 0x60000+offset
        shift_val = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @88b3: Calculate target memory pointer using base offset and global state
            target_ptr = 32 >>u (32 << (32 >>u (32 << (3 << shift_val))) + ptr_0_56)
            offset_val = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @8985: Perform bit-packed write via sign-extended shift
                target_ptr->field_393216 = 32 >>u (32 << sext8(32 >>u (32 << u64[32 >>u (32 << (32 >>u (32 << (3 << offset_val))) + ptr_0_56) + 0x60000])))
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0 // Reset status word at memory offset
                goto block_8a80;
            } else {
                // @897d: Clamp offset_val to valid range
                offset_val = 12
            }
        } else {
            // @88ab: Clamp shift_val to valid range
            shift_val = 12
        }
    }

    block_8a80:
    // @8a80
    return
}
fn func_31(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let shift_val
    let target_ptr
    let offset_idx

    // Validate memory boundary: r1 - 432 is index within reachable heap
    if (0xFEFD0000 >=u r1 - 432) {
        // @8aad
        // Calculate shift based on stored state at 0x60000 + r10 offset
        shift_val = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @8b41
            // Compute indirect target address using shift_val
            target_ptr = 32 >>u (32 << (32 >>u (32 << (3 << shift_val))) + ptr_0_56)
            offset_idx = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @8c13
                // Perform sign-extended write to memory location
                target_ptr->field_393216 = 32 >>u (32 << sext16(32 >>u (32 << u64[32 >>u (32 << (32 >>u (32 << (3 << offset_idx))) + ptr_0_56) + 0x60000])))
                // Clear state fields
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_8d0e;
            } else {
                // @8c0b
                offset_idx = 12
            }
        } else {
            // @8b39
            shift_val = 12
        }
    }

    block_8d0e:
    // @8d0e
    return
}
fn func_32(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let shift_val_a
    let target_ptr
    let shift_val_b

    // Boundary check using r1 (offset) against 0xFEFD0000
    if (0xFEFD0000 >=u r1 - 424) {
        // @8d3b: Calculate initial shift value from memory at r10 + 0x60004
        shift_val_a = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @8dcf: Secondary shift calculations using stack/global offsets
            target_ptr = 32 >>u (32 << (32 >>u (32 << (3 << shift_val_a))) + ptr_0_56)
            shift_val_b = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @8ea1: Update target field using masked bitwise operations
                target_ptr->field_393216 = 32 >>u (32 << (32 >>u (32 << (0xFFFF & 32 >>u (32 << u64[32 >>u (32 << (32 >>u (32 << (3 << shift_val_b))) + ptr_0_56) + 0x60000])))))
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0 // Clear word at memory offset
                goto block_8f9f;
            } else {
                // @8e99
                shift_val_b = 12
            }
        } else {
            // @8dc7
            shift_val_a = 12
        }
    }

    block_8f9f:
    // @8f9f
    return
}
fn func_33(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val
    let output_ptr
    let bit_index

    if (0xFEFD0000 >=u r1 - 456) {
        // @8fcc: Calculate initial bit shift based on memory offset
        bit_shift_val = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @9060: Calculate target pointer and bit index
            output_ptr = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val))) + ptr_0_56)
            bit_index = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @9132: Bitwise manipulation and byte swapping routine
                let bit_data = u64[32 >>u (32 << (32 >>u (32 << (3 << bit_index))) + ptr_0_56) + 0x60000]
                let swap_8 = 8 << (0xFF00FF00FF00FF & bit_data) | 0xFF00FF00FF00FF & 8 >>u bit_data
                let swap_16 = 16 << (0xFFFF0000FFFF & swap_8) | 0xFFFF0000FFFF & 16 >>u swap_8
                
                // Store result into structure field
                output_ptr->field_393216 = (63 & 32) >>u swap_16 | (63 & 32) - 64 << swap_16
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_929d;
            } else {
                // @912a: Fallback index if loop condition fails
                bit_index = 12
            }
        } else {
            // @9058: Fallback shift if bounds check fails
            bit_shift_val = 12
        }
    }

    block_929d:
    // @929d
    return
}
fn func_34(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    if (0xFEFD0000 >=u r1 - 320) {
        // Calculate bitmask shift value based on PVM state memory
        let bit_shift_val = 32 >>u (32 << u32[r10 + 0x60000])

        if (u32[r10 + 0x60000] <=u 13 >=u 0) {
            let const_32 = 32
            let var_step_size = 32

            // Compare PVM memory at offset 0x60000 with computed register index
            if (!(u32[4 + ptr_0_48 + 0x60000] ^ u64[var_step_size >>u (var_step_size << (const_32 >>u (const_32 << (3 << bit_shift_val))) + ptr_0_56) + 0x60000]) <u 0) {
                goto block_947b;
            } else {
                goto block_9432;
            }
        }
    }

    block_9432:
    // Memory write: set state field and propagate secondary state value
    ptr_0_40->field_393216 = 1
    u32[4 + ptr_0_40 + 0x60000] = u32[8 + ptr_0_48 + 0x60000]

    block_941a:
    return

    block_947b:
    // Memory write: reset state field and clear secondary memory buffer
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0
    goto block_941a;
}
fn func_35(r0: u64, input_len: u64, r9: u64, heap_base: u64, r11: u64, r12: u64) {
    // Check if input_len - 320 is within bounds of the memory segment
    if (0xFEFD0000 >=u input_len - 320) {
        // @94b7: Calculate shift factor based on memory-mapped state
        let shift_factor = 32 >>u (32 << u32[heap_base + 0x60000])

        // Verify if the index at memory offset 0x60000 is within [0, 13]
        if (u32[heap_base + 0x60000] <=u 13 >=u 0) {
            // @953d
            let const_32 = 32
            let const_32_alt = 32

            // Validate state bitmask against memory buffer
            if ((u32[4 + ptr_0_48 + 0x60000] ^ u64[const_32_alt >>u (const_32_alt << (const_32 >>u (const_32 << (3 << shift_factor))) + ptr_0_56) + 0x60000]) <u 0 <u 0) {
                // @9665
                goto block_966a;
            } else {
                // @9604
                goto block_9621;
            }
        }
    }

    block_9621:
    // @9621: Set object active flag and sync buffer data
    ptr_0_40->field_393216 = 1
    u32[4 + ptr_0_40 + 0x60000] = u32[8 + ptr_0_48 + 0x60000]

    block_9609:
    // @9609
    return

    block_966a:
    // @966a: Clear object flag and reset sync buffer
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0
    goto block_9609;
}
fn func_36(base_addr: u64, input_offset: u64, r9: u64, mem_base: u64, r11: u64, r12: u64) {
    // Check if input_offset is within valid bounds (0xFEFD0000 >= input_offset - 320)
    if (0xFEFD0000 >=u input_offset - 320) {
        // @96a6
        // Calculate shift factor based on value at mem_base + 0x60000
        let shift_factor = 32 >>u (32 << u32[mem_base + 0x60000])

        // Verify bounds of the value at mem_base + 0x60000
        if (u32[mem_base + 0x60000] <=u 13 >=u 0) {
            // @972c
            let bit_width = 32
            let shift_val = 32

            // Compare entry in table against constrained lookup
            if (u32[4 + ptr_0_48 + 0x60000] <u u64[shift_val >>u (shift_val << (bit_width >>u (bit_width << (3 << shift_factor))) + ptr_0_56) + 0x60000] <u 0) {
                // @984f
                goto block_9854;
            } else {
                // @97ee
                goto block_980b;
            }
        }
    }

    block_980b:
    // @980b
    // Set field at 393216 to 1 and update status word
    ptr_0_40->field_393216 = 1
    u32[4 + ptr_0_40 + 0x60000] = u32[8 + ptr_0_48 + 0x60000]

    block_97f3:
    // @97f3
    return

    block_9854:
    // @9854
    // Clear field at 393216 and reset status word output
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0
    goto block_97f3;
}
fn func_37(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    if (0xFEFD0000 >=u r1 - 320) {
        // Calculate bitmask shift based on state stored at offset 0x60000/r10
        let bit_shift_val = 32 >>u (32 << u32[r10 + 0x60000])

        if (u32[r10 + 0x60000] <=u 13 >=u 0) {
            let op_size = 32
            let base_offset = 32

            // Comparison against memory mapped structural data
            if (u64[base_offset >>u (base_offset << (op_size >>u (op_size << (3 << bit_shift_val))) + ptr_0_56) + 0x60000] >=u u32[4 + ptr_0_48 + 0x60000] <u 0) {
                // @9a3c
                goto block_9a41;
            } else {
                // @99db
                goto block_99f8;
            }
        }
    }

    block_99f8:
    // Update state flag and propagate data from registry buffer
    ptr_0_40->field_393216 = 1
    u32[4 + ptr_0_40 + 0x60000] = u32[8 + ptr_0_48 + 0x60000]

    block_99e0:
    // @99e0
    return

    block_9a41:
    // Reset state flag and clear buffer field
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0
    goto block_99e0;
}
fn func_38(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    if (0xFEFD0000 >=u r1 - 320) {
        // @9a7d: Calculate bitmask shift factor based on memory-mapped state
        let bit_shift_factor = 32 >>u (32 << u32[r10 + 0x60000])

        // Verify bounds for configuration index at offset 0x60000
        if (u32[r10 + 0x60000] <=u 13 >=u 0) {
            // @9b03
            let shift_base = 32
            let shift_base_alias = 32

            // Compare stored configuration value against calculated memory address offset
            if (u32[4 + ptr_0_48 + 0x60000] >=u u64[shift_base_alias >>u (shift_base_alias << (shift_base >>u (shift_base << (3 << bit_shift_factor))) + ptr_0_56) + 0x60000] <u 0) {
                // @9c29
                goto block_9c2e;
            } else {
                // @9bc8
                goto block_9be5;
            }
        }
    }

    block_9be5:
    // @9be5: Success path: Update record status flag and copy length value
    ptr_0_40->field_393216 = 1
    u32[4 + ptr_0_40 + 0x60000] = u32[8 + ptr_0_48 + 0x60000]

    block_9bcd:
    // @9bcd
    return

    block_9c2e:
    // @9c2e: Failure path: Reset record status flag and clear length field
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0
    goto block_9bcd;
}
fn func_39(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    if (0xFEFD0000 >=u r1 - 320) {
        // Calculate shift operand based on shared memory configuration at base 0x60000
        let shift_val = 32 >>u (32 << u32[r10 + 0x60000])

        if (u32[r10 + 0x60000] <=u 13 >=u 0) {
            let const_32 = 32
            let const_32_alt = 32

            // Compare indexed memory value against threshold at offset 4 in memory struct
            if (u64[const_32_alt >>u (const_32_alt << (const_32 >>u (const_32 << (3 << shift_val))) + ptr_0_56) + 0x60000] <u u32[4 + ptr_0_48 + 0x60000] <u 0) {
                goto block_9e18;
            } else {
                goto block_9dcf;
            }
        }
    }

    block_9dcf:
    // @9dcf: Set status flag and update memory struct field at offset 4
    ptr_0_40->field_393216 = 1
    u32[4 + ptr_0_40 + 0x60000] = u32[8 + ptr_0_48 + 0x60000]

    block_9db7:
    // @9db7
    return

    block_9e18:
    // @9e18: Clear status flag and reset memory struct field at offset 8
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0
    goto block_9db7;
}
fn func_40(input_ptr: u64, struct_base_ptr: u64, r9: u64, global_mem_base: u64, r11: u64, r12: u64) {
    if (0xFEFD0000 >=u input_ptr - 320) {
        // Calculate shift factor based on global memory offset 0x60000
        let shift_val = 32 >>u (32 << u32[global_mem_base + 0x60000])

        // Verify global memory index is within valid range [0, 13]
        if (u32[global_mem_base + 0x60000] <=u 13 >=u 0) {
            let var_27 = 32
            let var_38 = 32

            // Compare remote memory value against shifted calculated memory offset
            if (u32[4 + ptr_0_48 + 0x60000] <s u64[var_38 >>u (var_38 << (var_27 >>u (var_27 << (3 << shift_val))) + ptr_0_56) + 0x60000] <u 0) {
                // @9ffd
                goto block_a002;
            } else {
                // @9f9c
                goto block_9fb9;
            }
        }
    }

    block_9fb9:
    // @9fb9: Set status field to true and update value from input source
    ptr_0_40->field_393216 = 1
    u32[4 + ptr_0_40 + 0x60000] = u32[8 + ptr_0_48 + 0x60000]

    block_9fa1:
    // @9fa1
    return

    block_a002:
    // @a002: Set status field to false and clear related memory address
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0
    goto block_9fa1;
}
fn func_41(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    if (0xFEFD0000 >=u r1 - 320) {
        // @a03e: Calculate bit shift constant based on state in memory at r10+0x60000
        let shift_val = 32 >>u (32 << u32[r10 + 0x60000])

        if (u32[r10 + 0x60000] <=u 13 >=u 0) {
            // @a0c4: Threshold constants for bitwise indexing
            let var_27 = 32
            let var_38 = 32

            // Validate state bitmask against memory-mapped bounds
            if (u64[var_38 >>u (var_38 << (var_27 >>u (var_27 << (3 << shift_val))) + ptr_0_56) + 0x60000] >=s u32[4 + ptr_0_48 + 0x60000] <u 0) {
                // @a1ea
                goto block_a1ef;
            } else {
                // @a189
                goto block_a1a6;
            }
        }
    }

    block_a1a6:
    // @a1a6: Set success status and copy data to memory offset 0x60000
    ptr_0_40->field_393216 = 1
    u32[4 + ptr_0_40 + 0x60000] = u32[8 + ptr_0_48 + 0x60000]

    block_a18e:
    // @a18e
    return

    block_a1ef:
    // @a1ef: Reset operation status and clear memory at offset 0x60000
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0
    goto block_a18e;
}
fn func_42(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    if (0xFEFD0000 >=u r1 - 320) {
        // @a22b: Compute a bitmask or shift factor from memory-mapped state offset 0x60000
        let shift_factor = 32 >>u (32 << u32[r10 + 0x60000])

        if (u32[r10 + 0x60000] <=u 13 >=u 0) {
            // @a2b1: Constants for bitwise normalization
            let bit_mask_a = 32
            let bit_mask_b = 32

            // Validate memory bound against value stored at offset 0x60000 + 4
            if (u32[4 + ptr_0_48 + 0x60000] >=s u64[bit_mask_b >>u (bit_mask_b << (bit_mask_a >>u (bit_mask_a << (3 << shift_factor))) + ptr_0_56) + 0x60000] <u 0) {
                // @a3d7: Condition failed, route to error handling block
                goto block_a3dc;
            } else {
                // @a376: Condition met, proceed to set flags
                goto block_a393;
            }
        }
    }

    block_a393:
    // @a393: Success path: mark field as initialized/active
    ptr_0_40->field_393216 = 1
    u32[4 + ptr_0_40 + 0x60000] = u32[8 + ptr_0_48 + 0x60000]

    block_a37b:
    // @a37b: Standard exit
    return

    block_a3dc:
    // @a3dc: Error path: reset status fields
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0
    goto block_a37b;
}
fn func_43(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    if (0xFEFD0000 >=u r1 - 320) {
        // @a418: Calculate shift exponent based on memory-mapped state at r10+0x60000
        let shift_val = 32 >>u (32 << u32[r10 + 0x60000])

        if (u32[r10 + 0x60000] <=u 13 >=u 0) {
            // @a49e: Constants used for bitwise bounds calculation
            let bit_width = 32
            let mem_base = 32

            // Compare state field against offset calculation
            if (u64[mem_base >>u (mem_base << (bit_width >>u (bit_width << (3 << shift_val))) + ptr_0_56) + 0x60000] <s u32[4 + ptr_0_48 + 0x60000] <u 0) {
                // @a5c1: Invalid state condition
                goto block_a5c6;
            } else {
                // @a560: Valid state condition
                goto block_a57d;
            }
        }
    }

    block_a57d:
    // @a57d: Success path: mark record as valid and update field_393216
    ptr_0_40->field_393216 = 1
    u32[4 + ptr_0_40 + 0x60000] = u32[8 + ptr_0_48 + 0x60000]

    block_a565:
    // @a565: Exit function
    return

    block_a5c6:
    // @a5c6: Failure path: clear status flag and reset destination field
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0
    goto block_a565;
}
fn func_44(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val

    if (0xFEFD0000 >=u r1 - 440) {
        // @a602: Calculate shift based on memory-mapped index
        bit_shift_val = 32 >>u (32 << u32[r10 + 0x60000])

        if (u32[r10 + 0x60000] <=u 13 >=u 0) {
            // @a688: Access memory indexed by state bit_shift_val and global counter
            let state_val_a = u64[32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val))) + ptr_0_56) + 0x60000]
            let bit_shift_val_alt = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

            if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                // @a775: Compare values derived from state offsets
                let var_70 = 32
                let var_81 = 32

                if (!(u64[var_81 >>u (var_81 << (var_70 >>u (var_70 << (3 << bit_shift_val_alt))) + ptr_0_56) + 0x60000] ^ state_val_a) <u 0) {
                    // @a87f
                    goto block_a884;
                } else {
                    // @a81e
                    goto block_a83b;
                }
            }
        } else {
            // @a680
            bit_shift_val = 12
        }
    }

    block_a83b:
    // @a83b: Set success status and update field from input pointer
    ptr_0_40->field_393216 = 1
    u32[4 + ptr_0_40 + 0x60000] = u32[8 + ptr_0_48 + 0x60000]

    block_a823:
    // @a823
    return

    block_a884:
    // @a884: Reset status and clear associated field
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0
    goto block_a823;
}
fn func_45(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val

    if (0xFEFD0000 >=u r1 - 440) {
        // Calculate shift factor based on memory state at 0x60000 offset
        bit_shift_val = 32 >>u (32 << u32[r10 + 0x60000])

        if (u32[r10 + 0x60000] <=u 13 >=u 0) {
            // Retrieve lookup value based on bit-shifted address calculation
            let val_a = u64[32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val))) + ptr_0_56) + 0x60000]
            let val_b = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

            if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                let shift_const = 32
                
                // Perform XOR comparison on memory-mapped structures
                if ((u64[shift_const >>u (shift_const << (shift_const >>u (shift_const << (3 << val_b))) + ptr_0_56) + 0x60000] ^ val_a) <u 0 <u 0) {
                    goto block_ab44;
                } else {
                    goto block_aafb;
                }
            }
        } else {
            bit_shift_val = 12
        }
    }

    block_aafb:
    // Success path: set field and update state
    ptr_0_40->field_393216 = 1
    u32[4 + ptr_0_40 + 0x60000] = u32[8 + ptr_0_48 + 0x60000]

    block_aae3:
    return

    block_ab44:
    // Failure path: clear field and reset state
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0
    goto block_aae3;
}
fn func_46(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let index_val

    // Check bounds against memory offset
    if (0xFEFD0000 >=u r1 - 440) {
        // @ab80: Calculate index via bit-shift on memory field
        index_val = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @ac14: Fetch value from a lookup table at offset 0x60000
            let target_val = u64[32 >>u (32 << (32 >>u (32 << (3 << index_val))) + ptr_0_56) + 0x60000]
            let shift_idx = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @acf3: Compare target_val against nested memory structure
                let const_32 = 32
                let const_32_alt = 32

                if (u64[const_32_alt >>u (const_32_alt << (const_32 >>u (const_32 << (3 << shift_idx))) + ptr_0_56) + 0x60000] <u target_val <u 0) {
                    // @adfa
                    goto block_adff;
                } else {
                    // @ad99
                    goto block_adb6;
                }
            }
        } else {
            // @ac0c: Fallback index
            index_val = 12
        }
    }

    block_adb6:
    // @adb6: Target path A: modify fields of object ptr_0_40
    ptr_0_40->field_393216 = 1
    u32[4 + ptr_0_40 + 0x60000] = u32[8 + ptr_0_48 + 0x60000]

    block_ad9e:
    // @ad9e
    return

    block_adff:
    // @adff: Target path B: clear fields of object ptr_0_40
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0
    goto block_ad9e;
}
fn func_47(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val;
    let state_base = 0x60000;

    if (0xFEFD0000 >=u r1 - 440) {
        // Calculate shift amount based on memory-mapped state
        bit_shift_val = 32 >>u (32 << u32[4 + r10 + state_base])

        if (u32[4 + r10 + state_base] <=u 13 >=u 0) {
            // Retrieve comparison keys from state memory
            let key_a = u64[32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val))) + ptr_0_56) + state_base]
            let key_b_shift = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                let var_70 = 32
                let var_81 = 32

                // Compare values retrieved using bit-manipulated offsets
                if (u64[var_81 >>u (var_81 << (var_70 >>u (var_70 << (3 << key_b_shift))) + ptr_0_56) + state_base] <s key_a <u 0) {
                    // @b0b5
                    goto block_b0ba;
                } else {
                    // @b054
                    goto block_b071;
                }
            }
        } else {
            // @aec7
            bit_shift_val = 12
        }
    }

    block_b071:
    // Update state fields on successful comparison
    ptr_0_40->field_393216 = 1
    u32[4 + ptr_0_40 + state_base] = u32[8 + ptr_0_48 + state_base]

    block_b059:
    return

    block_b0ba:
    // Clear state fields on failed comparison
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + state_base] = 0
    goto block_b059;
}
fn func_48(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let lookup_idx

    // Validate memory boundary against r1 (possibly a buffer size or pointer check)
    if (0xFEFD0000 >=u r1 - 440) {
        // Calculate bit-shift lookup index from state memory
        lookup_idx = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // Retrieve value from memory using shifted index calculation
            let memory_val = u64[32 >>u (32 << (32 >>u (32 << (3 << lookup_idx))) + ptr_0_56) + 0x60000]
            let shift_factor = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                let shift_const = 32
                let shift_const_alt = 32

                // Compare memory value at current state against retrieve value
                if (u64[shift_const_alt >>u (shift_const_alt << (shift_const >>u (shift_const << (3 << shift_factor))) + ptr_0_56) + 0x60000] >=u memory_val <u 0) {
                    // @b373
                    goto block_b378;
                } else {
                    // @b312
                    goto block_b32f;
                }
            }
        } else {
            // @b182: Default error index
            lookup_idx = 12
        }
    }

    block_b32f:
    // Update state fields on mismatch
    ptr_0_40->field_393216 = 1
    u32[4 + ptr_0_40 + 0x60000] = u32[8 + ptr_0_48 + 0x60000]

    block_b317:
    return

    block_b378:
    // Successful validation logic
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0
    goto block_b317;
}
fn func_49(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val

    if (0xFEFD0000 >=u r1 - 440) {
        // Calculate shift based on index stored at 0x60004 offset
        bit_shift_val = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // Retrieve value from memory via calculated offset + global state base 0x60000
            let target_val = u64[32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val))) + ptr_0_56) + 0x60000]
            let secondary_shift = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // Secondary check against global storage
                let size_const = 32
                if (u64[size_const >>u (size_const << (size_const >>u (size_const << (3 << secondary_shift))) + ptr_0_56) + 0x60000] >=s target_val <u 0) {
                    goto block_b636;
                } else {
                    goto block_b5ed;
                }
            }
        } else {
            // Default shift value if index exceeds bounds
            bit_shift_val = 12
        }
    }

    block_b5ed:
    // Set status flag to 1 and update secondary state field
    ptr_0_40->field_393216 = 1
    u32[4 + ptr_0_40 + 0x60000] = u32[8 + ptr_0_48 + 0x60000]

    block_b5d5:
    return

    block_b636:
    // Reset status flag and clear secondary state field
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0
    goto block_b5d5;
}
fn func_50(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    // Check if bounds align with protected configuration memory region
    if (0xFEFD0000 >=u r1 - 112) {
        // @b671
        // Set configuration flag at offset 0x60000 to true (1)
        u32[r9 + 0x60000] = 1
        // Copy word from source context (r10) to destination context (r9) config area
        u32[4 + r9 + 0x60000] = u32[r10 + 0x60000]
    }

    // @b6c1
    return
}
fn func_51(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let shift_mask

    // Check bounds against memory offset
    if (0xFEFD0000 >=u r1 - 272) {
        // @b6ec
        // Calculate bitwise mask using state from memory at r10 + 0x60000
        shift_mask = 32 >>u (32 << u32[r10 + 0x60000])

        // Verify index state within valid range [0, 13]
        if (u32[r10 + 0x60000] <=u 13 >=u 0) {
            // @b772
            ptr_0_40->field_393216 = 2
            // Complex memory-based state update using calculated shift_mask and shifted offsets
            u32[8 + ptr_0_40 + 0x60000] = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000] + u64[32 >>u (32 << (32 >>u (32 << (3 << shift_mask))) + ptr_0_56) + 0x60000])
            goto block_b84b;
        } else {
            // @b76a
            // Fallback shift value for out-of-bounds state
            shift_mask = 12
        }
    }

    block_b84b:
    // @b84b
    return
}
fn func_52(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bitmask_shift

    // Check if remaining buffer space is within bounds (r1 - 264)
    if (0xFEFD0000 >=u r1 - 264) {
        // Calculate shift factor based on state value at memory offset 0x60000
        bitmask_shift = 32 >>u (32 << u32[r10 + 0x60000])

        // Validate state index within range [0, 13]
        if (u32[r10 + 0x60000] <=u 13 >=u 0) {
            // Apply state and write to memory block 0x60000
            u64[32 >>u (32 << (32 >>u (32 << (3 << bitmask_shift))) + ptr_0_56) + 0x60000] = u32[4 + ptr_0_48 + 0x60000]
            ptr_0_40->field_393216 = 1
            // Copy secondary state field
            u32[4 + ptr_0_40 + 0x60000] = u32[8 + ptr_0_48 + 0x60000]
            goto block_b9c7;
        } else {
            // Default shift value for out-of-range state
            bitmask_shift = 12
        }
    }

    block_b9c7:
    // @b9c7
    return
}
fn func_53(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let shift_op_128
    let target_val_232
    let shift_op_304

    // Check bounds: ensure r1 is within valid memory range offset by 424
    if (0xFEFD0000 >=u r1 - 424) {
        // @b9f4: Calculate shift based on memory-mapped configuration index at 0x60000
        shift_op_128 = 32 >>u (32 << u32[r10 + 0x60000])

        // Verify configuration index is within valid range [0, 13]
        if (u32[r10 + 0x60000] <=u 13 >=u 0) {
            // @ba7a: Compute storage values based on memory offsets and bit-shifted pointers
            target_val_232 = 32 >>u (32 << u32[12 + ptr_0_48 + 0x60000] + u64[32 >>u (32 << (32 >>u (32 << (3 << shift_op_128))) + ptr_0_56) + 0x60000])
            shift_op_304 = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

            // Check secondary configuration index validity
            if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                // @bbad: Write computed value to calculated memory address
                u64[32 >>u (32 << (32 >>u (32 << (3 << shift_op_304))) + ptr_0_56) + 0x60000] = u32[8 + ptr_0_48 + 0x60000]
                ptr_0_40->field_393216 = 2
                u32[8 + ptr_0_40 + 0x60000] = target_val_232
                goto block_bc5c;
            } else {
                // @bba5: Fallback shift index
                shift_op_304 = 12
            }
        } else {
            // @ba72: Fallback shift index
            shift_op_128 = 12
        }
    }

    block_bc5c:
    // @bc5c
    return
}
fn func_54(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val

    // Check bounds against memory offset 280
    if (0xFEFD0000 >=u r1 - 280) {
        // Calculate shift factor based on index at r10 + 0x60000
        bit_shift_val = 32 >>u (32 << u32[r10 + 0x60000])

        // Verify index is within valid range [0, 13]
        if (u32[r10 + 0x60000] <=u 13 >=u 0) {
            // Write calculated value to memory table at offset 0x60000
            u64[32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val))) + ptr_0_56) + 0x60000] = (32 << u32[8 + ptr_0_48 + 0x60000]) + u32[4 + ptr_0_48 + 0x60000]
            
            // Zero out struct fields and status
            ptr_0_40->field_393216 = 0
            u32[8 + ptr_0_40 + 0x60000] = 0
            goto block_bee8;
        } else {
            // Set default shift value on out-of-bounds index
            bit_shift_val = 12
        }
    }

    block_bee8:
    return
}
fn func_55(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bitmask_offset

    // Check if remaining buffer space is at least 248 bytes
    if (0xFEFD0000 >=u r1 - 248) {
        // Calculate shift amount based on memory-mapped index
        bitmask_offset = 32 >>u (32 << u32[r10 + 0x60000])

        // Verify index range validity (0 <= index <= 13)
        if (u32[r10 + 0x60000] <=u 13 >=u 0) {
            // Apply bitwise mask to calculate memory address and store header constant
            u64[32 >>u (32 << (32 >>u (32 << (3 << bitmask_offset))) + ptr_0_56) + 0x60000] = u32[4 + ptr_0_48 + 0x60000]
            
            // Reset control structure at ptr_0_40
            ptr_0_40->field_393216 = 0
            u32[8 + ptr_0_40 + 0x60000] = 0
            goto block_c047;
        } else {
            // Default shift value for out-of-bounds index
            bitmask_offset = 12
        }
    }

    block_c047:
    // @c047
    return
}
fn func_57(r1: u64) {
    // @dbc1
    let input_val = u8[u64[r1 + 48] + 0x60000] // Load byte from base address offset 0x60000
    u64[r1 + 96] = input_val
    u64[r1 + 104] = input_val
    let cond_0 = input_val <u 0
    u64[r1 + 112] = cond_0

    if (input_val <u 0) {
        // @dc0f
        goto block_dc14;
    } else {
        // @dbee
    }

    // @dc0a

    block_dbf3:
    // @dbf3
    r7 = u64[r1 + 120]
    return

    block_dc14:
    // @dc14
    let struct_ptr = 8 + u64[r1 + 40]
    u64[r1 + 128] = struct_ptr
    let offset_val = u32[struct_ptr + 0x60000]
    u64[r1 + 136] = offset_val
    let data_ptr = 4 + offset_val
    u64[r1 + 144] = data_ptr
    let cluster_size = u32[data_ptr + 0x60000]
    u64[r1 + 152] = cluster_size
    let base_idx = u64[r1 + 64]
    u64[r1 + 160] = base_idx
    let mask_idx = 255 & base_idx // Mask lower 8 bits
    u64[r1 + 168] = mask_idx
    let shift_op = 32 >>u (32 << mask_idx) // Bitwise normalization
    u64[r1 + 176] = shift_op
    u64[r1 + 184] = cluster_size
    u64[r1 + 192] = shift_op
    let sum_val = shift_op + cluster_size
    u64[r1 + 200] = sum_val
    let max_boundary = 32 >>u (32 << sum_val)
    u64[r1 + 208] = max_boundary
    u64[r1 + 216] = max_boundary
    let cond_1 = max_boundary <u 0x1000
    u64[r1 + 224] = cond_1
    u64[r1 + 232] = cond_1
    u64[r1 + 240] = cond_1
    let cond_2 = cond_1 <u 0
    u64[r1 + 248] = cond_2
    u64[r1 + 256] = max_boundary

    if (cond_1 >=u 0) {
        // @dcfe
        goto block_dd03;
    } else {
        // @dcf5
        u64[r1 + 256] = 0x1000 // Clamp to max 0x1000
    }

    // @dcf5
    u64[r1 + 256] = 0x1000

    // @dcfe

    block_dd03:
    while (cond_3 >=u 0) {
        // @dd03
        let iter_count = u64[r1 + 272]
        u64[r1 + 280] = iter_count
        let limit = u64[r1 + 256]
        u64[r1 + 288] = limit
        let cond_3 = limit <u iter_count
        u64[r1 + 296] = cond_3
        u64[r1 + 304] = cond_3
        u64[r1 + 312] = cond_3
        let cond_4 = cond_3 <u 0
        u64[r1 + 320] = cond_4

        // @dd78
        let ptr_a = 8 + u64[r1 + 40]
        u64[r1 + 344] = ptr_a
        let val_a = u32[ptr_a + 0x60000]
        u64[r1 + 352] = val_a
        let val_b = u32[val_a + 0x60000]
        u64[r1 + 360] = val_b
        let ptr_b = 4 + val_b
        u64[r1 + 368] = ptr_b
        let cluster_len = u32[ptr_b + 0x60000]
        u64[r1 + 376] = cluster_len
        let current_ptr = u64[r1 + 272]
        u64[r1 + 384] = current_ptr
        u64[r1 + 392] = cluster_len
        let combined_idx = cluster_len + current_ptr
        u64[r1 + 400] = combined_idx
        let mask_val = 32 >>u (32 << combined_idx)
        u64[r1 + 408] = mask_val
        let byte_read = u8[mask_val + 0x60000]
        u64[r1 + 416] = byte_read
        let shift_factor = 8 << u64[r1 + 264]
        u64[r1 + 424] = shift_factor
        u64[r1 + 432] = shift_factor | byte_read
        let next_idx = u64[r1 + 272]
        u64[r1 + 440] = next_idx
        let incremented = 1 + next_idx
        u64[r1 + 448] = incremented
        u64[r1 + 456] = 32 >>u (32 << incremented)
    }

    // @de46

    // @de4b

    // @dd59
    let header_ptr = 8 + u64[r1 + 40]
    u64[r1 + 328] = header_ptr
    u64[r1 + 336] = u32[header_ptr + 0x60000]

    for (let ptr_3 = header_ptr; cond_5 >=u 0; ptr_3 = u64[r1 + 472]) {
        // @de50
        let entry_ptr = 12 + u64[r1 + 336]
        u64[r1 + 480] = entry_ptr
        let entry_val = u32[entry_ptr + 0x60000]
        u64[r1 + 488] = entry_val
        let loop_limit = u64[r1 + 472]
        u64[r1 + 496] = loop_limit
        u64[r1 + 504] = entry_val
        let cond_5 = entry_val <u loop_limit
        u64[r1 + 512] = cond_5
        u64[r1 + 520] = cond_5
        u64[r1 + 528] = cond_5
        let cond_6 = cond_5 <u 0
        u64[r1 + 536] = cond_6

        // @dec6
        let meta_ptr = 8 + u64[r1 + 336]
        u64[r1 + 544] = meta_ptr
        let meta_val = u32[meta_ptr + 0x60000]
        u64[r1 + 552] = meta_val
        let item_ptr = 4 + meta_val
        u64[r1 + 560] = item_ptr
        let item_size = u32[item_ptr + 0x60000]
        u64[r1 + 568] = item_size
        let iteration = u64[r1 + 472]
        u64[r1 + 576] = iteration
        u64[r1 + 584] = item_size
        let total_offset = item_size + iteration
        u64[r1 + 592] = total_offset
        let final_mask = 32 >>u (32 << total_offset)
        u64[r1 + 600] = final_mask
        let final_byte = u8[final_mask + 0x60000]
        u64[r1 + 608] = final_byte
        let shift_val = 8 << u64[r1 + 464]
        u64[r1 + 616] = shift_val
        u64[r1 + 624] = shift_val | final_byte
        u64[r1 + 632] = ptr_3
        let next_ptr = 1 + ptr_3
        u64[r1 + 640] = next_ptr
        u64[r1 + 648] = 32 >>u (32 << next_ptr)
    }

    // @df88

    // @df8d

    // @dec1
    goto block_dbf3;
}
fn func_58(r1: u64, r7: u64) {
    // @e002
    u64[r1 + 96] = r7
    u64[r1 + 104] = r7
    // Normalize boolean/integer: (32 << x) results in 0 (if x>=5) or a power of 2; 32 >> ... acts as boolean conversion
    let is_not_zero_r7 = 32 >>u (32 << r7)
    u64[r1 + 112] = is_not_zero_r7
    u64[r1 + 120] = is_not_zero_r7
    let byte_val = 255 & is_not_zero_r7
    u64[r1 + 128] = byte_val
    let is_not_zero_byte = 32 >>u (32 << byte_val)
    u64[r1 + 136] = is_not_zero_byte
    u64[r1 + 144] = is_not_zero_byte
    u64[r1 + 152] = 32 >>u (32 << is_not_zero_byte)
    let global_idx = u32[24]
    u64[r1 + 160] = global_idx
    let global_byte = u8[global_idx + 0x60000] // Access global state memory
    u64[r1 + 168] = global_byte
    u64[r1 + 176] = global_byte
    let is_zero_flag = !(global_byte)
    u64[r1 + 184] = is_zero_flag
    u64[r1 + 192] = is_zero_flag
    u64[r1 + 200] = is_zero_flag
    let is_neg_flag = is_zero_flag <u 0 // Always false for u64 logical comparison
    u64[r1 + 208] = is_neg_flag

    if (is_zero_flag <u 0) {
        // @e1c0
        goto block_e1c5;
    } else {
        // @e0bf
    }

    // @e0dc: Process path if flag is non-negative
    let mode_val = u32[u64[r1 + 48] + 0x60000]
    u64[r1 + 224] = mode_val
    u64[r1 + 232] = mode_val
    let is_set_mode = 32 >>u (32 << mode_val)
    u64[r1 + 240] = is_set_mode
    let is_in_range = mode_val <=u 13
    u64[r1 + 248] = is_in_range
    u64[r1 + 256] = is_in_range
    u64[r1 + 264] = is_in_range
    let is_neg_range = is_in_range <u 0
    u64[r1 + 272] = is_neg_range
    u64[r1 + 280] = is_set_mode

    if (is_in_range >=u 0) {
        // @e152: Logic branch for valid range
        let base_offset = u64[r1 + 280]
        u64[r1 + 288] = base_offset
        let bit_shift = 3 << base_offset
        u64[r1 + 296] = bit_shift
        let normalized_shift = 32 >>u (32 << bit_shift)
        u64[r1 + 304] = normalized_shift
        let ext_val = u64[r1 + 56]
        u64[r1 + 312] = ext_val
        u64[r1 + 320] = normalized_shift
        let total_val = normalized_shift + ext_val
        u64[r1 + 328] = total_val
        let final_addr = 32 >>u (32 << total_val)
        u64[r1 + 336] = final_addr
        u64[final_addr + 0x60000] = u64[r1 + 152]
        goto block_e1c5;
    } else {
        // @e14a: Fallback case
        u64[r1 + 280] = 12
    }

    // @e14a
    u64[r1 + 280] = 12

    // @e152: Repeated block logic flow
    var_62 = u64[r1 + 280]
    u64[r1 + 288] = var_62
    var_66 = 3 << var_62
    u64[r1 + 296] = var_66
    var_70 = 32 >>u (32 << var_66)
    u64[r1 + 304] = var_70
    var_71 = u64[r1 + 56]
    u64[r1 + 312] = var_71
    u64[r1 + 320] = var_70
    var_77 = var_70 + var_71
    u64[r1 + 328] = var_77
    var_81 = 32 >>u (32 << var_77)
    u64[r1 + 336] = var_81
    u64[var_81 + 0x60000] = u64[r1 + 152]

    block_e1c5:
    // @e1c5
    let current_idx = u32[24]
    u64[r1 + 352] = current_idx
    let entry_byte = u8[current_idx + 0x60000]
    u64[r1 + 360] = entry_byte
    u64[r1 + 368] = entry_byte
    let cond_5 = entry_byte <u 0
    u64[r1 + 376] = cond_5

    if (entry_byte <u 0) {
        // @e2b7
        goto block_e2bc;
    } else {
        // @e203
    }
    // @e208
    u32[u64[r1 + 40] + 0x60000] = 3
    let next_idx = 1 + u64[r1 + 352]
    u64[r1 + 392] = next_idx
    let next_byte = u8[next_idx + 0x60000]
    u64[r1 + 400] = next_byte
    u64[r1 + 408] = next_byte
    let cond_6 = next_byte <u 0
    u64[r1 + 416] = cond_6
    u64[r1 + 424] = 1

    if (next_byte >=u 0) {
        // @e263
        let target_ptr = 12 + u64[r1 + 40]
        u64[r1 + 432] = target_ptr
        u32[target_ptr + 0x60000] = u64[r1 + 424]
        let offset_idx = 4 + u64[r1 + 352]
        u64[r1 + 448] = offset_idx
        let offset_val = u32[offset_idx + 0x60000]
        u64[r1 + 456] = offset_val
        let write_ptr = 16 + u64[r1 + 40]
        u64[r1 + 464] = write_ptr
        u32[write_ptr + 0x60000] = offset_val
        goto block_e2bc;
    } else {
        // @e25b
        u64[r1 + 424] = 2
    }

    // @e25b
    u64[r1 + 424] = 2

    // @e263
    var_108 = 12 + u64[r1 + 40]
    u64[r1 + 432] = var_108
    u32[var_108 + 0x60000] = u64[r1 + 424]
    var_112 = 4 + u64[r1 + 352]
    u64[r1 + 448] = var_112
    var_113 = u32[var_112 + 0x60000]
    u64[r1 + 456] = var_113
    var_116 = 16 + u64[r1 + 40]
    u64[r1 + 464] = var_116
    u32[var_116 + 0x60000] = var_113

    // @e2b7

    block_e2bc:
    // @e2bc

    // @e0c4
    r7 = u64[r1 + 216]
    return
}
fn func_59(r1: u64, r7: u64) {
    // @e331
    u64[r1 + 96] = r7
    u64[r1 + 104] = r7
    // Normalize value using bitwise bit-field extraction pattern: 32 >> (32 << x)
    let norm_val_1 = 32 >>u (32 << r7)
    u64[r1 + 112] = norm_val_1
    u64[r1 + 120] = norm_val_1
    let masked_val = 255 & norm_val_1
    u64[r1 + 128] = masked_val
    let norm_val_2 = 32 >>u (32 << masked_val)
    u64[r1 + 136] = norm_val_2
    u64[r1 + 144] = norm_val_2
    u64[r1 + 152] = norm_val_2
    let sign_ext_val = sext8(norm_val_2)
    u64[r1 + 160] = sign_ext_val
    let norm_val_3 = 32 >>u (32 << sign_ext_val)
    u64[r1 + 168] = norm_val_3
    u64[r1 + 176] = norm_val_3
    u64[r1 + 184] = norm_val_3
    let global_offset = u32[24]
    u64[r1 + 192] = global_offset
    let status_byte = u8[global_offset + 0x60000] // Read relative to global memory base
    u64[r1 + 200] = status_byte
    u64[r1 + 208] = status_byte
    let cond_is_zero = !(status_byte)
    u64[r1 + 216] = cond_is_zero
    u64[r1 + 224] = cond_is_zero
    u64[r1 + 232] = cond_is_zero
    let cond_0 = cond_is_zero <u 0
    u64[r1 + 240] = cond_0

    if (cond_is_zero <u 0) {
        // @e517
        goto block_e51c;
    } else {
        // @e416
    }

    // @e433
    let config_val = u32[u64[r1 + 48] + 0x60000]
    u64[r1 + 256] = config_val
    u64[r1 + 264] = config_val
    let norm_config = 32 >>u (32 << config_val)
    u64[r1 + 272] = norm_config
    let cond_valid_range = config_val <=u 13
    u64[r1 + 280] = cond_valid_range
    u64[r1 + 288] = cond_valid_range
    u64[r1 + 296] = cond_valid_range
    let cond_4 = cond_valid_range <u 0
    u64[r1 + 304] = cond_4
    u64[r1 + 312] = norm_config

    if (cond_valid_range >=u 0) {
        // @e4a9
        let shift_idx = u64[r1 + 312]
        u64[r1 + 320] = shift_idx
        let mask = 3 << shift_idx
        u64[r1 + 328] = mask
        let masked_shift = 32 >>u (32 << mask)
        u64[r1 + 336] = masked_shift
        let base_ptr = u64[r1 + 56]
        u64[r1 + 344] = base_ptr
        u64[r1 + 352] = masked_shift
        let target_addr_raw = masked_shift + base_ptr
        u64[r1 + 360] = target_addr_raw
        let target_addr = 32 >>u (32 << target_addr_raw)
        u64[r1 + 368] = target_addr
        u64[target_addr + 0x60000] = u64[r1 + 184]
        goto block_e51c;
    } else {
        // @e4a1
        u64[r1 + 312] = 12
    }

    // @e4a1
    u64[r1 + 312] = 12

    // @e4a9 logic path continuation
    let var_70 = u64[r1 + 312]
    u64[r1 + 320] = var_70
    let var_74 = 3 << var_70
    u64[r1 + 328] = var_74
    let var_78 = 32 >>u (32 << var_74)
    u64[r1 + 336] = var_78
    let var_79 = u64[r1 + 56]
    u64[r1 + 344] = var_79
    u64[r1 + 352] = var_78
    let var_85 = var_78 + var_79
    u64[r1 + 360] = var_85
    let var_89 = 32 >>u (32 << var_85)
    u64[r1 + 368] = var_89
    u64[var_89 + 0x60000] = u64[r1 + 184]

    block_e51c:
    // @e51c
    let cur_global = u32[24]
    u64[r1 + 384] = cur_global
    let status_byte_2 = u8[cur_global + 0x60000]
    u64[r1 + 392] = status_byte_2
    u64[r1 + 400] = status_byte_2
    let cond_5 = status_byte_2 <u 0
    u64[r1 + 408] = cond_5

    if (status_byte_2 <u 0) {
        // @e60e
        goto block_e613;
    } else {
        // @e55a
    }
    // @e55f
    u32[u64[r1 + 40] + 0x60000] = 3
    let next_global = 1 + u64[r1 + 384]
    u64[r1 + 424] = next_global
    let status_byte_3 = u8[next_global + 0x60000]
    u64[r1 + 432] = status_byte_3
    u64[r1 + 440] = status_byte_3
    let cond_6 = status_byte_3 <u 0
    u64[r1 + 448] = cond_6
    u64[r1 + 456] = 1

    if (status_byte_3 >=u 0) {
        // @e5ba
        let state_ptr = 12 + u64[r1 + 40]
        u64[r1 + 464] = state_ptr
        u32[state_ptr + 0x60000] = u64[r1 + 456]
        let data_ptr = 4 + u64[r1 + 384]
        u64[r1 + 480] = data_ptr
        let data_val = u32[data_ptr + 0x60000]
        u64[r1 + 488] = data_val
        let dest_ptr = 16 + u64[r1 + 40]
        u64[r1 + 496] = dest_ptr
        u32[dest_ptr + 0x60000] = data_val
        goto block_e613;
    } else {
        // @e5b2
        u64[r1 + 456] = 2
    }

    // @e5b2
    u64[r1 + 456] = 2

    // @e5ba
    let var_116 = 12 + u64[r1 + 40]
    u64[r1 + 464] = var_116
    u32[var_116 + 0x60000] = u64[r1 + 456]
    let var_120 = 4 + u64[r1 + 384]
    u64[r1 + 480] = var_120
    let var_121 = u32[var_120 + 0x60000]
    u64[r1 + 488] = var_121
    let var_124 = 16 + u64[r1 + 40]
    u64[r1 + 496] = var_124
    u32[var_124 + 0x60000] = var_121

    block_e613:
    // @e613
    // @e41b
    r7 = u64[r1 + 248]
    return
}
fn func_60(r1: u64, r7: u64) {
    // @e664
    // Initialize storage structure at r1 offset
    u64[r1 + 64] = r7
    u64[r1 + 72] = r7
    
    // Normalize r7 to bitmask-like value
    let bitmask_base = 32 >>u (32 << r7)
    u64[r1 + 80] = bitmask_base
    u64[r1 + 88] = bitmask_base
    
    // Scale shift operation
    let scaled_val = 8 << bitmask_base
    u64[r1 + 96] = scaled_val
    
    let secondary_mask = 32 >>u (32 << scaled_val)
    u64[r1 + 104] = secondary_mask
    u64[r1 + 112] = bitmask_base
    
    // Extract lower 16 bits
    let truncated_val = 0xFFFF & bitmask_base
    u64[r1 + 120] = truncated_val
    
    let tertiary_mask = 32 >>u (32 << truncated_val)
    u64[r1 + 128] = tertiary_mask
    u64[r1 + 136] = tertiary_mask
    
    // Perform logical right shift on mask
    let shifted_mask = 8 >>u tertiary_mask
    u64[r1 + 144] = shifted_mask
    
    let quaternary_mask = 32 >>u (32 << shifted_mask)
    u64[r1 + 152] = quaternary_mask
    u64[r1 + 160] = secondary_mask
    u64[r1 + 168] = quaternary_mask
    
    // Combine masks
    let combined_mask = quaternary_mask | secondary_mask
    u64[r1 + 176] = combined_mask
    
    let final_mask = 32 >>u (32 << combined_mask)
    u64[r1 + 184] = final_mask
    u64[r1 + 192] = final_mask
    
    let final_truncated = 0xFFFF & final_mask
    u64[r1 + 200] = final_truncated
    
    let result_mask = 32 >>u (32 << final_truncated)
    u64[r1 + 208] = result_mask
    u64[r1 + 216] = result_mask
    u64[r1 + 224] = 32 >>u (32 << result_mask)

    // @e76f
    r7 = u64[r1 + 232]
    return
}
fn func_61(r1: u64, r7: u64) {
    // @e7f4
    u64[r1 + 96] = r7 // Store input r7 in structure
    let current_ptr = u32[24]
    u64[r1 + 104] = current_ptr
    let flags = u8[current_ptr + 0x60000] // Load status flags from memory
    u64[r1 + 112] = flags
    u64[r1 + 120] = flags
    let is_zero = !(flags)
    u64[r1 + 128] = is_zero
    u64[r1 + 136] = is_zero
    u64[r1 + 144] = is_zero
    let cond_1 = is_zero <u 0 // Sign-check of the inverted flag
    u64[r1 + 152] = cond_1

    if (is_zero <u 0) {
        // @e952
        goto block_e957;
    } else {
        // @e852
    }

    // @e86f
    let shift_val = u32[u64[r1 + 48] + 0x60000] // Load shift distance from memory
    u64[r1 + 168] = shift_val
    u64[r1 + 176] = shift_val
    let mask = 32 >>u (32 << shift_val) // Bit-mask calculation
    u64[r1 + 184] = mask
    let cond_3 = shift_val <=u 13
    u64[r1 + 192] = cond_3
    u64[r1 + 200] = cond_3
    u64[r1 + 208] = cond_3
    let cond_4 = cond_3 <u 0
    u64[r1 + 216] = cond_4
    u64[r1 + 224] = mask

    if (cond_3 >=u 0) {
        // @e8e5
        let index_val = u64[r1 + 224]
        u64[r1 + 232] = index_val
        let power_of_two = 3 << index_val // Bit-shift scaling
        u64[r1 + 240] = power_of_two
        let masked_val = 32 >>u (32 << power_of_two)
        u64[r1 + 248] = masked_val
        let base_offset = u64[r1 + 56]
        u64[r1 + 256] = base_offset
        u64[r1 + 264] = masked_val
        let final_addr = masked_val + base_offset
        u64[r1 + 272] = final_addr
        let aligned_addr = 32 >>u (32 << final_addr)
        u64[r1 + 280] = aligned_addr
        u64[aligned_addr + 0x60000] = u64[r1 + 96]
        goto block_e957;
    } else {
        // @e8dd
        u64[r1 + 224] = 12
    }

    // @e8dd
    u64[r1 + 224] = 12

    // @e8e5
    var_41 = u64[r1 + 224]
    u64[r1 + 232] = var_41
    var_45 = 3 << var_41
    u64[r1 + 240] = var_45
    var_49 = 32 >>u (32 << var_45)
    u64[r1 + 248] = var_49
    var_50 = u64[r1 + 56]
    u64[r1 + 256] = var_50
    u64[r1 + 264] = var_49
    var_56 = var_49 + var_50
    u64[r1 + 272] = var_56
    var_60 = 32 >>u (32 << var_56)
    u64[r1 + 280] = var_60
    u64[var_60 + 0x60000] = u64[r1 + 96]

    block_e957:
    // @e957
    let next_ptr = u32[24]
    u64[r1 + 296] = next_ptr
    let next_flags = u8[next_ptr + 0x60000]
    u64[r1 + 304] = next_flags
    u64[r1 + 312] = next_flags
    let cond_5 = next_flags <u 0
    u64[r1 + 320] = cond_5

    if (next_flags <u 0) {
        // @ea49
        goto block_ea4e;
    } else {
        // @e995
    }
    // @e99a
    u32[u64[r1 + 40] + 0x60000] = 3 // Write constant 3 to memory
    let alt_ptr = 1 + u64[r1 + 296]
    u64[r1 + 336] = alt_ptr
    let alt_flags = u8[alt_ptr + 0x60000]
    u64[r1 + 344] = alt_flags
    u64[r1 + 352] = alt_flags
    let cond_6 = alt_flags <u 0
    u64[r1 + 360] = cond_6
    u64[r1 + 368] = 1

    if (alt_flags >=u 0) {
        // @e9f5
        let write_target_1 = 12 + u64[r1 + 40]
        u64[r1 + 376] = write_target_1
        u32[write_target_1 + 0x60000] = u64[r1 + 368]
        let read_target = 4 + u64[r1 + 296]
        u64[r1 + 392] = read_target
        let loaded_val = u32[read_target + 0x60000]
        u64[r1 + 400] = loaded_val
        let write_target_2 = 16 + u64[r1 + 40]
        u64[r1 + 408] = write_target_2
        u32[write_target_2 + 0x60000] = loaded_val
        goto block_ea4e;
    } else {
        // @e9ed
        u64[r1 + 368] = 2
    }

    // @e9ed
    u64[r1 + 368] = 2

    // @e9f5
    var_87 = 12 + u64[r1 + 40]
    u64[r1 + 376] = var_87
    u32[var_87 + 0x60000] = u64[r1 + 368]
    var_91 = 4 + u64[r1 + 296]
    u64[r1 + 392] = var_91
    var_92 = u32[var_91 + 0x60000]
    u64[r1 + 400] = var_92
    var_95 = 16 + u64[r1 + 40]
    u64[r1 + 408] = var_95
    u32[var_95 + 0x60000] = var_92

    // @ea49

    block_ea4e:
    // @ea4e

    // @e857
    r7 = u64[r1 + 160] // Return value cached at offset 160
    return
}
fn func_62(r1: u64, r7: u64) {
    // @eac0
    u64[r1 + 96] = r7
    u64[r1 + 104] = r7
    let bit_mask_32 = 32 >>u (32 << r7) // Normalize value to 32 bits
    u64[r1 + 112] = bit_mask_32
    u64[r1 + 120] = bit_mask_32
    u64[r1 + 128] = bit_mask_32
    let sign_ext_val = sext16(bit_mask_32)
    u64[r1 + 136] = sign_ext_val
    let bit_mask_sext = 32 >>u (32 << sign_ext_val)
    u64[r1 + 144] = bit_mask_sext
    u64[r1 + 152] = bit_mask_sext
    u64[r1 + 160] = bit_mask_sext
    let pc_ptr = u32[24] // Global program counter/memory base index
    u64[r1 + 168] = pc_ptr
    let status_byte = u8[pc_ptr + 0x60000] // Load status byte from static memory
    u64[r1 + 176] = status_byte
    u64[r1 + 184] = status_byte
    let status_is_zero = !(status_byte)
    u64[r1 + 192] = status_is_zero
    u64[r1 + 200] = status_is_zero
    u64[r1 + 208] = status_is_zero
    let cond_is_negative = status_is_zero <u 0

    if (status_is_zero <u 0) {
        // @ec7c
        goto block_ec81;
    } else {
        // @eb7b
    }

    // @eb98
    let val_at_offset = u32[u64[r1 + 48] + 0x60000]
    u64[r1 + 232] = val_at_offset
    u64[r1 + 240] = val_at_offset
    let mask_at_offset = 32 >>u (32 << val_at_offset)
    u64[r1 + 248] = mask_at_offset
    let cond_le_13 = val_at_offset <=u 13
    u64[r1 + 256] = cond_le_13
    u64[r1 + 264] = cond_le_13
    u64[r1 + 272] = cond_le_13
    let cond_le_13_neg = cond_le_13 <u 0
    u64[r1 + 280] = cond_le_13_neg
    u64[r1 + 288] = mask_at_offset

    if (cond_le_13 >=u 0) {
        // @ec0e
        let val_idx = u64[r1 + 288]
        u64[r1 + 296] = val_idx
        let shifted_val = 3 << val_idx
        u64[r1 + 304] = shifted_val
        let mask_result = 32 >>u (32 << shifted_val)
        u64[r1 + 312] = mask_result
        let context_val = u64[r1 + 56]
        u64[r1 + 320] = context_val
        u64[r1 + 328] = mask_result
        let calc_dest = mask_result + context_val
        u64[r1 + 336] = calc_dest
        let dest_addr = 32 >>u (32 << calc_dest)
        u64[r1 + 344] = dest_addr
        u64[dest_addr + 0x60000] = u64[r1 + 160]
        goto block_ec81;
    } else {
        // @ec06
        u64[r1 + 288] = 12
    }

    // @ec06
    u64[r1 + 288] = 12

    // @ec0e logic repeated (fallthrough or jump target)
    let val_idx_r = u64[r1 + 288]
    u64[r1 + 296] = val_idx_r
    let shifted_val_r = 3 << val_idx_r
    u64[r1 + 304] = shifted_val_r
    let mask_result_r = 32 >>u (32 << shifted_val_r)
    u64[r1 + 312] = mask_result_r
    let context_val_r = u64[r1 + 56]
    u64[r1 + 320] = context_val_r
    u64[r1 + 328] = mask_result_r
    let calc_dest_r = mask_result_r + context_val_r
    u64[r1 + 336] = calc_dest_r
    let dest_addr_r = 32 >>u (32 << calc_dest_r)
    u64[r1 + 344] = dest_addr_r
    u64[dest_addr_r + 0x60000] = u64[r1 + 160]

    block_ec81:
    // @ec81
    let pc_ptr_curr = u32[24]
    u64[r1 + 360] = pc_ptr_curr
    let status_byte_curr = u8[pc_ptr_curr + 0x60000]
    u64[r1 + 368] = status_byte_curr
    u64[r1 + 376] = status_byte_curr
    let cond_byte_neg = status_byte_curr <u 0
    u64[r1 + 384] = cond_byte_neg

    if (status_byte_curr <u 0) {
        // @ed73
        goto block_ed78;
    } else {
        // @ecbf
    }
    // @ecc4
    u32[u64[r1 + 40] + 0x60000] = 3
    let next_pc = 1 + u64[r1 + 360]
    u64[r1 + 400] = next_pc
    let next_byte = u8[next_pc + 0x60000]
    u64[r1 + 408] = next_byte
    u64[r1 + 416] = next_byte
    let cond_next_neg = next_byte <u 0
    u64[r1 + 424] = cond_next_neg
    u64[r1 + 432] = 1

    if (next_byte >=u 0) {
        // @ed1f
        let target_ptr = 12 + u64[r1 + 40]
        u64[r1 + 440] = target_ptr
        u32[target_ptr + 0x60000] = u64[r1 + 432]
        let data_ptr = 4 + u64[r1 + 360]
        u64[r1 + 456] = data_ptr
        let data_val = u32[data_ptr + 0x60000]
        u64[r1 + 464] = data_val
        let write_ptr = 16 + u64[r1 + 40]
        u64[r1 + 472] = write_ptr
        u32[write_ptr + 0x60000] = data_val
        goto block_ed78;
    } else {
        // @ed17
        u64[r1 + 432] = 2
    }

    // @ed17
    u64[r1 + 432] = 2

    // @ed1f logic repeated
    let target_ptr_r = 12 + u64[r1 + 40]
    u64[r1 + 440] = target_ptr_r
    u32[target_ptr_r + 0x60000] = u64[r1 + 432]
    let data_ptr_r = 4 + u64[r1 + 360]
    u64[r1 + 456] = data_ptr_r
    let data_val_r = u32[data_ptr_r + 0x60000]
    u64[r1 + 464] = data_val_r
    let write_ptr_r = 16 + u64[r1 + 40]
    u64[r1 + 472] = write_ptr_r
    u32[write_ptr_r + 0x60000] = data_val_r

    block_ed78:
    // @ed78

    // @eb80
    r7 = u64[r1 + 224]
    return
}
fn func_63(r1: u64, r7: u64) {
    // @edc9
    // r1 acts as a base struct pointer for serialized state storage
    u64[r1 + 64] = r7
    u64[r1 + 72] = r7
    let bit_mask_a = 32 >>u (32 << r7)
    u64[r1 + 80] = bit_mask_a
    u64[r1 + 88] = bit_mask_a
    let masked_a = 0xFF00FF00 & bit_mask_a
    u64[r1 + 96] = masked_a
    let bit_mask_b = 32 >>u (32 << masked_a)
    u64[r1 + 104] = bit_mask_b
    u64[r1 + 112] = bit_mask_b
    // (31 & 8) resolves to constant 8
    let bit_rot_a = (31 & 8) - 32 >>u bit_mask_b | (31 & 8) << bit_mask_b
    u64[r1 + 120] = bit_rot_a
    let bit_mask_c = 32 >>u (32 << bit_rot_a)
    u64[r1 + 128] = bit_mask_c
    u64[r1 + 136] = bit_mask_a
    let masked_b = 0xFF00FF & bit_mask_a
    u64[r1 + 144] = masked_b
    let bit_mask_d = 32 >>u (32 << masked_b)
    u64[r1 + 152] = bit_mask_d
    u64[r1 + 160] = bit_mask_d
    let bit_rot_b = (31 & 8) >>u bit_mask_d | (31 & 8) - 32 << bit_mask_d
    u64[r1 + 168] = bit_rot_b
    let bit_mask_e = 32 >>u (32 << bit_rot_b)
    u64[r1 + 176] = bit_mask_e
    u64[r1 + 184] = bit_mask_c
    u64[r1 + 192] = bit_mask_e
    let combined_mask = bit_mask_e | bit_mask_c
    u64[r1 + 200] = combined_mask
    let final_mask_a = 32 >>u (32 << combined_mask)
    u64[r1 + 208] = final_mask_a
    u64[r1 + 216] = final_mask_a
    u64[r1 + 224] = 32 >>u (32 << final_mask_a)

    // @ef05
    // Load result from struct memory into register
    r7 = u64[r1 + 232]
    return
}
fn func_64(r1: u64, r7: u64) {
    // @ef8a
    u64[r1 + 96] = r7 // Store input r7 in structure offset 96
    let current_ptr = u32[24] // Global pointer address
    u64[r1 + 104] = current_ptr
    let flags = u8[current_ptr + 0x60000] // Load metadata/flags from heap
    u64[r1 + 112] = flags
    u64[r1 + 120] = flags
    let is_zero = !(flags)
    u64[r1 + 128] = is_zero
    u64[r1 + 136] = is_zero
    u64[r1 + 144] = is_zero
    let cond_1 = is_zero <u 0
    u64[r1 + 152] = cond_1

    if (is_zero <u 0) {
        // @f0e8
        goto block_f0ed;
    } else {
        // @efe8
    }

    // @f005
    let index_val = u32[u64[r1 + 48] + 0x60000]
    u64[r1 + 168] = index_val
    u64[r1 + 176] = index_val
    let masked_val = 32 >>u (32 << index_val) // Bit-mask arithmetic
    u64[r1 + 184] = masked_val
    let in_range = index_val <=u 13
    u64[r1 + 192] = in_range
    u64[r1 + 200] = in_range
    u64[r1 + 208] = in_range
    let cond_4 = in_range <u 0
    u64[r1 + 216] = cond_4
    u64[r1 + 224] = masked_val

    if (in_range >=u 0) {
        // @f07b
        let bit_shift_val = u64[r1 + 224]
        u64[r1 + 232] = bit_shift_val
        let shift_calc = 3 << bit_shift_val
        u64[r1 + 240] = shift_calc
        let mask = 32 >>u (32 << shift_calc)
        u64[r1 + 248] = mask
        let offset_base = u64[r1 + 56]
        u64[r1 + 256] = offset_base
        u64[r1 + 264] = mask
        let final_addr = mask + offset_base
        u64[r1 + 272] = final_addr
        let aligned_addr = 32 >>u (32 << final_addr)
        u64[r1 + 280] = aligned_addr
        u64[aligned_addr + 0x60000] = u64[r1 + 96] // Write cached r7 to calculated heap address
        goto block_f0ed;
    } else {
        // @f073
        u64[r1 + 224] = 12
    }

    // @f073 - Fallback/Default path
    u64[r1 + 224] = 12

    // @f07b - Repeated logic due to jump structure
    var_41 = u64[r1 + 224]
    u64[r1 + 232] = var_41
    var_45 = 3 << var_41
    u64[r1 + 240] = var_45
    var_49 = 32 >>u (32 << var_45)
    u64[r1 + 248] = var_49
    var_50 = u64[r1 + 56]
    u64[r1 + 256] = var_50
    u64[r1 + 264] = var_49
    var_56 = var_49 + var_50
    u64[r1 + 272] = var_56
    var_60 = 32 >>u (32 << var_56)
    u64[r1 + 280] = var_60
    u64[var_60 + 0x60000] = u64[r1 + 96]

    block_f0ed:
    // @f0ed
    let next_ptr = u32[24]
    u64[r1 + 296] = next_ptr
    let next_flags = u8[next_ptr + 0x60000]
    u64[r1 + 304] = next_flags
    u64[r1 + 312] = next_flags
    let cond_5 = next_flags <u 0
    u64[r1 + 320] = cond_5

    if (next_flags <u 0) {
        // @f1df
        goto block_f1e4;
    } else {
        // @f12b
    }
    // @f130
    u32[u64[r1 + 40] + 0x60000] = 3
    let next_offset = 1 + u64[r1 + 296]
    u64[r1 + 336] = next_offset
    let next_val = u8[next_offset + 0x60000]
    u64[r1 + 344] = next_val
    u64[r1 + 352] = next_val
    let cond_6 = next_val <u 0
    u64[r1 + 360] = cond_6
    u64[r1 + 368] = 1 // Default status code

    if (next_val >=u 0) {
        // @f18b
        let status_field = 12 + u64[r1 + 40]
        u64[r1 + 376] = status_field
        u32[status_field + 0x60000] = u64[r1 + 368]
        let data_idx = 4 + u64[r1 + 296]
        u64[r1 + 392] = data_idx
        let data_val = u32[data_idx + 0x60000]
        u64[r1 + 400] = data_val
        let target_field = 16 + u64[r1 + 40]
        u64[r1 + 408] = target_field
        u32[target_field + 0x60000] = data_val
        goto block_f1e4;
    } else {
        // @f183
        u64[r1 + 368] = 2
    }

    // @f183
    u64[r1 + 368] = 2

    // @f18b
    var_87 = 12 + u64[r1 + 40]
    u64[r1 + 376] = var_87
    u32[var_87 + 0x60000] = u64[r1 + 368]
    var_91 = 4 + u64[r1 + 296]
    u64[r1 + 392] = var_91
    var_92 = u32[var_91 + 0x60000]
    u64[r1 + 400] = var_92
    var_95 = 16 + u64[r1 + 40]
    u64[r1 + 408] = var_95
    u32[var_95 + 0x60000] = var_92

    // @f1df

    block_f1e4:
    // @f1e4
    r7 = u64[r1 + 160] // Restore or return final result from struct
    return
}
fn func_65(r1: u64, r7: u64) {
    // @f256
    // Initialize memory slots with input register r7
    u64[r1 + 96] = r7
    u64[r1 + 104] = r7
    u64[r1 + 112] = r7
    
    let base_offset = u32[24] // Global base offset from memory address 24
    u64[r1 + 120] = base_offset
    
    let base_val = u8[base_offset + 0x60000] // Load byte relative to global offset
    u64[r1 + 128] = base_val
    u64[r1 + 136] = base_val
    
    let is_zero = !(base_val) // Check if byte is null
    u64[r1 + 144] = is_zero
    u64[r1 + 152] = is_zero
    u64[r1 + 160] = is_zero
    
    let cond_0 = is_zero <u 0 // Always false for u8
    u64[r1 + 168] = cond_0

    if (cond_0 <u 0) {
        // @f3c8
        goto block_f3cd;
    } else {
        // @f2c8
    }

    // @f2e5
    // Calculate mask based on memory reference at r1 + 48
    let mask_input = u32[u64[r1 + 48] + 0x60000]
    u64[r1 + 184] = mask_input
    u64[r1 + 192] = mask_input
    
    let bit_mask = 32 >>u (32 << mask_input) // Standard bit-masking operation
    u64[r1 + 200] = bit_mask
    
    let is_lth_13 = mask_input <=u 13
    u64[r1 + 208] = is_lth_13
    u64[r1 + 216] = is_lth_13
    u64[r1 + 224] = is_lth_13
    
    let cond_4 = is_lth_13 <u 0
    u64[r1 + 232] = cond_4
    u64[r1 + 240] = bit_mask

    if (is_lth_13 >=u 0) {
        // @f35b
        let shift_val = u64[r1 + 240]
        u64[r1 + 248] = shift_val
        let shift_calc = 3 << shift_val
        u64[r1 + 256] = shift_calc
        let calc_res = 32 >>u (32 << shift_calc)
        u64[r1 + 264] = calc_res
        let offset_val = u64[r1 + 56]
        u64[r1 + 272] = offset_val
        u64[r1 + 280] = calc_res
        let final_addr = calc_res + offset_val
        u64[r1 + 288] = final_addr
        let target_idx = 32 >>u (32 << final_addr)
        u64[r1 + 296] = target_idx
        u64[target_idx + 0x60000] = u64[r1 + 112]
        goto block_f3cd;
    } else {
        // @f353
        u64[r1 + 240] = 12
    }

    // Fallthrough: set default shift and apply logic
    u64[r1 + 240] = 12
    var_45 = u64[r1 + 240]
    u64[r1 + 248] = var_45
    var_49 = 3 << var_45
    u64[r1 + 256] = var_49
    var_53 = 32 >>u (32 << var_49)
    u64[r1 + 264] = var_53
    var_54 = u64[r1 + 56]
    u64[r1 + 272] = var_54
    u64[r1 + 280] = var_53
    var_60 = var_53 + var_54
    u64[r1 + 288] = var_60
    var_64 = 32 >>u (32 << var_60)
    u64[r1 + 296] = var_64
    u64[var_64 + 0x60000] = u64[r1 + 112]

    block_f3cd:
    // @f3cd
    let secondary_ptr = u32[24]
    u64[r1 + 312] = secondary_ptr
    let secondary_val = u8[secondary_ptr + 0x60000]
    u64[r1 + 320] = secondary_val
    u64[r1 + 328] = secondary_val
    let cond_5 = secondary_val <u 0
    u64[r1 + 336] = cond_5

    if (secondary_val <u 0) {
        goto block_f4c4;
    } else {
        // @f40b
    }
    
    // @f410
    u32[u64[r1 + 40] + 0x60000] = 3
    let next_ptr = 1 + u64[r1 + 312]
    u64[r1 + 352] = next_ptr
    let next_val = u8[next_ptr + 0x60000]
    u64[r1 + 360] = next_val
    u64[r1 + 368] = next_val
    let cond_6 = next_val <u 0
    u64[r1 + 376] = cond_6
    u64[r1 + 384] = 1

    if (next_val >=u 0) {
        // @f46b
        let target_ptr = 12 + u64[r1 + 40]
        u64[r1 + 392] = target_ptr
        u32[target_ptr + 0x60000] = u64[r1 + 384]
        let struct_ptr = 4 + u64[r1 + 312]
        u64[r1 + 408] = struct_ptr
        let struct_val = u32[struct_ptr + 0x60000]
        u64[r1 + 416] = struct_val
        let dest_ptr = 16 + u64[r1 + 40]
        u64[r1 + 424] = dest_ptr
        u32[dest_ptr + 0x60000] = struct_val
        goto block_f4c4;
    } else {
        // @f463
        u64[r1 + 384] = 2
    }

    // Fallthrough: update secondary memory mapping
    u64[r1 + 384] = 2
    var_91 = 12 + u64[r1 + 40]
    u64[r1 + 392] = var_91
    u32[var_91 + 0x60000] = u64[r1 + 384]
    var_95 = 4 + u64[r1 + 312]
    u64[r1 + 408] = var_95
    var_96 = u32[var_95 + 0x60000]
    u64[r1 + 416] = var_96
    var_99 = 16 + u64[r1 + 40]
    u64[r1 + 424] = var_99
    u32[var_99 + 0x60000] = var_96

    block_f4c4:
    // @f2cd
    r7 = u64[r1 + 176]
    return
}
fn func_66(r1: u64, r7: u64) {
    // @f515
    u64[r1 + 64] = r7
    let shift_8_r7 = 8 >>u r7                      // Logical shift right by 8
    u64[r1 + 72] = shift_8_r7
    let mask_byte_pattern_1 = 0xFF00FF00FF00FF & shift_8_r7
    u64[r1 + 80] = mask_byte_pattern_1
    let mask_byte_pattern_2 = 0xFF00FF00FF00FF & r7
    u64[r1 + 88] = mask_byte_pattern_2
    let shift_8_left_pattern = 8 << mask_byte_pattern_2
    u64[r1 + 96] = shift_8_left_pattern
    let combined_pattern_1 = shift_8_left_pattern | mask_byte_pattern_1
    u64[r1 + 104] = combined_pattern_1
    let shift_16_combined_1 = 16 >>u combined_pattern_1  // Logical shift right by 16
    u64[r1 + 112] = shift_16_combined_1
    let mask_word_pattern_1 = 0xFFFF0000FFFF & shift_16_combined_1
    u64[r1 + 120] = mask_word_pattern_1
    let mask_word_pattern_2 = 0xFFFF0000FFFF & combined_pattern_1
    u64[r1 + 128] = mask_word_pattern_2
    let shift_16_left_pattern = 16 << mask_word_pattern_2
    u64[r1 + 136] = shift_16_left_pattern
    let combined_pattern_2 = shift_16_left_pattern | mask_word_pattern_1
    u64[r1 + 144] = combined_pattern_2
    // Swap bytes/words using bitwise masks and shifts (common in endianness conversion or bit permutation)
    u64[r1 + 152] = (63 & 32) >>u combined_pattern_2 | (63 & 32) - 64 << combined_pattern_2

    // @f5db
    r7 = u64[r1 + 160]
    return
}
fn func_67(r1: u64, r7: u64) {
    // @f660
    u64[r1 + 96] = r7
    let current_ptr = u32[24]
    u64[r1 + 104] = current_ptr
    let mem_val = u8[current_ptr + 0x60000] // Load byte from global heap/memory offset
    u64[r1 + 112] = mem_val
    u64[r1 + 120] = mem_val
    let is_zero = !(mem_val)
    u64[r1 + 128] = is_zero
    u64[r1 + 136] = is_zero
    u64[r1 + 144] = is_zero
    let cond_1 = is_zero <u 0
    u64[r1 + 152] = cond_1

    if (is_zero <u 0) {
        // @f7be
        goto block_f7c3;
    } else {
        // @f6be
    }

    // @f6db
    let shift_val = u32[u64[r1 + 48] + 0x60000]
    u64[r1 + 168] = shift_val
    u64[r1 + 176] = shift_val
    let mask_calc = 32 >>u (32 << shift_val) // Bitwise masking operation
    u64[r1 + 184] = mask_calc
    let is_valid_shift = shift_val <=u 13
    u64[r1 + 192] = is_valid_shift
    u64[r1 + 200] = is_valid_shift
    u64[r1 + 208] = is_valid_shift
    let cond_4 = is_valid_shift <u 0
    u64[r1 + 216] = cond_4
    u64[r1 + 224] = mask_calc

    if (is_valid_shift >=u 0) {
        // @f751
        let shift_amt = u64[r1 + 224]
        u64[r1 + 232] = shift_amt
        let val_scaled = 3 << shift_amt
        u64[r1 + 240] = val_scaled
        let final_mask = 32 >>u (32 << val_scaled)
        u64[r1 + 248] = final_mask
        let base_offset = u64[r1 + 56]
        u64[r1 + 256] = base_offset
        u64[r1 + 264] = final_mask
        let combined_idx = final_mask + base_offset
        u64[r1 + 272] = combined_idx
        let final_idx = 32 >>u (32 << combined_idx)
        u64[r1 + 280] = final_idx
        u64[final_idx + 0x60000] = u64[r1 + 96]
        goto block_f7c3;
    } else {
        // @f749
        u64[r1 + 224] = 12
    }

    // @f749 - Fallback path for invalid shifts
    u64[r1 + 224] = 12

    // @f751 - Execution path for recalculated values
    var_41 = u64[r1 + 224]
    u64[r1 + 232] = var_41
    var_45 = 3 << var_41
    u64[r1 + 240] = var_45
    var_49 = 32 >>u (32 << var_45)
    u64[r1 + 248] = var_49
    var_50 = u64[r1 + 56]
    u64[r1 + 256] = var_50
    u64[r1 + 264] = var_49
    var_56 = var_49 + var_50
    u64[r1 + 272] = var_56
    var_60 = 32 >>u (32 << var_56)
    u64[r1 + 280] = var_60
    u64[var_60 + 0x60000] = u64[r1 + 96]

    block_f7c3:
    // @f7c3
    let next_ptr = u32[24]
    u64[r1 + 296] = next_ptr
    let status_byte = u8[next_ptr + 0x60000]
    u64[r1 + 304] = status_byte
    u64[r1 + 312] = status_byte
    let cond_5 = status_byte <u 0
    u64[r1 + 320] = cond_5

    if (status_byte <u 0) {
        // @f8b5
        goto block_f8ba;
    } else {
        // @f801
    }
    // @f806
    u32[u64[r1 + 40] + 0x60000] = 3
    let next_idx = 1 + u64[r1 + 296]
    u64[r1 + 336] = next_idx
    let next_status = u8[next_idx + 0x60000]
    u64[r1 + 344] = next_status
    u64[r1 + 352] = next_status
    let cond_6 = next_status <u 0
    u64[r1 + 360] = cond_6
    u64[r1 + 368] = 1

    if (next_status >=u 0) {
        // @f861
        let offset_a = 12 + u64[r1 + 40]
        u64[r1 + 376] = offset_a
        u32[offset_a + 0x60000] = u64[r1 + 368]
        let offset_b = 4 + u64[r1 + 296]
        u64[r1 + 392] = offset_b
        let data_val = u32[offset_b + 0x60000]
        u64[r1 + 400] = data_val
        let offset_c = 16 + u64[r1 + 40]
        u64[r1 + 408] = offset_c
        u32[offset_c + 0x60000] = data_val
        goto block_f8ba;
    } else {
        // @f859
        u64[r1 + 368] = 2
    }

    // @f859
    u64[r1 + 368] = 2

    // @f861
    var_87 = 12 + u64[r1 + 40]
    u64[r1 + 376] = var_87
    u32[var_87 + 0x60000] = u64[r1 + 368]
    var_91 = 4 + u64[r1 + 296]
    u64[r1 + 392] = var_91
    var_92 = u32[var_91 + 0x60000]
    u64[r1 + 400] = var_92
    var_95 = 16 + u64[r1 + 40]
    u64[r1 + 408] = var_95
    u32[var_95 + 0x60000] = var_92

    // @f8b5

    block_f8ba:
    // @f8ba

    // @f6c3
    r7 = u64[r1 + 160]
    return
}
fn func_68(r1: u64, r7: u64) {
    // @fa31
    u64[r1 + 248] = r7
    u64[r1 + 256] = r7
    // Normalize boolean flag using shift pattern: 32 >> (32 << x) results in 1 if x != 0, else 0
    let is_nonzero_r7 = 32 >>u (32 << r7)
    u64[r1 + 264] = is_nonzero_r7
    u64[r1 + 272] = is_nonzero_r7
    let mask_8bit = 255 & is_nonzero_r7
    u64[r1 + 280] = mask_8bit
    let is_nonzero_mask = 32 >>u (32 << mask_8bit)
    u64[r1 + 288] = is_nonzero_mask
    u64[r1 + 296] = is_nonzero_mask
    u64[r1 + 304] = 32 >>u (32 << is_nonzero_mask)
    let global_offset_ptr = u32[24]
    u64[r1 + 312] = global_offset_ptr
    let state_byte = u8[global_offset_ptr + 0x60000] // Load byte from global state
    u64[r1 + 320] = state_byte
    u64[r1 + 328] = state_byte
    let cond_0 = !(state_byte) // Invert state byte
    u64[r1 + 336] = cond_0
    u64[r1 + 344] = cond_0
    u64[r1 + 352] = cond_0
    let cond_1 = cond_0 <u 0
    u64[r1 + 360] = cond_1

    if (cond_0 <u 0) {
        // @fc05
        goto block_fc0a;
    } else {
        // @faf6
    }

    // @fb13
    let data_ptr = 4 + u64[r1 + 48]
    u64[r1 + 376] = data_ptr
    let data_val = u32[data_ptr + 0x60000]
    u64[r1 + 384] = data_val
    u64[r1 + 392] = data_val
    let is_nonzero_data = 32 >>u (32 << data_val)
    u64[r1 + 400] = is_nonzero_data
    let cond_3 = data_val <=u 13
    u64[r1 + 408] = cond_3
    u64[r1 + 416] = cond_3
    u64[r1 + 424] = cond_3
    let cond_4 = cond_3 <u 0
    u64[r1 + 432] = cond_4
    u64[r1 + 440] = is_nonzero_data

    if (cond_3 >=u 0) {
        // @fb97
        let shift_val = u64[r1 + 440]
        u64[r1 + 448] = shift_val
        let bit_mask = 3 << shift_val
        u64[r1 + 456] = bit_mask
        let norm_mask = 32 >>u (32 << bit_mask)
        u64[r1 + 464] = norm_mask
        let base_addr = u64[r1 + 56]
        u64[r1 + 472] = base_addr
        u64[r1 + 480] = norm_mask
        let target_idx = norm_mask + base_addr
        u64[r1 + 488] = target_idx
        let norm_idx = 32 >>u (32 << target_idx)
        u64[r1 + 496] = norm_idx
        u64[norm_idx + 0x60000] = u64[r1 + 304] // Write normalized value to global state
        goto block_fc0a;
    } else {
        // @fb8f
        u64[r1 + 440] = 12
    }

    // @fb8f (fallthrough)
    u64[r1 + 440] = 12

    // @fb97 (fallthrough)
    let var_65 = u64[r1 + 440]
    u64[r1 + 448] = var_65
    let var_69 = 3 << var_65
    u64[r1 + 456] = var_69
    let var_73 = 32 >>u (32 << var_69)
    u64[r1 + 464] = var_73
    let var_74 = u64[r1 + 56]
    u64[r1 + 472] = var_74
    u64[r1 + 480] = var_73
    let var_80 = var_73 + var_74
    u64[r1 + 488] = var_80
    let var_84 = 32 >>u (32 << var_80)
    u64[r1 + 496] = var_84
    u64[var_84 + 0x60000] = u64[r1 + 304]

    block_fc0a:
    // @fc0a
    let current_ptr = u32[24]
    u64[r1 + 512] = current_ptr
    let status_byte = u8[current_ptr + 0x60000]
    u64[r1 + 520] = status_byte
    u64[r1 + 528] = status_byte
    let cond_5 = status_byte <u 0
    u64[r1 + 536] = cond_5

    if (status_byte <u 0) {
        // @fcfc
        goto block_fd01;
    } else {
        // @fc48
    }
    // @fc4d
    u32[u64[r1 + 40] + 0x60000] = 3
    let next_ptr = 1 + u64[r1 + 512]
    u64[r1 + 552] = next_ptr
    let next_byte = u8[next_ptr + 0x60000]
    u64[r1 + 560] = next_byte
    u64[r1 + 568] = next_byte
    let cond_6 = next_byte <u 0
    u64[r1 + 576] = cond_6
    u64[r1 + 584] = 1

    if (next_byte >=u 0) {
        // @fca8
        let addr_out = 12 + u64[r1 + 40]
        u64[r1 + 592] = addr_out
        u32[addr_out + 0x60000] = u64[r1 + 584]
        let struct_ptr = 4 + u64[r1 + 512]
        u64[r1 + 608] = struct_ptr
        let struct_val = u32[struct_ptr + 0x60000]
        u64[r1 + 616] = struct_val
        let target_addr = 16 + u64[r1 + 40]
        u64[r1 + 624] = target_addr
        u32[target_addr + 0x60000] = struct_val
        goto block_fd01;
    } else {
        // @fca0
        u64[r1 + 584] = 2
    }

    // @fca0 (fallthrough)
    u64[r1 + 584] = 2

    // @fca8 (fallthrough)
    let var_111 = 12 + u64[r1 + 40]
    u64[r1 + 592] = var_111
    u32[var_111 + 0x60000] = u64[r1 + 584]
    let var_115 = 4 + u64[r1 + 512]
    u64[r1 + 608] = var_115
    let var_116 = u32[var_115 + 0x60000]
    u64[r1 + 616] = var_116
    let var_119 = 16 + u64[r1 + 40]
    u64[r1 + 624] = var_119
    u32[var_119 + 0x60000] = var_116

    // @fcfc

    block_fd01:
    // @fd01
    // @fafb
    r7 = u64[r1 + 368]
    return
}
fn func_69(r1: u64, r7: u64) {
    // @fe78
    u64[r1 + 248] = r7
    u64[r1 + 256] = r7
    let normalized_val_1 = 32 >>u (32 << r7) // Normalize value to 0 or 1
    u64[r1 + 264] = normalized_val_1
    u64[r1 + 272] = normalized_val_1
    let mask_val = 255 & normalized_val_1
    u64[r1 + 280] = mask_val
    let normalized_val_2 = 32 >>u (32 << mask_val)
    u64[r1 + 288] = normalized_val_2
    u64[r1 + 296] = normalized_val_2
    u64[r1 + 304] = normalized_val_2
    let s_val = sext8(normalized_val_2) // Sign-extend 8-bit to 64-bit
    u64[r1 + 312] = s_val
    let normalized_val_3 = 32 >>u (32 << s_val)
    u64[r1 + 320] = normalized_val_3
    u64[r1 + 328] = normalized_val_3
    u64[r1 + 336] = normalized_val_3
    let base_ptr = u32[24] // Load base address from global constant 24
    u64[r1 + 344] = base_ptr
    let state_byte = u8[base_ptr + 0x60000] // Access state buffer at offset
    u64[r1 + 352] = state_byte
    u64[r1 + 360] = state_byte
    let cond_is_zero = !(state_byte)
    u64[r1 + 368] = cond_is_zero
    u64[r1 + 376] = cond_is_zero
    u64[r1 + 384] = cond_is_zero
    let cond_0 = cond_is_zero <u 0
    u64[r1 + 392] = cond_0

    if (cond_0 <u 0) {
        // @10074
        goto block_10079;
    } else {
        // @ff65
    }

    // @ff82
    let next_member_ptr = 4 + u64[r1 + 48]
    u64[r1 + 408] = next_member_ptr
    let member_val = u32[next_member_ptr + 0x60000]
    u64[r1 + 416] = member_val
    u64[r1 + 424] = member_val
    let normalized_member = 32 >>u (32 << member_val)
    u64[r1 + 432] = normalized_member
    let is_valid_range = member_val <=u 13
    u64[r1 + 440] = is_valid_range
    u64[r1 + 448] = is_valid_range
    u64[r1 + 456] = is_valid_range
    let cond_4 = is_valid_range <u 0
    u64[r1 + 464] = cond_4
    u64[r1 + 472] = normalized_member

    if (is_valid_range >=u 0) {
        // @10006
        let shift_idx = u64[r1 + 472]
        u64[r1 + 480] = shift_idx
        let shift_result = 3 << shift_idx
        u64[r1 + 488] = shift_result
        let norm_shift = 32 >>u (32 << shift_result)
        u64[r1 + 496] = norm_shift
        let offset_val = u64[r1 + 56]
        u64[r1 + 504] = offset_val
        u64[r1 + 512] = norm_shift
        let combined_idx = norm_shift + offset_val
        u64[r1 + 520] = combined_idx
        let target_slot = 32 >>u (32 << combined_idx)
        u64[r1 + 528] = target_slot
        u64[target_slot + 0x60000] = u64[r1 + 336]
        goto block_10079;
    } else {
        // @fffe
        u64[r1 + 472] = 12
    }

    // @fffe
    u64[r1 + 472] = 12

    // @10006 (Fallthrough logic)
    var_73 = u64[r1 + 472]
    u64[r1 + 480] = var_73
    var_77 = 3 << var_73
    u64[r1 + 488] = var_77
    var_81 = 32 >>u (32 << var_77)
    u64[r1 + 496] = var_81
    var_82 = u64[r1 + 56]
    u64[r1 + 504] = var_82
    u64[r1 + 512] = var_81
    var_88 = var_81 + var_82
    u64[r1 + 520] = var_88
    var_92 = 32 >>u (32 << var_88)
    u64[r1 + 528] = var_92
    u64[var_92 + 0x60000] = u64[r1 + 336]

    block_10079:
    // @10079
    let current_base = u32[24]
    u64[r1 + 544] = current_base
    let byte_check = u8[current_base + 0x60000]
    u64[r1 + 552] = byte_check
    u64[r1 + 560] = byte_check
    let cond_5 = byte_check <u 0
    u64[r1 + 568] = cond_5

    if (byte_check <u 0) {
        // @1016b
        goto block_10170;
    } else {
        // @100b7
    }
    // @100bc
    u32[u64[r1 + 40] + 0x60000] = 3
    let next_idx = 1 + u64[r1 + 544]
    u64[r1 + 584] = next_idx
    let next_byte = u8[next_idx + 0x60000]
    u64[r1 + 592] = next_byte
    u64[r1 + 600] = next_byte
    let cond_6 = next_byte <u 0
    u64[r1 + 608] = cond_6
    u64[r1 + 616] = 1

    if (next_byte >=u 0) {
        // @10117
        let target_ptr_1 = 12 + u64[r1 + 40]
        u64[r1 + 624] = target_ptr_1
        u32[target_ptr_1 + 0x60000] = u64[r1 + 616]
        let offset_ptr = 4 + u64[r1 + 544]
        u64[r1 + 640] = offset_ptr
        let loaded_val = u32[offset_ptr + 0x60000]
        u64[r1 + 648] = loaded_val
        let target_ptr_2 = 16 + u64[r1 + 40]
        u64[r1 + 656] = target_ptr_2
        u32[target_ptr_2 + 0x60000] = loaded_val
        goto block_10170;
    } else {
        // @1010f
        u64[r1 + 616] = 2
    }

    // @1010f
    u64[r1 + 616] = 2

    // @10117 (Fallthrough logic)
    var_119 = 12 + u64[r1 + 40]
    u64[r1 + 624] = var_119
    u32[var_119 + 0x60000] = u64[r1 + 616]
    var_123 = 4 + u64[r1 + 544]
    u64[r1 + 640] = var_123
    var_124 = u32[var_123 + 0x60000]
    u64[r1 + 648] = var_124
    var_127 = 16 + u64[r1 + 40]
    u64[r1 + 656] = var_127
    u32[var_127 + 0x60000] = var_124

    // @1016b

    block_10170:
    // @10170

    // @ff6a
    r7 = u64[r1 + 400]
    return
}
fn func_70(r1: u64, r7: u64) {
    // @102e4: Initialize state with r7
    u64[r1 + 248] = r7
    let current_base_addr = u32[24]
    u64[r1 + 256] = current_base_addr
    let flag_byte = u8[current_base_addr + 0x60000] // Load status flag from global memory
    u64[r1 + 264] = flag_byte
    u64[r1 + 272] = flag_byte
    let flag_is_zero = !(flag_byte)
    u64[r1 + 280] = flag_is_zero
    u64[r1 + 288] = flag_is_zero
    u64[r1 + 296] = flag_is_zero
    let cond_1 = flag_is_zero <u 0
    u64[r1 + 304] = cond_1

    if (flag_is_zero <u 0) {
        // @10458
        goto block_1045d;
    } else {
        // @10349
    }

    // @10366: Calculate offset and extract power-of-two related bitmask
    let data_ptr = 4 + u64[r1 + 48]
    u64[r1 + 320] = data_ptr
    let shift_amount = u32[data_ptr + 0x60000]
    u64[r1 + 328] = shift_amount
    u64[r1 + 336] = shift_amount
    let mask_val = 32 >>u (32 << shift_amount) // Bitmask isolation logic
    u64[r1 + 344] = mask_val
    let is_valid_shift = shift_amount <=u 13
    u64[r1 + 352] = is_valid_shift
    u64[r1 + 360] = is_valid_shift
    u64[r1 + 368] = is_valid_shift
    let cond_4 = is_valid_shift <u 0
    u64[r1 + 376] = cond_4
    u64[r1 + 384] = mask_val

    if (is_valid_shift >=u 0) {
        // @103ea
        let current_mask = u64[r1 + 384]
        u64[r1 + 392] = current_mask
        let bit_shift = 3 << current_mask
        u64[r1 + 400] = bit_shift
        let final_mask = 32 >>u (32 << bit_shift)
        u64[r1 + 408] = final_mask
        let offset_val = u64[r1 + 56]
        u64[r1 + 416] = offset_val
        u64[r1 + 424] = final_mask
        let calculated_addr = final_mask + offset_val
        u64[r1 + 432] = calculated_addr
        let result_addr = 32 >>u (32 << calculated_addr) // Final indexing mask
        u64[r1 + 440] = result_addr
        u64[result_addr + 0x60000] = u64[r1 + 248] // Store result to global memory
        goto block_1045d;
    } else {
        // @103e2: Fallback path
        u64[r1 + 384] = 12
    }

    // @103e2: Re-executing logic after fallback
    u64[r1 + 384] = 12

    // @103ea: Redundant block for control flow path merging
    var_44 = u64[r1 + 384]
    u64[r1 + 392] = var_44
    var_48 = 3 << var_44
    u64[r1 + 400] = var_48
    var_52 = 32 >>u (32 << var_48)
    u64[r1 + 408] = var_52
    var_53 = u64[r1 + 56]
    u64[r1 + 416] = var_53
    u64[r1 + 424] = var_52
    var_59 = var_52 + var_53
    u64[r1 + 432] = var_59
    var_63 = 32 >>u (32 << var_59)
    u64[r1 + 440] = var_63
    u64[var_63 + 0x60000] = u64[r1 + 248]

    block_1045d:
    // @1045d: Secondary check/update routine
    let current_base_addr_alt = u32[24]
    u64[r1 + 456] = current_base_addr_alt
    let flag_byte_alt = u8[current_base_addr_alt + 0x60000]
    u64[r1 + 464] = flag_byte_alt
    u64[r1 + 472] = flag_byte_alt
    let cond_5 = flag_byte_alt <u 0
    u64[r1 + 480] = cond_5

    if (flag_byte_alt <u 0) {
        // @1054f
        goto block_10554;
    } else {
        // @1049b
    }
    // @104a0: Write status code 3 to global memory
    u32[u64[r1 + 40] + 0x60000] = 3
    let next_data_ptr = 1 + u64[r1 + 456]
    u64[r1 + 496] = next_data_ptr
    let next_flag = u8[next_data_ptr + 0x60000]
    u64[r1 + 504] = next_flag
    u64[r1 + 512] = next_flag
    let cond_6 = next_flag <u 0
    u64[r1 + 520] = cond_6
    u64[r1 + 528] = 1

    if (next_flag >=u 0) {
        // @104fb
        let target_mem_ptr = 12 + u64[r1 + 40]
        u64[r1 + 536] = target_mem_ptr
        u32[target_mem_ptr + 0x60000] = u64[r1 + 528]
        let item_idx_ptr = 4 + u64[r1 + 456]
        u64[r1 + 552] = item_idx_ptr
        let item_val = u32[item_idx_ptr + 0x60000]
        u64[r1 + 560] = item_val
        let update_ptr = 16 + u64[r1 + 40]
        u64[r1 + 568] = update_ptr
        u32[update_ptr + 0x60000] = item_val
        goto block_10554;
    } else {
        // @104f3: Update default status code to 2
        u64[r1 + 528] = 2
    }

    // @104f3
    u64[r1 + 528] = 2

    // @104fb: Re-executing memory update
    var_90 = 12 + u64[r1 + 40]
    u64[r1 + 536] = var_90
    u32[var_90 + 0x60000] = u64[r1 + 528]
    var_94 = 4 + u64[r1 + 456]
    u64[r1 + 552] = var_94
    var_95 = u32[var_94 + 0x60000]
    u64[r1 + 560] = var_95
    var_98 = 16 + u64[r1 + 40]
    u64[r1 + 568] = var_98
    u32[var_98 + 0x60000] = var_95

    block_10554:
    // @10554: Clean up and exit
    r7 = u64[r1 + 312]
    return
}
fn func_71(r1: u64, r7: u64) {
    // @106c8
    u64[r1 + 248] = r7
    u64[r1 + 256] = r7
    let bitmask_val = 32 >>u (32 << r7) // Standard clearing of bits higher than r7
    u64[r1 + 264] = bitmask_val
    u64[r1 + 272] = bitmask_val
    u64[r1 + 280] = bitmask_val
    let offset_s16 = sext16(bitmask_val)
    u64[r1 + 288] = offset_s16
    let bitmask_val2 = 32 >>u (32 << offset_s16)
    u64[r1 + 296] = bitmask_val2
    u64[r1 + 304] = bitmask_val2
    u64[r1 + 312] = bitmask_val2
    let base_ptr = u32[24]
    u64[r1 + 320] = base_ptr
    let state_byte = u8[base_ptr + 0x60000] // Access state at memory 0x60000 offset
    u64[r1 + 328] = state_byte
    u64[r1 + 336] = state_byte
    let cond_is_zero = !(state_byte)
    u64[r1 + 344] = cond_is_zero
    u64[r1 + 352] = cond_is_zero
    u64[r1 + 360] = cond_is_zero
    let cond_0 = cond_is_zero <u 0
    u64[r1 + 368] = cond_0

    if (cond_0 <u 0) {
        // @1089a
        goto block_1089f;
    } else {
        // @1078b
    }

    // @107a8
    let next_ptr = 4 + u64[r1 + 48]
    u64[r1 + 384] = next_ptr
    let val_at_ptr = u32[next_ptr + 0x60000]
    u64[r1 + 392] = val_at_ptr
    u64[r1 + 400] = val_at_ptr
    let val_mask = 32 >>u (32 << val_at_ptr)
    u64[r1 + 408] = val_mask
    let is_le_13 = val_at_ptr <=u 13
    u64[r1 + 416] = is_le_13
    u64[r1 + 424] = is_le_13
    u64[r1 + 432] = is_le_13
    let cond_4 = is_le_13 <u 0
    u64[r1 + 440] = cond_4
    u64[r1 + 448] = val_mask

    if (is_le_13 >=u 0) {
        // @1082c
        let current_offset = u64[r1 + 448]
        u64[r1 + 456] = current_offset
        let shifted_val = 3 << current_offset
        u64[r1 + 464] = shifted_val
        let final_mask = 32 >>u (32 << shifted_val)
        u64[r1 + 472] = final_mask
        let base_ext = u64[r1 + 56]
        u64[r1 + 480] = base_ext
        u64[r1 + 488] = final_mask
        let sum_offset = final_mask + base_ext
        u64[r1 + 496] = sum_offset
        let final_target = 32 >>u (32 << sum_offset)
        u64[r1 + 504] = final_target
        u64[final_target + 0x60000] = u64[r1 + 312]
        goto block_1089f;
    } else {
        // @10824
        u64[r1 + 448] = 12
    }

    // @10824
    u64[r1 + 448] = 12

    // @1082c
    var_64 = u64[r1 + 448]
    u64[r1 + 456] = var_64
    var_68 = 3 << var_64
    u64[r1 + 464] = var_68
    var_72 = 32 >>u (32 << var_68)
    u64[r1 + 472] = var_72
    var_73 = u64[r1 + 56]
    u64[r1 + 480] = var_73
    u64[r1 + 488] = var_72
    var_79 = var_72 + var_73
    u64[r1 + 496] = var_79
    var_83 = 32 >>u (32 << var_79)
    u64[r1 + 504] = var_83
    u64[var_83 + 0x60000] = u64[r1 + 312]

    block_1089f:
    // @1089f
    let current_base = u32[24]
    u64[r1 + 520] = current_base
    let byte_at_base = u8[current_base + 0x60000]
    u64[r1 + 528] = byte_at_base
    u64[r1 + 536] = byte_at_base
    let cond_5 = byte_at_base <u 0
    u64[r1 + 544] = cond_5

    if (byte_at_base <u 0) {
        // @10991
        goto block_10996;
    } else {
        // @108dd
    }
    // @108e2
    u32[u64[r1 + 40] + 0x60000] = 3
    let next_idx = 1 + u64[r1 + 520]
    u64[r1 + 560] = next_idx
    let next_byte = u8[next_idx + 0x60000]
    u64[r1 + 568] = next_byte
    u64[r1 + 576] = next_byte
    let cond_6 = next_byte <u 0
    u64[r1 + 584] = cond_6
    u64[r1 + 592] = 1

    if (next_byte >=u 0) {
        // @1093d
        let addr_1 = 12 + u64[r1 + 40]
        u64[r1 + 600] = addr_1
        u32[addr_1 + 0x60000] = u64[r1 + 592]
        let offset_idx = 4 + u64[r1 + 520]
        u64[r1 + 616] = offset_idx
        let val_from_table = u32[offset_idx + 0x60000]
        u64[r1 + 624] = val_from_table
        let addr_2 = 16 + u64[r1 + 40]
        u64[r1 + 632] = addr_2
        u32[addr_2 + 0x60000] = val_from_table
        goto block_10996;
    } else {
        // @10935
        u64[r1 + 592] = 2
    }

    // @10935
    u64[r1 + 592] = 2

    // @1093d
    var_110 = 12 + u64[r1 + 40]
    u64[r1 + 600] = var_110
    u32[var_110 + 0x60000] = u64[r1 + 592]
    var_114 = 4 + u64[r1 + 520]
    u64[r1 + 616] = var_114
    var_115 = u32[var_114 + 0x60000]
    u64[r1 + 624] = var_115
    var_118 = 16 + u64[r1 + 40]
    u64[r1 + 632] = var_118
    u32[var_118 + 0x60000] = var_115

    // @10991

    block_10996:
    // @10996

    // @10790
    r7 = u64[r1 + 376]
    return
}
fn func_72(r1: u64, r7: u64) {
    // @10b0a
    u64[r1 + 248] = r7
    let current_base_ptr = u32[24] // Global pointer from offset 24
    u64[r1 + 256] = current_base_ptr
    let status_byte = u8[current_base_ptr + 0x60000] // Read status from memory mapped I/O
    u64[r1 + 264] = status_byte
    u64[r1 + 272] = status_byte
    let is_zero = !(status_byte)
    u64[r1 + 280] = is_zero
    u64[r1 + 288] = is_zero
    u64[r1 + 296] = is_zero
    let cond_1 = is_zero <u 0
    u64[r1 + 304] = cond_1

    if (is_zero <u 0) {
        // @10c7e
        goto block_10c83;
    } else {
        // @10b6f
    }

    // @10b8c
    let config_offset = 4 + u64[r1 + 48]
    u64[r1 + 320] = config_offset
    let config_val = u32[config_offset + 0x60000]
    u64[r1 + 328] = config_val
    u64[r1 + 336] = config_val
    // Masking operation: clear bits outside the range defined by config_val
    let masked_val = 32 >>u (32 << config_val)
    u64[r1 + 344] = masked_val
    let is_valid_range = config_val <=u 13
    u64[r1 + 352] = is_valid_range
    u64[r1 + 360] = is_valid_range
    u64[r1 + 368] = is_valid_range
    let cond_4 = is_valid_range <u 0
    u64[r1 + 376] = cond_4
    u64[r1 + 384] = masked_val

    if (is_valid_range >=u 0) {
        // @10c10
        let shift_val = u64[r1 + 384]
        u64[r1 + 392] = shift_val
        let bit_mask = 3 << shift_val
        u64[r1 + 400] = bit_mask
        let masked_shift = 32 >>u (32 << bit_mask)
        u64[r1 + 408] = masked_shift
        let base_addr = u64[r1 + 56]
        u64[r1 + 416] = base_addr
        u64[r1 + 424] = masked_shift
        let final_addr = masked_shift + base_addr
        u64[r1 + 432] = final_addr
        let target_reg = 32 >>u (32 << final_addr)
        u64[r1 + 440] = target_reg
        u64[target_reg + 0x60000] = u64[r1 + 248]
        goto block_10c83;
    } else {
        // @10c08
        u64[r1 + 384] = 12
    }

    // @10c08 - Fallback path for invalid range
    u64[r1 + 384] = 12

    // @10c10 - Shared logic for memory write
    var_44 = u64[r1 + 384]
    u64[r1 + 392] = var_44
    var_48 = 3 << var_44
    u64[r1 + 400] = var_48
    var_52 = 32 >>u (32 << var_48)
    u64[r1 + 408] = var_52
    var_53 = u64[r1 + 56]
    u64[r1 + 416] = var_53
    u64[r1 + 424] = var_52
    var_59 = var_52 + var_53
    u64[r1 + 432] = var_59
    var_63 = 32 >>u (32 << var_59)
    u64[r1 + 440] = var_63
    u64[var_63 + 0x60000] = u64[r1 + 248]

    block_10c83:
    // @10c83
    let next_ptr = u32[24]
    u64[r1 + 456] = next_ptr
    let next_status = u8[next_ptr + 0x60000]
    u64[r1 + 464] = next_status
    u64[r1 + 472] = next_status
    let cond_5 = next_status <u 0
    u64[r1 + 480] = cond_5

    if (next_status <u 0) {
        // @10d75
        goto block_10d7a;
    } else {
        // @10cc1
    }
    // @10cc6
    u32[u64[r1 + 40] + 0x60000] = 3
    let offset_ptr = 1 + u64[r1 + 456]
    u64[r1 + 496] = offset_ptr
    let offset_val = u8[offset_ptr + 0x60000]
    u64[r1 + 504] = offset_val
    u64[r1 + 512] = offset_val
    let cond_6 = offset_val <u 0
    u64[r1 + 520] = cond_6
    u64[r1 + 528] = 1

    if (offset_val >=u 0) {
        // @10d21
        let base_struct_ptr = 12 + u64[r1 + 40]
        u64[r1 + 536] = base_struct_ptr
        u32[base_struct_ptr + 0x60000] = u64[r1 + 528]
        let index_ptr = 4 + u64[r1 + 456]
        u64[r1 + 552] = index_ptr
        let struct_val = u32[index_ptr + 0x60000]
        u64[r1 + 560] = struct_val
        let target_ptr = 16 + u64[r1 + 40]
        u64[r1 + 568] = target_ptr
        u32[target_ptr + 0x60000] = struct_val
        goto block_10d7a;
    } else {
        // @10d19
        u64[r1 + 528] = 2
    }

    // @10d19
    u64[r1 + 528] = 2

    // @10d21 - Shared struct update
    var_90 = 12 + u64[r1 + 40]
    u64[r1 + 536] = var_90
    u32[var_90 + 0x60000] = u64[r1 + 528]
    var_94 = 4 + u64[r1 + 456]
    u64[r1 + 552] = var_94
    var_95 = u32[var_94 + 0x60000]
    u64[r1 + 560] = var_95
    var_98 = 16 + u64[r1 + 40]
    u64[r1 + 568] = var_98
    u32[var_98 + 0x60000] = var_95

    block_10d7a:
    // @10d7a
    r7 = u64[r1 + 312]
    return
}
fn func_73(r1: u64, r7: u64) {
    // @10eee
    // Initialize memory block at r1 offsets with r7 value
    u64[r1 + 248] = r7
    u64[r1 + 256] = r7
    u64[r1 + 264] = r7
    
    let current_ptr = u32[24]
    u64[r1 + 272] = current_ptr
    
    // Read state byte from memory
    let state_byte = u8[current_ptr + 0x60000]
    u64[r1 + 280] = state_byte
    u64[r1 + 288] = state_byte
    
    let is_zero = !(state_byte)
    u64[r1 + 296] = is_zero
    u64[r1 + 304] = is_zero
    u64[r1 + 312] = is_zero
    
    let is_less_than_zero = is_zero <u 0
    u64[r1 + 320] = is_less_than_zero

    if (is_less_than_zero) {
        // @11076
        goto block_1107b;
    } else {
        // @10f67
    }

    // @10f84
    // Calculate offset and resolve status value
    let status_ptr = 4 + u64[r1 + 48]
    u64[r1 + 336] = status_ptr
    let status_val = u32[status_ptr + 0x60000]
    u64[r1 + 344] = status_val
    u64[r1 + 352] = status_val
    
    // Normalize status_val to 32-bit width using bitwise shifts
    let normalized_status = 32 >>u (32 << status_val)
    u64[r1 + 360] = normalized_status
    let is_status_valid = status_val <=u 13
    u64[r1 + 368] = is_status_valid
    u64[r1 + 376] = is_status_valid
    u64[r1 + 384] = is_status_valid
    let cond_4 = is_status_valid <u 0
    u64[r1 + 392] = cond_4
    u64[r1 + 400] = normalized_status

    if (is_status_valid >=u 0) {
        // @11008
        let val = u64[r1 + 400]
        u64[r1 + 408] = val
        let shift_val = 3 << val
        u64[r1 + 416] = shift_val
        let mask = 32 >>u (32 << shift_val)
        u64[r1 + 424] = mask
        let base_val = u64[r1 + 56]
        u64[r1 + 432] = base_val
        u64[r1 + 440] = mask
        let final_idx = mask + base_val
        u64[r1 + 448] = final_idx
        let addr_mask = 32 >>u (32 << final_idx)
        u64[r1 + 456] = addr_mask
        u64[addr_mask + 0x60000] = u64[r1 + 264]
        goto block_1107b;
    } else {
        // @11000
        u64[r1 + 400] = 12
    }

    // @11000 Fallthrough (forced assignment 12)
    u64[r1 + 400] = 12

    // @11008 duplicate logic for fallthrough path
    let val = u64[r1 + 400]
    u64[r1 + 408] = val
    let shift_val = 3 << val
    u64[r1 + 416] = shift_val
    let mask = 32 >>u (32 << shift_val)
    u64[r1 + 424] = mask
    let base_val = u64[r1 + 56]
    u64[r1 + 432] = base_val
    u64[r1 + 440] = mask
    let final_idx = mask + base_val
    u64[r1 + 448] = final_idx
    let addr_mask = 32 >>u (32 << final_idx)
    u64[r1 + 456] = addr_mask
    u64[addr_mask + 0x60000] = u64[r1 + 264]

    block_1107b:
    // @1107b
    let next_ptr = u32[24]
    u64[r1 + 472] = next_ptr
    let byte_val = u8[next_ptr + 0x60000]
    u64[r1 + 480] = byte_val
    u64[r1 + 488] = byte_val
    let cond_5 = byte_val <u 0
    u64[r1 + 496] = cond_5

    if (byte_val <u 0) {
        goto block_11172;
    } else {
        // @110b9
    }
    
    // @110be
    u32[u64[r1 + 40] + 0x60000] = 3
    let next_idx = 1 + u64[r1 + 472]
    u64[r1 + 512] = next_idx
    let next_byte = u8[next_idx + 0x60000]
    u64[r1 + 520] = next_byte
    u64[r1 + 528] = next_byte
    let cond_6 = next_byte <u 0
    u64[r1 + 536] = cond_6
    u64[r1 + 544] = 1

    if (next_byte >=u 0) {
        // @11119
        let offset_12 = 12 + u64[r1 + 40]
        u64[r1 + 552] = offset_12
        u32[offset_12 + 0x60000] = u64[r1 + 544]
        let offset_ptr = 4 + u64[r1 + 472]
        u64[r1 + 568] = offset_ptr
        let val_at_ptr = u32[offset_ptr + 0x60000]
        u64[r1 + 576] = val_at_ptr
        let offset_16 = 16 + u64[r1 + 40]
        u64[r1 + 584] = offset_16
        u32[offset_16 + 0x60000] = val_at_ptr
        goto block_11172;
    } else {
        // @11111
        u64[r1 + 544] = 2
    }

    // @11111 Fallthrough
    u64[r1 + 544] = 2

    // @11119 Repeated logic for structure assignments
    let offset_12 = 12 + u64[r1 + 40]
    u64[r1 + 552] = offset_12
    u32[offset_12 + 0x60000] = u64[r1 + 544]
    let offset_ptr = 4 + u64[r1 + 472]
    u64[r1 + 568] = offset_ptr
    let val_at_ptr = u32[offset_ptr + 0x60000]
    u64[r1 + 576] = val_at_ptr
    let offset_16 = 16 + u64[r1 + 40]
    u64[r1 + 584] = offset_16
    u32[offset_16 + 0x60000] = val_at_ptr

    block_11172:
    r7 = u64[r1 + 328]
    return
}
fn func_74(r1: u64, r7: u64) {
    // @112e6
    u64[r1 + 248] = r7 // Stores input r7 into object state
    let state_ptr = u32[24] // Global pointer to current state
    u64[r1 + 256] = state_ptr
    let state_val = u8[state_ptr + 0x60000] // Access state at global offset
    u64[r1 + 264] = state_val
    u64[r1 + 272] = state_val
    let is_zero = !(state_val) // Check if state byte is 0
    u64[r1 + 280] = is_zero
    u64[r1 + 288] = is_zero
    u64[r1 + 296] = is_zero
    let cond_1 = is_zero <u 0 // Always false for u8, check consistency
    u64[r1 + 304] = cond_1

    if (is_zero <u 0) {
        // @1145a
        goto block_1145f;
    } else {
        // @1134b
    }

    // @11368
    let offset_ptr = 4 + u64[r1 + 48]
    u64[r1 + 320] = offset_ptr
    let offset_val = u32[offset_ptr + 0x60000]
    u64[r1 + 328] = offset_val
    u64[r1 + 336] = offset_val
    let mask_val = 32 >>u (32 << offset_val) // Bitmask generation
    u64[r1 + 344] = mask_val
    let is_valid_offset = offset_val <=u 13
    u64[r1 + 352] = is_valid_offset
    u64[r1 + 360] = is_valid_offset
    u64[r1 + 368] = is_valid_offset
    let cond_4 = is_valid_offset <u 0
    u64[r1 + 376] = cond_4
    u64[r1 + 384] = mask_val

    if (is_valid_offset >=u 0) {
        // @113ec
        let shift_val = u64[r1 + 384]
        u64[r1 + 392] = shift_val
        let shift_exp = 3 << shift_val
        u64[r1 + 400] = shift_exp
        let computed_addr = 32 >>u (32 << shift_exp)
        u64[r1 + 408] = computed_addr
        let base_addr = u64[r1 + 56]
        u64[r1 + 416] = base_addr
        u64[r1 + 424] = computed_addr
        let final_addr = computed_addr + base_addr
        u64[r1 + 432] = final_addr
        let target_idx = 32 >>u (32 << final_addr)
        u64[r1 + 440] = target_idx
        u64[target_idx + 0x60000] = u64[r1 + 248] // Store result to global calculated address
        goto block_1145f;
    } else {
        // @113e4
        u64[r1 + 384] = 12
    }

    // @113e4
    u64[r1 + 384] = 12

    // @113ec logic fall-through
    var_44 = u64[r1 + 384]
    u64[r1 + 392] = var_44
    var_48 = 3 << var_44
    u64[r1 + 400] = var_48
    var_52 = 32 >>u (32 << var_48)
    u64[r1 + 408] = var_52
    var_53 = u64[r1 + 56]
    u64[r1 + 416] = var_53
    u64[r1 + 424] = var_52
    var_59 = var_52 + var_53
    u64[r1 + 432] = var_59
    var_63 = 32 >>u (32 << var_59)
    u64[r1 + 440] = var_63
    u64[var_63 + 0x60000] = u64[r1 + 248]

    block_1145f:
    // @1145f
    let current_state_base = u32[24]
    u64[r1 + 456] = current_state_base
    let state_byte = u8[current_state_base + 0x60000]
    u64[r1 + 464] = state_byte
    u64[r1 + 472] = state_byte
    let is_neg = state_byte <u 0
    u64[r1 + 480] = is_neg

    if (state_byte <u 0) {
        // @11551
        goto block_11556;
    } else {
        // @1149d
    }
    // @114a2
    u32[u64[r1 + 40] + 0x60000] = 3 // Set status code at control struct
    let next_state_idx = 1 + u64[r1 + 456]
    u64[r1 + 496] = next_state_idx
    let next_byte = u8[next_state_idx + 0x60000]
    u64[r1 + 504] = next_byte
    u64[r1 + 512] = next_byte
    let is_next_neg = next_byte <u 0
    u64[r1 + 520] = is_next_neg
    u64[r1 + 528] = 1

    if (next_byte >=u 0) {
        // @114fd
        let base_output_ptr = 12 + u64[r1 + 40]
        u64[r1 + 536] = base_output_ptr
        u32[base_output_ptr + 0x60000] = u64[r1 + 528]
        let struct_offset = 4 + u64[r1 + 456]
        u64[r1 + 552] = struct_offset
        let struct_val = u32[struct_offset + 0x60000]
        u64[r1 + 560] = struct_val
        let final_ptr = 16 + u64[r1 + 40]
        u64[r1 + 568] = final_ptr
        u32[final_ptr + 0x60000] = struct_val
        goto block_11556;
    } else {
        // @114f5
        u64[r1 + 528] = 2
    }

    // @114f5 logic fall-through
    u64[r1 + 528] = 2

    // @114fd logic fall-through
    var_90 = 12 + u64[r1 + 40]
    u64[r1 + 536] = var_90
    u32[var_90 + 0x60000] = u64[r1 + 528]
    var_94 = 4 + u64[r1 + 456]
    u64[r1 + 552] = var_94
    var_95 = u32[var_94 + 0x60000]
    u64[r1 + 560] = var_95
    var_98 = 16 + u64[r1 + 40]
    u64[r1 + 568] = var_98
    u32[var_98 + 0x60000] = var_95

    block_11556:
    // @11556
    r7 = u64[r1 + 312] // Return value loaded from specific memory index
    return
}
fn func_75(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val
    let target_mem_ptr
    let offset_val

    if (0xFEFD0000 >=u r1 - 392) {
        // @11570: Extract shift amount from memory at r10 offset
        bit_shift_val = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @11604: Calculate target memory and index offsets using bitwise manipulation
            target_mem_ptr = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val))) + ptr_0_56)
            offset_val = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @116d6: Perform masked bitwise AND between memory locations
                target_mem_ptr->field_393216 = u32[8 + ptr_0_48 + 0x60000] & u64[32 >>u (32 << (32 >>u (32 << (3 << offset_val))) + ptr_0_56) + 0x60000]
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0 // Clear word at memory offset
                goto block_1179e;
            } else {
                // @116ce: Set default offset if bounds check fails
                offset_val = 12
            }
        } else {
            // @115fc: Set default shift if bounds check fails
            bit_shift_val = 12
        }
    }

    block_1179e:
    // @1179e
    return
}
fn func_76(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val
    let target_ptr
    let offset_val

    // Check if input parameter r1 is within valid memory bounds relative to base offset
    if (0xFEFD0000 >=u r1 - 392) {
        // @117cb: Normalize shift factor from memory-mapped configuration
        bit_shift_val = 32 >>u (32 << u32[4 + r10 + 0x60000])

        // Verify bounds on the configuration value
        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @1185f: Calculate address for target pointer with internal offset logic
            target_ptr = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val))) + ptr_0_56)
            offset_val = 32 >>u (32 << ptr_0_48->field_393216)

            // Validate configuration field for subsequent XOR operation
            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @11931: Perform XOR substitution on target memory location
                target_ptr->field_393216 = u32[8 + ptr_0_48 + 0x60000] ^ u64[32 >>u (32 << (32 >>u (32 << (3 << offset_val))) + ptr_0_56) + 0x60000]
                // Clear state flags
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_119f9;
            } else {
                // @11929: Fallback default offset
                offset_val = 12
            }
        } else {
            // @11857: Fallback default bit shift
            bit_shift_val = 12
        }
    }

    block_119f9:
    // @119f9: Finalize function execution
    return
}
fn func_77(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val
    let target_mem_ptr
    let offset_index

    // Check bounds for r1/pointer access
    if (0xFEFD0000 >=u r1 - 392) {
        // @11a26: Calculate bit shift based on index stored at base address + offset
        bit_shift_val = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @11aba: Calculate target pointers using nested bit-shifting and base memory offsets
            target_mem_ptr = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val))) + ptr_0_56)
            offset_index = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @11b8c: Perform bitwise OR operation on memory at calculated addresses
                target_mem_ptr->field_393216 = u32[8 + ptr_0_48 + 0x60000] | u64[32 >>u (32 << (32 >>u (32 << (3 << offset_index))) + ptr_0_56) + 0x60000]
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0 // Clear status word
                goto block_11c54;
            } else {
                // @11b84: Default index on out-of-range
                offset_index = 12
            }
        } else {
            // @11ab2: Default shift on out-of-range index
            bit_shift_val = 12
        }
    }

    block_11c54:
    // @11c54
    return
}
fn func_78(r0: u64, input_val: u64, r9: u64, mem_base: u64, r11: u64, r12: u64) {
    let bit_shift_idx1
    let bit_shift_idx2
    let mask_val
    let target_base_offset
    let bit_shift_idx3

    // Validate memory bounds check for input_val
    if (0xFEFD0000 >=u input_val - 512) {
        // Calculate bit shift based on index stored at mem_base + 0x60008
        bit_shift_idx1 = 32 >>u (32 << u32[8 + mem_base + 0x60000])

        if (u32[8 + mem_base + 0x60000] <=u 13 >=u 0) {
            // Apply bit manipulation to determine memory slot
            bit_shift_idx2 = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_idx1))) + ptr_0_56)
            bit_shift_idx3 = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

            if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                // Fetch value from calculated memory location
                mask_val = u64[32 >>u (32 << (32 >>u (32 << (3 << bit_shift_idx3))) + ptr_0_56) + 0x60000]
                target_base_offset = 32 >>u (32 << ptr_0_48->field_393216)

                if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                    // Perform bitwise AND update in memory
                    bit_shift_idx2->field_393216 = u64[32 >>u (32 << (32 >>u (32 << (3 << target_base_offset))) + ptr_0_56) + 0x60000] & mask_val
                    ptr_0_40->field_393216 = 0
                    u32[8 + ptr_0_40 + 0x60000] = 0
                    goto block_11f80;
                } else {
                    target_base_offset = 12
                }
            } else {
                bit_shift_idx3 = 12
            }
        } else {
            bit_shift_idx1 = 12
        }
    }

    block_11f80:
    return
}
fn func_79(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val_a
    let mem_offset_base_b
    let mem_offset_base_c
    let retrieved_val_d
    let bit_shift_val_e

    // Check if r1 (likely an address/offset) is within a valid memory bounds region
    if (0xFEFD0000 >=u r1 - 512) {
        // @11fad: normalize bit shift index from memory at r10 + 0x60000 region
        bit_shift_val_a = 32 >>u (32 << u32[8 + r10 + 0x60000])

        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // @12041: Calculate memory pointers based on bit-manipulated offsets
            mem_offset_base_b = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val_a))) + ptr_0_56)
            mem_offset_base_c = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

            if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                // @12121: Load value from memory using double-shifted offset
                retrieved_val_d = u64[32 >>u (32 << (32 >>u (32 << (3 << mem_offset_base_c))) + ptr_0_56) + 0x60000]
                bit_shift_val_e = 32 >>u (32 << ptr_0_48->field_393216)

                if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                    // @12200: XOR operation between loaded memory and retrieved_val_d, then zero out metadata
                    mem_offset_base_b->field_393216 = u64[32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val_e))) + ptr_0_56) + 0x60000] ^ retrieved_val_d
                    ptr_0_40->field_393216 = 0
                    u32[8 + ptr_0_40 + 0x60000] = 0
                    goto block_122ac;
                } else {
                    // @121f8: Default fallback state
                    bit_shift_val_e = 12
                }
            } else {
                // @12119: Default fallback state
                mem_offset_base_c = 12
            }
        } else {
            // @12039: Default fallback state
            bit_shift_val_a = 12
        }
    }

    block_122ac:
    // @122ac
    return
}
fn func_80(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let shift_val_a
    let base_addr_b
    let shift_val_c
    let target_mem_d
    let shift_val_e

    if (0xFEFD0000 >=u r1 - 512) {
        // Evaluate index from memory at offset 0x60008, shift 32 by result
        shift_val_a = 32 >>u (32 << u32[8 + r10 + 0x60000])

        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // Calculate base address index using sequence of shifts and input ptr_0_56
            base_addr_b = 32 >>u (32 << (32 >>u (32 << (3 << shift_val_a))) + ptr_0_56)
            shift_val_c = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

            if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                // Fetch target 64-bit value from memory based on calculation
                target_mem_d = u64[32 >>u (32 << (32 >>u (32 << (3 << shift_val_c))) + ptr_0_56) + 0x60000]
                shift_val_e = 32 >>u (32 << ptr_0_48->field_393216)

                if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                    // Update field by bitwise OR of current memory value and target_mem_d
                    base_addr_b->field_393216 = u64[32 >>u (32 << (32 >>u (32 << (3 << shift_val_e))) + ptr_0_56) + 0x60000] | target_mem_d
                    // Reset fields to zero
                    ptr_0_40->field_393216 = 0
                    u32[8 + ptr_0_40 + 0x60000] = 0
                    goto block_125d8;
                } else {
                    // Bounds overflow/error handling, clamp shift variable
                    shift_val_e = 12
                }
            } else {
                // Bounds overflow/error handling, clamp shift variable
                shift_val_c = 12
            }
        } else {
            // Bounds overflow/error handling, clamp shift variable
            shift_val_a = 12
        }
    }

    block_125d8:
    // @125d8
    return
}
fn func_81(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val_1
    let addr_offset_1
    let base_ptr_val
    let bit_shift_val_2
    let bit_shift_val_3

    // Check if r1 is within valid bounds for memory access
    if (0xFEFD0000 >=u r1 - 520) {
        // @12605: Normalize bit shift amount for word boundary
        bit_shift_val_1 = 32 >>u (32 << u32[8 + r10 + 0x60000])

        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // @12699: Calculate address offsets using bit manipulation patterns
            addr_offset_1 = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val_1))) + ptr_0_56)
            bit_shift_val_2 = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

            if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                // @12779: Dereference memory based on calculated offset
                base_ptr_val = u64[32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val_2))) + ptr_0_56) + 0x60000]
                bit_shift_val_3 = 32 >>u (32 << ptr_0_48->field_393216)

                if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                    // @12858: Apply bitwise mask to memory at computed location
                    addr_offset_1->field_393216 = (-1 ^ u64[32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val_3))) + ptr_0_56) + 0x60000]) & base_ptr_val
                    ptr_0_40->field_393216 = 0
                    u32[8 + ptr_0_40 + 0x60000] = 0
                    goto block_12919;
                } else {
                    // @12850: Default bit shift limit
                    bit_shift_val_3 = 12
                }
            } else {
                // @12771
                bit_shift_val_2 = 12
            }
        } else {
            // @12691
            bit_shift_val_1 = 12
        }
    }

    block_12919:
    // @12919
    return
}
fn func_82(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_mask_idx       // calculated index for bit shifting
    let bit_offset_pos     // calculated bit position
    let extracted_val      // value loaded from memory based on offset
    let target_idx         // index for bitwise field access
    let bit_mask_val       // processed bit mask value

    if (0xFEFD0000 >=u r1 - 520) {
        bit_mask_idx = 32 >>u (32 << u32[8 + r10 + 0x60000]) // Normalize index from memory

        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // Calculate bit position using shift operations to isolate bits
            bit_offset_pos = 32 >>u (32 << (32 >>u (32 << (3 << bit_mask_idx))) + ptr_0_56)
            bit_mask_val = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

            if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                // Read 64-bit value from memory based on bit offset
                extracted_val = u64[32 >>u (32 << (32 >>u (32 << (3 << bit_mask_val))) + ptr_0_56) + 0x60000]
                target_idx = 32 >>u (32 << ptr_0_48->field_393216)

                if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                    // Update field via bitwise XOR inversion and OR mask
                    bit_offset_pos->field_393216 = -1 ^ u64[32 >>u (32 << (32 >>u (32 << (3 << target_idx))) + ptr_0_56) + 0x60000] | extracted_val
                    ptr_0_40->field_393216 = 0
                    u32[8 + ptr_0_40 + 0x60000] = 0
                    goto block_12c5a;
                } else {
                    target_idx = 12 // Default fallback index
                }
            } else {
                bit_mask_val = 12 // Default fallback index
            }
        } else {
            bit_mask_idx = 12 // Default fallback index
        }
    }

    block_12c5a:
    return
}
fn func_83(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_idx_a
    let bit_shift_idx_b
    let bit_shift_idx_c
    let target_val_mask
    let bit_shift_idx_d

    // Verify bounds: Check if r1 (likely stack/offset pointer) is within a valid memory range
    if (0xFEFD0000 >=u r1 - 520) {
        // @12c87: Calculate index for A using field in r10
        bit_shift_idx_a = 32 >>u (32 << u32[8 + r10 + 0x60000])

        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // @12d1b: Calculate indices B and C based on shift logic
            bit_shift_idx_b = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_idx_a))) + ptr_0_56)
            bit_shift_idx_c = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

            if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                // @12dfb: Fetch value via memory offset calculation
                target_val_mask = u64[32 >>u (32 << (32 >>u (32 << (3 << bit_shift_idx_c))) + ptr_0_56) + 0x60000]
                bit_shift_idx_d = 32 >>u (32 << ptr_0_48->field_393216)

                if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                    // @12eda: Apply XOR masking operation to memory
                    bit_shift_idx_b->field_393216 = -1 ^ u64[32 >>u (32 << (32 >>u (32 << (3 << bit_shift_idx_d))) + ptr_0_56) + 0x60000] ^ target_val_mask
                    ptr_0_40->field_393216 = 0
                    u32[8 + ptr_0_40 + 0x60000] = 0 // Reset status or flag
                    goto block_12f9b;
                } else {
                    // @12ed2: Default fallback for index D
                    bit_shift_idx_d = 12
                }
            } else {
                // @12df3: Default fallback for index C
                bit_shift_idx_c = 12
            }
        } else {
            // @12d13: Default fallback for index A
            bit_shift_idx_a = 12
        }
    }

    block_12f9b:
    // @12f9b
    return
}
fn func_84(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val
    let target_ptr
    let offset_val

    // Check if r1 is within the expected bounds (r1 - 408 <= 0xFEFD0000)
    if (0xFEFD0000 >=u r1 - 408) {
        // @12fc8: Calculate shift amount based on memory at r10 + 0x60004
        bit_shift_val = 32 >>u (32 << u32[4 + r10 + 0x60000])

        // Verify index from memory is within valid range [0, 13]
        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @1305c: Calculate pointers using bit manipulation logic
            target_ptr = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val))) + ptr_0_56)
            offset_val = 32 >>u (32 << ptr_0_48->field_393216)

            // Verify secondary condition for field operation
            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @1312e: Perform update, result = base_val + memory_lookup
                target_ptr->field_393216 = u32[8 + ptr_0_48 + 0x60000] + u64[32 >>u (32 << (32 >>u (32 << (3 << offset_val))) + ptr_0_56) + 0x60000]
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_1320a;
            } else {
                // @13126: Clamp offset_val to 12
                offset_val = 12
            }
        } else {
            // @13054: Clamp bit_shift_val to 12
            bit_shift_val = 12
        }
    }

    block_1320a:
    // @1320a
    return
}
fn func_85(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let shift_val_a
    let addr_offset_b
    let shift_val_c

    if (0xFEFD0000 >=u r1 - 408) {
        // Calculate shift based on state stored at Memory[r10 + 0x60004]
        shift_val_a = 32 >>u (32 << u32[4 + r10 + 0x60000])

        // Verify if state is within valid range [0, 13]
        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // Compute memory offsets based on state and system counters
            addr_offset_b = 32 >>u (32 << (32 >>u (32 << (3 << shift_val_a))) + ptr_0_56)
            shift_val_c = 32 >>u (32 << ptr_0_48->field_393216)

            // Validate secondary state against field_393216
            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // Perform calculation: result = multiplied_factor * Memory[calculated_base_addr + 0x60000]
                ptr_0_192->field_393216 = u32[8 + ptr_0_48 + 0x60000] * u64[32 >>u (32 << (32 >>u (32 << (3 << shift_val_c))) + ptr_0_56) + 0x60000]
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_13477;
            } else {
                // Recovery/default case: force constraint to 12
                shift_val_c = 12
            }
        } else {
            // Recovery/default case: force constraint to 12
            shift_val_a = 12
        }
    }

    block_13477:
    return
}
fn func_86(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let shift_val_a
    let shifted_base_ptr
    let combined_offset
    let shift_val_b

    // Check if r1 (likely an address or buffer limit) is within bounds [416, 0xFEFD0000 + 416]
    if (0xFEFD0000 >=u r1 - 416) {
        // @134a4
        // Extract shift value using logic from memory at r10 + 0x60000 + 4
        shift_val_a = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @13538
            shifted_base_ptr = 32 >>u (32 << (32 >>u (32 << (3 << shift_val_a))) + ptr_0_56)
            // Combine high-order mask with u32 value from memory at ptr_0_48 base
            combined_offset = 0x100000000 | u32[8 + ptr_0_48 + 0x60000]
            shift_val_b = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @13639
                // Perform memory subtraction based on shift values and update fields
                shifted_base_ptr->field_393216 = u64[32 >>u (32 << (32 >>u (32 << (3 << shift_val_b))) + ptr_0_56) + 0x60000] - combined_offset
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_136f9;
            } else {
                // @13631
                shift_val_b = 12
            }
        } else {
            // @13530
            shift_val_a = 12
        }
    }

    block_136f9:
    // @136f9
    return
}
fn func_87(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let shift_val_a
    let addr_base
    let addr_offset

    if (0xFEFD0000 >=u r1 - 392) {
        // Calculate shift amount based on memory value at r10 + 0x60004
        shift_val_a = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // Apply bitwise shifting pattern to determine memory address
            addr_base = 32 >>u (32 << (32 >>u (32 << (3 << shift_val_a))) + ptr_0_56)
            addr_offset = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // Update memory at calculated base using value from secondary offset
                addr_base->field_393216 = u32[8 + ptr_0_48 + 0x60000] + u64[32 >>u (32 << (32 >>u (32 << (3 << addr_offset))) + ptr_0_56) + 0x60000]
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_13954;
            } else {
                // Bounds fallback for offset
                addr_offset = 12
            }
        } else {
            // Bounds fallback for shift_val
            shift_val_a = 12
        }
    }

    block_13954:
    // @13954
    return
}
fn func_88(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bitmask_shift
    let target_ptr
    let offset_val

    // Check if r1 is within bounds relative to the 392-byte header
    if (0xFEFD0000 >=u r1 - 392) {
        // Calculate shift based on a flags word at memory offset 0x60004
        bitmask_shift = 32 >>u (32 << u32[4 + r10 + 0x60000])

        // Verify index is within valid range [0, 13]
        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // Calculate base pointer and offset using complex bitwise shifting
            target_ptr = 32 >>u (32 << (32 >>u (32 << (3 << bitmask_shift))) + ptr_0_56)
            offset_val = 32 >>u (32 << ptr_0_48->field_393216)

            // Further bounds check on field_393216
            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // Perform multiplication and store result in target memory locations
                target_ptr->field_393216 = u32[8 + ptr_0_48 + 0x60000] * u64[32 >>u (32 << (32 >>u (32 << (3 << offset_val))) + ptr_0_56) + 0x60000]
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_13baf;
            } else {
                // Default fallback index
                offset_val = 12
            }
        } else {
            // Default fallback shift
            bitmask_shift = 12
        }
    }

    block_13baf:
    return
}
fn func_89(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val
    let target_mem_addr
    let base_offset_val
    let shift_index

    if (0xFEFD0000 >=u r1 - 392) {
        // Calculate shift depth based on memory-mapped configuration value
        bit_shift_val = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // Compute base memory pointer offset using bitwise normalization
            target_mem_addr = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val))) + ptr_0_56)
            base_offset_val = u32[8 + ptr_0_48 + 0x60000]
            shift_index = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // Perform memory relocation: update field based on delta from base_offset_val
                target_mem_addr->field_393216 = u64[32 >>u (32 << (32 >>u (32 << (3 << shift_index))) + ptr_0_56) + 0x60000] - base_offset_val
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_13e0a;
            } else {
                // Clamp shift index to maximum allowed shift
                shift_index = 12
            }
        } else {
            // Clamp bit shift depth to maximum allowed
            bit_shift_val = 12
        }
    }

    block_13e0a:
    // @13e0a
    return
}
fn func_90(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val_1
    let lookup_idx_1
    let lookup_idx_2
    let bit_shift_val_2
    let bit_shift_val_3

    // Check if input parameter r1 is within valid memory range relative to 584 byte offset
    if (0xFEFD0000 >=u r1 - 584) {
        // @13e37: Calculate initial bit shift from memory at r10 + 0x60000
        bit_shift_val_1 = 32 >>u (32 << u32[8 + r10 + 0x60000])

        // Verify if shift value is within valid lookup range [0, 13]
        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // @13ecb: Derive lookup indices based on memory state and ptr_0_56
            lookup_idx_1 = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val_1))) + ptr_0_56)
            lookup_idx_2 = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @13f9d: Retrieve bit shift values from calculated memory addresses
                bit_shift_val_2 = 32 >>u (32 << u64[32 >>u (32 << (32 >>u (32 << (3 << lookup_idx_2))) + ptr_0_56) + 0x60000])
                bit_shift_val_3 = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

                if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                    // @140a5: Update structure fields in memory using computed shifts
                    lookup_idx_1->field_393216 = 32 >>u (32 << (32 >>u (32 << u64[32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val_3))) + ptr_0_56) + 0x60000])) + bit_shift_val_2)
                    ptr_0_40->field_393216 = 0
                    u32[8 + ptr_0_40 + 0x60000] = 0
                    goto block_141a5;
                } else {
                    // @1409d: Default fallback for bit_shift_val_3
                    bit_shift_val_3 = 12
                }
            } else {
                // @13f95: Default fallback for lookup_idx_2
                lookup_idx_2 = 12
            }
        } else {
            // @13ec3: Default fallback for bit_shift_val_1
            bit_shift_val_1 = 12
        }
    }

    block_141a5:
    // @141a5: Function exit
    return
}
fn func_91(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val_1
    let addr_offset_1
    let addr_offset_2
    let base_val_ptr
    let bit_shift_val_2

    if (0xFEFD0000 >=u r1 - 568) {
        // @141d2: Normalize bitshift index using memory at (r10 + 0x60008)
        bit_shift_val_1 = 32 >>u (32 << u32[8 + r10 + 0x60000])

        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // @14266: Calculate memory offset relative to heap base (ptr_0_56)
            addr_offset_1 = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val_1))) + ptr_0_56)
            addr_offset_2 = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

            if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                // @14346: Fetch block pointer from heap and add magic base constant
                base_val_ptr = 0x100000000 + u64[32 >>u (32 << (32 >>u (32 << (3 << addr_offset_2))) + ptr_0_56) + 0x60000]
                bit_shift_val_2 = 32 >>u (32 << ptr_0_48->field_393216)

                if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                    // @1443a: Update field value by subtracting base_val_ptr
                    addr_offset_1->field_393216 = (32 >>u (32 << (32 >>u (32 << u64[32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val_2))) + ptr_0_56) + 0x60000])))) - base_val_ptr
                    ptr_0_40->field_393216 = 0
                    u32[8 + ptr_0_40 + 0x60000] = 0
                    goto block_14530;
                } else {
                    // @14432: Clamp shift value
                    bit_shift_val_2 = 12
                }
            } else {
                // @1433e: Clamp offset
                addr_offset_2 = 12
            }
        } else {
            // @1425e: Clamp shift value
            bit_shift_val_1 = 12
        }
    }

    block_14530:
    // @14530
    return
}
fn func_92(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let offset_state // Temporary storage for calculated structure pointers
    let row_idx
    let col_idx
    let dest_ptr
    let source_val

    if (0xFEFD0000 >=u r1 - 528) {
        // @1455d - Calculate index from memory mapped register at base r10 + 0x60000
        row_idx = 32 >>u (32 << u32[8 + r10 + 0x60000])

        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // @145f1 - Validate bounds and calculate complex address offsets for lookup
            dest_ptr = 32 >>u (32 << (32 >>u (32 << (3 << row_idx))) + ptr_0_56)
            col_idx = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @146c3 - Retrieve operand from memory block at 0x60000
                source_val = u64[32 >>u (32 << (32 >>u (32 << (3 << col_idx))) + ptr_0_56) + 0x60000]
                offset_state = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

                if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                    // @147b0 - Perform multiplication and store result back into structure field
                    dest_ptr->field_393216 = u64[32 >>u (32 << (32 >>u (32 << (3 << offset_state))) + ptr_0_56) + 0x60000] * source_val
                    ptr_0_40->field_393216 = 0
                    u32[8 + ptr_0_40 + 0x60000] = 0 // Reset status word at ptr_0_40
                    goto block_14870;
                } else {
                    // @147a8 - Default index if out of bounds
                    offset_state = 12
                }
            } else {
                // @146bb
                col_idx = 12
            }
        } else {
            // @145e9
            row_idx = 12
        }
    }

    block_14870:
    // @14870
    return
}
fn func_93(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_idx_base      // Used for bit-shifting operations
    let result_val        // Intermediate calculation result
    let table_idx         // Index for memory table lookups
    let target_ptr        // Computed memory offset
    let operand_offset    // Second operand for field calculation
    let shift_out         // Final shift result

    // Check bounds: ensure r1 is within valid memory region relative to constant offset 736
    if (0xFEFD0000 >=u r1 - 736) {
        // Clamp/Mask index via bit manipulation: 32 >>u (32 << x) acts as a specialized bit-limiting operator
        bit_idx_base = 32 >>u (32 << u32[r10 + 0x60000])

        if (u32[r10 + 0x60000] <=u 13 >=u 0) {
            // Complex indexing calculation for memory access
            let var_47 = 32 >>u (32 << u64[32 >>u (32 << (32 >>u (32 << (3 << bit_idx_base))) + ptr_0_56) + 0x60000])
            result_val = var_47

            if (var_47 <u 0) {
                shift_out = 32 >>u (32 << u32[8 + ptr_0_48 + 0x60000])

                if (u32[8 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                    // Set memory flag at computed address
                    u64[32 >>u (32 << (32 >>u (32 << (3 << shift_out))) + ptr_0_56) + 0x60000] = -1
                    goto block_14d24;
                } else {
                    shift_out = 12
                }
            } else {
                goto block_149ea;
            }
        } else {
            bit_idx_base = 12
        }
    }

    block_149ea:
    table_idx = 32 >>u (32 << u32[8 + ptr_0_48 + 0x60000])

    if (u32[8 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
        target_ptr = 32 >>u (32 << (32 >>u (32 << (3 << table_idx))) + ptr_0_56)
        operand_offset = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

        if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
            // Write computed value to object field at target_ptr
            target_ptr->field_393216 = 32 >>u (32 << result_val /u (32 >>u (32 << u64[32 >>u (32 << (32 >>u (32 << (3 << operand_offset))) + ptr_0_56) + 0x60000])))
            goto block_14d24;
        } else {
            operand_offset = 12
        }
    } else {
        table_idx = 12
    }

    // Fallthrough: Reset default values and perform final write
    table_idx = 12
    target_ptr = 32 >>u (32 << (32 >>u (32 << (3 << table_idx))) + ptr_0_56)
    operand_offset = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

    if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
        target_ptr->field_393216 = 32 >>u (32 << result_val /u (32 >>u (32 << u64[32 >>u (32 << (32 >>u (32 << (3 << operand_offset))) + ptr_0_56) + 0x60000])))
        goto block_14d24;
    } else {
        operand_offset = 12
    }

    operand_offset = 12
    target_ptr->field_393216 = 32 >>u (32 << result_val /u (32 >>u (32 << u64[32 >>u (32 << (32 >>u (32 << (3 << operand_offset))) + ptr_0_56) + 0x60000])))

    block_14d24:
    // Reset status fields
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0

    return
}
fn func_94(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let divisor_shift
    let dividend_shift
    let lookup_idx_a
    let lookup_idx_b
    let result_storage_idx
    let overflow_storage_idx
    let div_result_idx

    // Validate memory range for r1 input context
    if (0xFEFD0000 >=u r1 - 928) {
        // Calculate shift depth for divisor lookup (based on r10 + offset)
        divisor_shift = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // Fetch divisor from memory table via calculated shift
            div_result_idx = u64[32 >>u (32 << (32 >>u (32 << (3 << divisor_shift))) + ptr_0_56) + 0x60000]
            lookup_idx_a = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // Fetch dividend from memory table
                let dividend = u64[32 >>u (32 << (32 >>u (32 << (3 << lookup_idx_a))) + ptr_0_56) + 0x60000]
                dividend_shift = dividend

                // Check if dividend is non-zero (non-null pointer/value check)
                if (!(dividend) <u 0) {
                    // Constant 32 for bitwise significance clamping
                    let bit_limit = 32

                    // Perform division check using XOR/AND masking patterns
                    if (bit_limit >>u (bit_limit << (!(-1 ^ dividend_shift) & !(-0x80000000 ^ div_result_idx))) <u 0) {
                        result_storage_idx = 32 >>u (32 << u32[8 + ptr_0_48 + 0x60000])

                        if (u32[8 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                            // Assign quotient to memory location table
                            u64[32 >>u (32 << (32 >>u (32 << (3 << result_storage_idx))) + ptr_0_56) + 0x60000] = dividend_shift /s div_result_idx
                            goto block_15370;
                        } else {
                            result_storage_idx = 12
                        }
                    } else {
                        goto block_1517d;
                    }
                } else {
                    goto block_14fbf;
                }
            } else {
                lookup_idx_a = 12
            }
        } else {
            divisor_shift = 12
        }
    }

    block_14fbf:
    overflow_storage_idx = 32 >>u (32 << u32[8 + ptr_0_48 + 0x60000])

    if (u32[8 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
        // Set error/overflow index to -1
        u64[32 >>u (32 << (32 >>u (32 << (3 << overflow_storage_idx))) + ptr_0_56) + 0x60000] = -1
        goto block_15156;
    } else {
        overflow_storage_idx = 12
    }

    overflow_storage_idx = 12
    u64[32 >>u (32 << (32 >>u (32 << (3 << overflow_storage_idx))) + ptr_0_56) + 0x60000] = -1

    block_15156:
    // Reset state fields
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0
    return

    block_1517d:
    lookup_idx_b = 32 >>u (32 << u32[8 + ptr_0_48 + 0x60000])

    if (u32[8 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
        u64[32 >>u (32 << (32 >>u (32 << (3 << lookup_idx_b))) + ptr_0_56) + 0x60000] = div_result_idx
        goto block_15370;
    } else {
        lookup_idx_b = 12
    }

    lookup_idx_b = 12
    u64[32 >>u (32 << (32 >>u (32 << (3 << lookup_idx_b))) + ptr_0_56) + 0x60000] = div_result_idx

    block_15370:
    goto block_15156;
}
fn func_95(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let index_val_1
    let val_extracted_216
    let index_val_2
    let var_96
    let index_val_3
    let index_val_4

    if (0xFEFD0000 >=u r1 - 752) {
        // Normalize input range via bitwise shift masking
        index_val_1 = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // Memory access into table at 0x60000 offset using ptr_0_56 base
            val_extracted_216 = 32 >>u (32 << u64[32 >>u (32 << (32 >>u (32 << (3 << index_val_1))) + ptr_0_56) + 0x60000])
            index_val_2 = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // Access secondary table or state array
                var_96 = 32 >>u (32 << u64[32 >>u (32 << (32 >>u (32 << (3 << index_val_2))) + ptr_0_56) + 0x60000])
                index_val_3 = var_96

                if (var_96 <u 0) {
                    index_val_4 = 32 >>u (32 << u32[8 + ptr_0_48 + 0x60000])

                    if (u32[8 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                        u64[32 >>u (32 << (32 >>u (32 << (3 << index_val_4))) + ptr_0_56) + 0x60000] = val_extracted_216
                        goto block_1581f;
                    } else {
                        index_val_4 = 12
                    }
                } else {
                    goto block_155df;
                }
            } else {
                index_val_2 = 12
            }
        } else {
            index_val_1 = 12
        }
    }

    block_155df:
    index_val_4 = 32 >>u (32 << u32[8 + ptr_0_48 + 0x60000])

    if (u32[8 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
        // Calculate modular remainder and update state at table offset
        u64[32 >>u (32 << (32 >>u (32 << (3 << index_val_4))) + ptr_0_56) + 0x60000] = 32 >>u (32 << var_96 %u val_extracted_216)
        goto block_1581f;
    } else {
        index_val_4 = 12
    }

    index_val_4 = 12

    u64[32 >>u (32 << (32 >>u (32 << (3 << index_val_4))) + ptr_0_56) + 0x60000] = 32 >>u (32 << var_96 %u val_extracted_216)

    block_1581f:
    // Reset control fields
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0

    return
}
fn func_96(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let offset_idx_a // normalized index for memory access
    let val_a      // value retrieved from memory
    let offset_idx_b // normalized index for memory access
    let val_b      // value retrieved from memory
    let mem_storage_offset // offset into state storage
    let mem_storage_ptr   // pointer into state storage
    let target_idx     // index used for stack/memory targeting

    // Bounds check on input r1 against a memory base 976
    if (0xFEFD0000 >=u r1 - 976) {
        // @1585b
        // Calculate offset from u32 array stored at 0x60000 base
        offset_idx_a = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @158ef
            // Retrieve value from deep pointer path, normalized by bitwise shifts
            val_a = 32 >>u (32 << u64[32 >>u (32 << (32 >>u (32 << (3 << offset_idx_a))) + ptr_0_56) + 0x60000])
            offset_idx_b = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @159e9
                let logic_val = 32 >>u (32 << u64[32 >>u (32 << (32 >>u (32 << (3 << offset_idx_b))) + ptr_0_56) + 0x60000])
                val_b = logic_val

                if (logic_val <u 0) {
                    // @15b63
                    mem_storage_offset = 32 >>u (32 << u32[8 + ptr_0_48 + 0x60000])

                    if (u32[8 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                        // @15be7
                        // Store val_a into memory calculated at mem_storage_offset
                        u64[32 >>u (32 << (32 >>u (32 << (3 << mem_storage_offset))) + ptr_0_56) + 0x60000] = val_a
                        goto block_15c69;
                    } else {
                        // @15bdf
                        mem_storage_offset = 12
                    }
                } else {
                    // @15a93 - Jump to arithmetic logic
                    goto block_15ab0;
                }
            } else {
                // @159e1
                offset_idx_b = 12
            }
        } else {
            // @158e7
            offset_idx_a = 12
        }
    }

    block_15ab0:
    // @15ab0
    // Perform bitwise logic to evaluate conditional branch
    let shift_op = 32

    if (shift_op >>u (shift_op << (!(0xFFFFFFFF ^ val_b) & !(0x80000000 ^ val_a))) <u 0) {
        // @15d80
        target_idx = 32 >>u (32 << u32[8 + ptr_0_48 + 0x60000])

        if (u32[8 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
            // @15e04
            // Perform signed modulo operation and store
            u64[32 >>u (32 << (32 >>u (32 << (3 << target_idx))) + ptr_0_56) + 0x60000] = val_b %s val_a
            goto block_15ea9;
        } else {
            // @15dfc
            target_idx = 12
        }
    } else {
        // @15b5e
        goto block_15c90;
    }

    block_15c90:
    // @15c90 - Default case: zero out memory location
    mem_storage_ptr = 32 >>u (32 << u32[8 + ptr_0_48 + 0x60000])

    if (u32[8 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
        // @15d14
        u64[32 >>u (32 << (32 >>u (32 << (3 << mem_storage_ptr))) + ptr_0_56) + 0x60000] = 0
        goto block_15ea9;
    } else {
        // @15d0c
        mem_storage_ptr = 12
    }

    // Secondary path logic...
    mem_storage_ptr = 12
    u64[32 >>u (32 << (32 >>u (32 << (3 << mem_storage_ptr))) + ptr_0_56) + 0x60000] = 0

    target_idx = 32 >>u (32 << u32[8 + ptr_0_48 + 0x60000])
    if (u32[8 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
        u64[32 >>u (32 << (32 >>u (32 << (3 << target_idx))) + ptr_0_56) + 0x60000] = val_b %s val_a
        goto block_15ea9;
    } else {
        target_idx = 12
    }

    target_idx = 12
    u64[32 >>u (32 << (32 >>u (32 << (3 << target_idx))) + ptr_0_56) + 0x60000] = val_b %s val_a

    block_15ea9:
    block_15c69:
    // Reset control fields
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0

    // @15a98
    return
}
fn func_97(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let shift_reg_val
    let index_map_base
    let accumulated_val
    let offset_scale_val
    let intermediate_calc

    // Verify bounds: Check if r1 - 512 is within the allowed memory range (0xFEFD0000)
    if (0xFEFD0000 >=u r1 - 512) {
        // Fetch value from memory at r10 + 0x60008, normalize shift
        shift_reg_val = 32 >>u (32 << u32[8 + r10 + 0x60000])

        // Verify index range: [0, 13]
        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // Apply double-shift bit manipulation to calculate memory index
            index_map_base = 32 >>u (32 << (32 >>u (32 << (3 << shift_reg_val))) + ptr_0_56)
            accumulated_val = 32 >>u (32 << ptr_0_48->field_393216)

            // Nested bounds check for field_393216
            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // Fetch 64-bit value using calculated offset from memory base 0x60000
                intermediate_calc = u64[32 >>u (32 << (32 >>u (32 << (3 << accumulated_val))) + ptr_0_56) + 0x60000]
                offset_scale_val = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

                if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                    // Update field with sum of indexed memory read and previous intermediate value
                    index_map_base->field_393216 = u64[32 >>u (32 << (32 >>u (32 << (3 << offset_scale_val))) + ptr_0_56) + 0x60000] + intermediate_calc
                    // Reset internal state registers
                    ptr_0_40->field_393216 = 0
                    u32[8 + ptr_0_40 + 0x60000] = 0
                    goto block_161c2;
                } else {
                    // Fallback to default index
                    offset_scale_val = 12
                }
            } else {
                // Fallback to default index
                accumulated_val = 12
            }
        } else {
            // Fallback to default index
            shift_reg_val = 12
        }
    }

    block_161c2:
    // Exit routine
    return
}
fn func_98(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_idx_1
    let addr_offset_1
    let addr_offset_2
    let val_data_1
    let addr_offset_3

    // Check if input parameter r1 is within valid memory bounds constrained by 0xFEFD0000
    if (0xFEFD0000 >=u r1 - 512) {
        // @161ef: Calculate bit shift index from memory address + offset
        bit_shift_idx_1 = 32 >>u (32 << u32[8 + r10 + 0x60000])

        // Validate index range (0 to 13)
        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // @16283: Calculate memory offsets based on bit-shifted indices and global base
            addr_offset_1 = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_idx_1))) + ptr_0_56)
            addr_offset_2 = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

            if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                // @16363: Retrieve value from memory mapped region 0x60000
                val_data_1 = u64[32 >>u (32 << (32 >>u (32 << (3 << addr_offset_2))) + ptr_0_56) + 0x60000]
                addr_offset_3 = 32 >>u (32 << ptr_0_48->field_393216)

                if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                    // @16442: Perform subtraction and update structure field
                    addr_offset_1->field_393216 = u64[32 >>u (32 << (32 >>u (32 << (3 << addr_offset_3))) + ptr_0_56) + 0x60000] - val_data_1
                    ptr_0_40->field_393216 = 0
                    u32[8 + ptr_0_40 + 0x60000] = 0
                    goto block_164ee;
                } else {
                    // @1643a: Clamp invalid index
                    addr_offset_3 = 12
                }
            } else {
                // @1635b: Clamp invalid index
                addr_offset_2 = 12
            }
        } else {
            // @1627b: Clamp invalid index
            bit_shift_idx_1 = 12
        }
    }

    block_164ee:
    // @164ee
    return
}
fn func_99(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let mask_val            // Corresponds to ptr_0_136
    let target_node_addr    // Corresponds to ptr_0_192
    let shift_factor        // Corresponds to ptr_0_256
    let operand_a           // Corresponds to ptr_0_320
    let operand_b           // Corresponds to ptr_0_392

    if (0xFEFD0000 >=u r1 - 512) {
        // Calculate mask from memory at r10 + 0x60008 (u32 array base + offset)
        mask_val = 32 >>u (32 << u32[8 + r10 + 0x60000])

        // Verify index is within valid range [0, 13]
        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // Memory address calculation utilizing bitwise shifts
            target_node_addr = 32 >>u (32 << (32 >>u (32 << (3 << mask_val))) + ptr_0_56)
            shift_factor = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // Fetch operand_a from memory calculated via shift logic
                operand_a = u64[32 >>u (32 << (32 >>u (32 << (3 << shift_factor))) + ptr_0_56) + 0x60000]
                operand_b = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

                if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                    // Update field in target_node with product of fetched operands
                    target_node_addr->field_393216 = u64[32 >>u (32 << (32 >>u (32 << (3 << operand_b))) + ptr_0_56) + 0x60000] * operand_a
                    ptr_0_40->field_393216 = 0
                    u32[8 + ptr_0_40 + 0x60000] = 0 // Clear flag/counter in struct
                    goto block_1681a;
                } else {
                    operand_b = 12
                }
            } else {
                shift_factor = 12
            }
        } else {
            mask_val = 12
        }
    }

    block_1681a:
    return
}
fn func_100(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_idx_res
    let mem_offset_calc
    let divisor_idx
    let mask_val
    let divisor_val
    let field_selector

    if (0xFEFD0000 >=u r1 - 808) {
        // Calculate bitmask index based on memory at r10 + 0x60000
        let bit_shift_val = 32 >>u (32 << u32[r10 + 0x60000])

        if (u32[r10 + 0x60000] <=u 13 >=u 0) {
            let limit_a = 32
            let limit_b = 32

            // Check if memory value is non-negative
            if (!(u64[limit_b >>u (limit_b << (limit_a >>u (limit_a << (3 << bit_shift_val))) + ptr_0_56) + 0x60000] <u 0)) {
                mem_offset_calc = 32 >>u (32 << u32[8 + ptr_0_48 + 0x60000])

                if (u32[8 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                    divisor_idx = 32 >>u (32 << (32 >>u (32 << (3 << mem_offset_calc))) + ptr_0_56)
                    mask_val = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

                    if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                        // Retrieve divisor from memory structure
                        divisor_val = u64[32 >>u (32 << (32 >>u (32 << (3 << mask_val))) + ptr_0_56) + 0x60000]
                        field_selector = 32 >>u (32 << ptr_0_48->field_393216)

                        if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                            // Perform division and store in memory field
                            divisor_idx->field_393216 = u64[32 >>u (32 << (32 >>u (32 << (3 << field_selector))) + ptr_0_56) + 0x60000] /u divisor_val
                            goto block_16d56;
                        } else {
                            field_selector = 12
                        }
                    } else {
                        mask_val = 12
                    }
                } else {
                    mem_offset_calc = 12
                }
            } else {
                goto block_16991;
            }
        } else {
            bit_shift_val = 12
        }
    }

    block_16991:
    bit_idx_res = 32 >>u (32 << u32[8 + ptr_0_48 + 0x60000])

    if (u32[8 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
        // Set memory word to -1 (error sentinel)
        u64[32 >>u (32 << (32 >>u (32 << (3 << bit_idx_res))) + ptr_0_56) + 0x60000] = -1
        goto block_16d56;
    } else {
        bit_idx_res = 12
    }

    bit_idx_res = 12
    u64[32 >>u (32 << (32 >>u (32 << (3 << bit_idx_res))) + ptr_0_56) + 0x60000] = -1

    block_16d56:
    // Reset object fields
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0
    return
}
fn func_101(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let index_a
    let numerator
    let index_b
    let index_c
    let index_d
    let index_e
    let index_f

    if (0xFEFD0000 >=u r1 - 896) {
        // Calculate offset index from memory at r10
        index_a = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // Load numerator from memory pool
            numerator = u64[32 >>u (32 << (32 >>u (32 << (3 << index_a))) + ptr_0_56) + 0x60000]
            index_b = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // Load denominator/divisor from memory pool
                let divisor = u64[32 >>u (32 << (32 >>u (32 << (3 << index_b))) + ptr_0_56) + 0x60000]
                index_c = divisor

                if (!(divisor) <u 0) {
                    let bit_width = 32

                    // Check for invalid divisor (0 or overflow conditions)
                    if (bit_width >>u (bit_width << (!(-1 ^ index_c) & !(-0x8000000000000000 ^ numerator))) <u 0) {
                        index_f = 32 >>u (32 << u32[8 + ptr_0_48 + 0x60000])

                        if (u32[8 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                            // Perform division: result = numerator / divisor
                            u64[32 >>u (32 << (32 >>u (32 << (3 << index_f))) + ptr_0_56) + 0x60000] = index_c /s numerator
                            goto block_1737a;
                        } else {
                            index_f = 12
                        }
                    } else {
                        goto block_17187;
                    }
                } else {
                    goto block_16fc9;
                }
            } else {
                index_b = 12
            }
        } else {
            index_a = 12
        }
    }

    block_16fc9:
    index_d = 32 >>u (32 << u32[8 + ptr_0_48 + 0x60000])

    if (u32[8 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
        // Set error code/null sentinel (-1) in memory
        u64[32 >>u (32 << (32 >>u (32 << (3 << index_d))) + ptr_0_56) + 0x60000] = -1
        goto block_17160;
    } else {
        index_d = 12
    }

    index_d = 12
    u64[32 >>u (32 << (32 >>u (32 << (3 << index_d))) + ptr_0_56) + 0x60000] = -1

    block_17160:
    // Reset state fields
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0
    return

    block_17187:
    index_e = 32 >>u (32 << u32[8 + ptr_0_48 + 0x60000])

    if (u32[8 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
        // Fallback: update memory with numerator value
        u64[32 >>u (32 << (32 >>u (32 << (3 << index_e))) + ptr_0_56) + 0x60000] = numerator
        goto block_1737a;
    } else {
        index_e = 12
    }

    index_e = 12
    u64[32 >>u (32 << (32 >>u (32 << (3 << index_e))) + ptr_0_56) + 0x60000] = numerator

    block_1737a:
    goto block_17160;
}
fn func_102(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let offset_state // Shared state or base pointer
    let shift_op_1
    let addr_calc_1
    let base_ptr_lookup
    let shift_op_2
    let index_val
    let divisor_val
    let register_index

    if (0xFEFD0000 >=u r1 - 944) {
        // Calculate shift factor based on state at r10+0x60000
        let shift_op_1 = 32 >>u (32 << u32[r10 + 0x60000])

        if (u32[r10 + 0x60000] <=u 13 >=u 0) {
            let mask_a = 32
            let mask_b = 32

            // Validate memory content at calculated address against 0
            if (!(u64[mask_b >>u (mask_b << (mask_a >>u (mask_a << (3 << shift_op_1))) + ptr_0_56) + 0x60000]) <u 0) {
                shift_op_2 = 32 >>u (32 << u32[8 + ptr_0_48 + 0x60000])

                if (u32[8 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                    base_ptr_lookup = 32 >>u (32 << (32 >>u (32 << (3 << shift_op_2))) + ptr_0_56)
                    divisor_val = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

                    if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                        index_val = u64[32 >>u (32 << (32 >>u (32 << (3 << divisor_val))) + ptr_0_56) + 0x60000]
                        register_index = 32 >>u (32 << ptr_0_48->field_393216)

                        if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                            // Perform modulus arithmetic on field value
                            base_ptr_lookup->field_393216 = u64[32 >>u (32 << (32 >>u (32 << (3 << register_index))) + ptr_0_56) + 0x60000] %u index_val
                            goto block_1798a;
                        } else {
                            register_index = 12
                        }
                    } else {
                        divisor_val = 12
                    }
                } else {
                    shift_op_2 = 12
                }
            } else {
                goto block_174de;
            }
        } else {
            shift_op_1 = 12
        }
    }

    block_174de:
    offset_state = 32 >>u (32 << u32[8 + ptr_0_48 + 0x60000])

    if (u32[8 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
        base_ptr_lookup = 32 >>u (32 << (32 >>u (32 << (3 << offset_state))) + ptr_0_56)
        divisor_val = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

        if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
            base_ptr_lookup->field_393216 = u64[32 >>u (32 << (32 >>u (32 << (3 << divisor_val))) + ptr_0_56) + 0x60000]
            goto block_1798a;
        } else {
            divisor_val = 12
        }
    } else {
        offset_state = 12
    }

    offset_state = 12
    base_ptr_lookup = 32 >>u (32 << (32 >>u (32 << (3 << offset_state))) + ptr_0_56)
    divisor_val = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

    if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
        base_ptr_lookup->field_393216 = u64[32 >>u (32 << (32 >>u (32 << (3 << divisor_val))) + ptr_0_56) + 0x60000]
        goto block_1798a;
    } else {
        divisor_val = 12
    }

    divisor_val = 12
    base_ptr_lookup->field_393216 = u64[32 >>u (32 << (32 >>u (32 << (3 << divisor_val))) + ptr_0_56) + 0x60000]

    block_1798a:
    // Reset object field and internal state flag
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0

    return
}
fn func_103(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let offset_shift_idx // Offset calculated from shift logic
    let dividend_val      // Value for modular division
    let divisor_shift_idx
    let mod_res_write_ptr
    let result_storage_idx
    let op_res_idx
    let operand_idx

    // Validate memory bounds for specific structure access at 0x60000
    if (0xFEFD0000 >=u r1 - 896) {
        // Calculate shift register index from field at [r10 + 0x60004]
        offset_shift_idx = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // Retrieve dividend from memory based on calculated index and counter
            dividend_val = u64[32 >>u (32 << (32 >>u (32 << (3 << offset_shift_idx))) + ptr_0_56) + 0x60000]
            divisor_shift_idx = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // Retrieve divisor from memory using shift-masked index
                let div_val = u64[32 >>u (32 << (32 >>u (32 << (3 << divisor_shift_idx))) + ptr_0_56) + 0x60000]
                mod_res_write_ptr = div_val

                if (!(div_val) <u 0) {
                    let word_size = 32

                    // Check for non-zero bitmasks before modular operation
                    if (word_size >>u (word_size << (!(-1 ^ mod_res_write_ptr) & !(-0x8000000000000000 ^ dividend_val))) <u 0) {
                        op_res_idx = 32 >>u (32 << u32[8 + ptr_0_48 + 0x60000])

                        if (u32[8 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                            // Write remainder of (dividend % divisor) to determined memory slot
                            u64[32 >>u (32 << (32 >>u (32 << (3 << op_res_idx))) + ptr_0_56) + 0x60000] = mod_res_write_ptr %s dividend_val
                            goto block_17fa6;
                        } else {
                            op_res_idx = 12
                        }
                    } else {
                        goto block_17db5;
                    }
                } else {
                    goto block_17bfd;
                }
            } else {
                divisor_shift_idx = 12
            }
        } else {
            offset_shift_idx = 12
        }
    }

    block_17bfd:
    result_storage_idx = 32 >>u (32 << u32[8 + ptr_0_48 + 0x60000])

    if (u32[8 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
        u64[32 >>u (32 << (32 >>u (32 << (3 << result_storage_idx))) + ptr_0_56) + 0x60000] = dividend_val
        goto block_17d8e;
    } else {
        result_storage_idx = 12
    }

    result_storage_idx = 12
    u64[32 >>u (32 << (32 >>u (32 << (3 << result_storage_idx))) + ptr_0_56) + 0x60000] = dividend_val

    block_17d8e:
    // Reset status fields in memory structure
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0
    return

    block_17db5:
    operand_idx = 32 >>u (32 << u32[8 + ptr_0_48 + 0x60000])

    if (u32[8 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
        u64[32 >>u (32 << (32 >>u (32 << (3 << operand_idx))) + ptr_0_56) + 0x60000] = 0
        goto block_17fa6;
    } else {
        operand_idx = 12
    }

    operand_idx = 12
    u64[32 >>u (32 << (32 >>u (32 << (3 << operand_idx))) + ptr_0_56) + 0x60000] = 0

    block_17fa6:
    goto block_17d8e;
}
fn func_104(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let index_val_1
    let index_val_2
    let index_val_3
    let memory_lookup_1
    let status_code
    let result_val

    // Check bounds against memory offset/length
    if (0xFEFD0000 >=u r1 - 1040) {
        // @17fc0: Normalize index from memory array base
        index_val_1 = 32 >>u (32 << u32[8 + r10 + 0x60000])

        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // @18054: Calculate shifted offsets for indirect addressing
            index_val_2 = 32 >>u (32 << (32 >>u (32 << (3 << index_val_1))) + ptr_0_56)
            index_val_3 = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @18126: Fetch value at calculated memory pointer
                memory_lookup_1 = u64[32 >>u (32 << (32 >>u (32 << (3 << index_val_3))) + ptr_0_56) + 0x60000]
                status_code = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

                if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                    // @18213: Indirect load of status register
                    let var_126 = u64[32 >>u (32 << (32 >>u (32 << (3 << status_code))) + ptr_0_56) + 0x60000]
                    result_val = var_126

                    if (var_126 >s 0 <u 0) {
                        // @18330
                        goto block_18335;
                    } else {
                        // @182b7
                        goto block_18308;
                    }
                }
            } else {
                // @1811e
                index_val_3 = 12
            }
        } else {
            // @1804c
            index_val_1 = 12
        }
    }

    block_18308:
    // @18308

    block_18335:
    if (memory_lookup_1 >s 0 <u 0) {
        // @183bf
        goto block_183c4;
    } else {
        // @18370
    }
    // @18375

    // @183bf

    block_183c4:
    if (ptr_0_656 <u 0) {
        // Handle negative pointer case
        goto block_1858e;
    } else {
        // @183ea
    }
    // @183ef: Complex bitwise arithmetic logic for state transition
    let var_260 = (-1 ^ (32 >>u (0xFFFFFFFF & (0xFFFFFFFF & ptr_0_648) * (32 >>u ptr_0_560)) + (32 >>u (0xFFFFFFFF & ptr_0_648) * (0xFFFFFFFF & ptr_0_560)) + (0xFFFFFFFF & (0xFFFFFFFF & ptr_0_560) * (32 >>u ptr_0_648))) + (32 >>u (0xFFFFFFFF & ptr_0_648) * (32 >>u ptr_0_560)) + (32 >>u (0xFFFFFFFF & ptr_0_560) * (32 >>u ptr_0_648)) + (32 >>u ptr_0_648) * (32 >>u ptr_0_560)) + (32 >>u (32 << !(ptr_0_560 * ptr_0_648)))
    goto block_182d4;

    // @18589

    block_1858e:
    // @1858e: Alternative state calculation
    let var_317 = (32 >>u (0xFFFFFFFF & (0xFFFFFFFF & ptr_0_648) * (32 >>u ptr_0_560)) + (32 >>u (0xFFFFFFFF & ptr_0_560) * (0xFFFFFFFF & ptr_0_648)) + (0xFFFFFFFF & (0xFFFFFFFF & ptr_0_560) * (32 >>u ptr_0_648))) + (32 >>u (0xFFFFFFFF & ptr_0_648) * (32 >>u ptr_0_560)) + (32 >>u (0xFFFFFFFF & ptr_0_560) * (32 >>u ptr_0_648)) + (32 >>u ptr_0_648) * (32 >>u ptr_0_560)

    block_182d4:
    // Finalize state updates
    index_val_2->field_393216 = ptr_0_504
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0

    // @182bc
    return
}
fn func_105(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_idx
    let high_bits_result
    let low_bits_mask
    let bit_shift_idx_2
    let val_struct_base
    let val_high_parts
    let product_low_part
    let bit_shift_idx_3

    // Check if r1 is within valid bounds after offset
    if (0xFEFD0000 >=u r1 - 656) {
        // @186d7: Calculate rotation/shift index based on memory at r10 + 0x60004
        bit_shift_idx = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @1876b: Extract 64-bit value using calculated shift and base memory offset
            let raw_val = u64[32 >>u (32 << (32 >>u (32 << (3 << bit_shift_idx))) + ptr_0_56) + 0x60000]
            high_bits_result = 32 >>u raw_val // Get upper 32 bits
            low_bits_mask = 0xFFFFFFFF & raw_val // Mask lower 32 bits
            bit_shift_idx_2 = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @1886d: Fetch second reference value
                let val_ref_2 = u64[32 >>u (32 << (32 >>u (32 << (3 << bit_shift_idx_2))) + ptr_0_56) + 0x60000]
                val_struct_base = val_ref_2
                let split_val = 32 >>u val_ref_2
                val_high_parts = split_val
                product_low_part = split_val * low_bits_mask
                bit_shift_idx_3 = 32 >>u (32 << u32[8 + ptr_0_48 + 0x60000])

                if (u32[8 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                    // @18977: Perform complex arithmetic update on memory
                    u64[32 >>u (32 << (32 >>u (32 << (3 << bit_shift_idx_3))) + ptr_0_56) + 0x60000] = (32 >>u (0xFFFFFFFF & high_bits_result * (0xFFFFFFFF & val_struct_base)) + (32 >>u (0xFFFFFFFF & val_struct_base) * low_bits_mask) + (0xFFFFFFFF & product_low_part)) + (32 >>u high_bits_result * (0xFFFFFFFF & val_struct_base)) + (32 >>u product_low_part) + val_high_parts * high_bits_result
                    ptr_0_40->field_393216 = 0
                    u32[8 + ptr_0_40 + 0x60000] = 0
                    goto block_18af6;
                } else {
                    // @1896f: Default index for out-of-bounds safety
                    bit_shift_idx_3 = 12
                }
            } else {
                // @18865
                bit_shift_idx_2 = 12
            }
        } else {
            // @18763
            bit_shift_idx = 12
        }
    }

    block_18af6:
    // @18af6
    return
}
fn func_106(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let shift_idx_a
    let base_ptr_b
    let shift_idx_c
    let val_d
    let shift_idx_e
    let result_f

    if (0xFEFD0000 >=u r1 - 920) {
        // @18b23: Normalize shift index to 0-31 range
        shift_idx_a = 32 >>u (32 << u32[8 + r10 + 0x60000])

        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // @18bb7: Calculate base pointers using bit-shifting masks
            base_ptr_b = 32 >>u (32 << (32 >>u (32 << (3 << shift_idx_a))) + ptr_0_56)
            shift_idx_c = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @18c89: Load indirect values via calculated pointer offset
                val_d = u64[32 >>u (32 << (32 >>u (32 << (3 << shift_idx_c))) + ptr_0_56) + 0x60000]
                shift_idx_e = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

                if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                    // @18d76: Retrieve final conditional value
                    let val_fetch = u64[32 >>u (32 << (32 >>u (32 << (3 << shift_idx_e))) + ptr_0_56) + 0x60000]
                    result_f = val_fetch

                    if (val_fetch >s 0 <u 0) {
                        // @19028: Success branch for calculation
                        goto block_1902d;
                    } else {
                        // @18e1a: Fallback branch
                        goto block_18e6b;
                    }
                }
            } else {
                // @18c81: Default shift index
                shift_idx_c = 12
            }
        } else {
            // @18baf: Default shift index
            shift_idx_a = 12
        }
    }

    block_18e6b:
    // @18e6b: Complex bitwise arithmetic for non-positive case
    let var_231 = (-1 ^ (32 >>u (0xFFFFFFFF & (0xFFFFFFFF & val_d) * (32 >>u 1 + (-1 ^ result_f))) + (32 >>u (0xFFFFFFFF & val_d) * (0xFFFFFFFF & 1 + (-1 ^ result_f))) + (0xFFFFFFFF & (32 >>u val_d) * (0xFFFFFFFF & 1 + (-1 ^ result_f)))) + (32 >>u (0xFFFFFFFF & val_d) * (32 >>u 1 + (-1 ^ result_f))) + (32 >>u (32 >>u val_d) * (0xFFFFFFFF & 1 + (-1 ^ result_f))) + (32 >>u val_d) * (32 >>u 1 + (-1 ^ result_f))) + (32 >>u (32 << !((1 + (-1 ^ result_f)) * val_d)))

    block_18e37:
    // @18e37: Memory cleanup and reset pointer fields
    base_ptr_b->field_393216 = ptr_0_504
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0

    // @18e1f: Exit function
    return

    block_1902d:
    // @1902d: Optimized bitwise arithmetic path
    let var_288 = (32 >>u (0xFFFFFFFF & (0xFFFFFFFF & val_d) * (32 >>u result_f)) + (32 >>u (0xFFFFFFFF & val_d) * (0xFFFFFFFF & result_f)) + (0xFFFFFFFF & (32 >>u val_d) * (0xFFFFFFFF & result_f))) + (32 >>u (0xFFFFFFFF & val_d) * (32 >>u result_f)) + (32 >>u (32 >>u val_d) * (0xFFFFFFFF & result_f)) + (32 >>u val_d) * (32 >>u result_f)
    goto block_18e37;
}
fn func_107(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val_a
    let mem_target_ptr
    let bit_shift_val_b
    let val_a
    let bit_shift_val_c
    let result_val

    if (0xFEFD0000 >=u r1 - 544) {
        // Calculate shift based on PVM memory state at offset 0x60000
        bit_shift_val_a = 32 >>u (32 << u32[8 + r10 + 0x60000])

        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // Calculate pointer into memory block for state retrieval
            mem_target_ptr = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val_a))) + ptr_0_56)
            bit_shift_val_b = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

            if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                // Fetch value from state memory
                val_a = u64[32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val_b))) + ptr_0_56) + 0x60000]
                bit_shift_val_c = 32 >>u (32 << ptr_0_48->field_393216)

                if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                    // Compare values and perform memory swap if condition met
                    let val_b = u64[32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val_c))) + ptr_0_56) + 0x60000]
                    result_val = val_b

                    if (val_a <s val_b >=u 0) {
                        // Apply state update: reassign memory fields
                        mem_target_ptr->field_393216 = result_val
                        ptr_0_40->field_393216 = 0
                        u32[8 + ptr_0_40 + 0x60000] = 0
                        goto block_194b4;
                    } else {
                        // fallback assignment
                        result_val = val_a
                    }
                } else {
                    bit_shift_val_c = 12
                }
            } else {
                bit_shift_val_b = 12
            }
        } else {
            bit_shift_val_a = 12
        }
    }

    block_194b4:
    // Exit routine
    return
}
fn func_108(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_idx_a
    let addr_offset_a
    let addr_offset_b
    let val_a
    let val_b
    let selected_val

    // Verify bounds: r1 is likely a memory pointer or buffer size
    if (0xFEFD0000 >=u r1 - 544) {
        // @194e1 - Calculate bit index from r10 memory store
        bit_idx_a = 32 >>u (32 << u32[8 + r10 + 0x60000])

        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // @19575 - Calculate base address offsets using bit index and global ptr
            addr_offset_a = 32 >>u (32 << (32 >>u (32 << (3 << bit_idx_a))) + ptr_0_56)
            addr_offset_b = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

            if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                // @19655 - Fetch comparison values from memory regions
                val_a = u64[32 >>u (32 << (32 >>u (32 << (3 << addr_offset_b))) + ptr_0_56) + 0x60000]
                ptr_0_392 = 32 >>u (32 << ptr_0_48->field_393216)

                if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                    // @19734 - Compare values fetched from memory
                    let val_b = u64[32 >>u (32 << (32 >>u (32 << (3 << ptr_0_392))) + ptr_0_56) + 0x60000]
                    selected_val = val_b

                    if (val_a <u val_b >=u 0) {
                        // @197eb - Update memory fields if condition met
                        addr_offset_a->field_393216 = selected_val
                        ptr_0_40->field_393216 = 0
                        u32[8 + ptr_0_40 + 0x60000] = 0
                        goto block_1981f;
                    } else {
                        // @197e2
                        selected_val = val_a
                    }
                } else {
                    // @1972c - Default index if out of range
                    ptr_0_392 = 12
                }
            } else {
                // @1964d - Default index if out of range
                addr_offset_b = 12
            }
        } else {
            // @1956d - Default index if out of range
            bit_idx_a = 12
        }
    }

    block_1981f:
    // @1981f
    return
}
fn func_109(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val    // Holds calculated shift magnitude
    let target_node_addr // Address calculated for memory node access
    let node_idx_a
    let node_idx_b
    let node_idx_c
    let final_val

    if (0xFEFD0000 >=u r1 - 544) {
        // @1984c: Calculate initial bit shift from memory at r10 + 0x60000
        bit_shift_val = 32 >>u (32 << u32[8 + r10 + 0x60000])

        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // @198e0: Calculate node address using base r10 offset
            target_node_addr = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val))) + ptr_0_56)
            node_idx_a = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

            if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                // @199c0: Retrieve value from memory structure
                node_idx_b = u64[32 >>u (32 << (32 >>u (32 << (3 << node_idx_a))) + ptr_0_56) + 0x60000]
                node_idx_c = 32 >>u (32 << ptr_0_48->field_393216)

                if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                    // @19a9f: Traverse deeper structure to retrieve comparison value
                    let var_126 = u64[32 >>u (32 << (32 >>u (32 << (3 << node_idx_c))) + ptr_0_56) + 0x60000]
                    final_val = var_126

                    if (var_126 <s node_idx_b >=u 0) {
                        // @19b56: Update state if new value is smaller
                        target_node_addr->field_393216 = final_val
                        ptr_0_40->field_393216 = 0
                        u32[8 + ptr_0_40 + 0x60000] = 0
                        goto block_19b8a;
                    } else {
                        // @19b4d: Fallback to existing node_idx_b value
                        final_val = node_idx_b
                    }
                } else {
                    // @19a97: Clamp index to 12
                    node_idx_c = 12
                }
            } else {
                // @199b8: Clamp index to 12
                node_idx_a = 12
            }
        } else {
            // @198d8: Clamp shift to 12
            bit_shift_val = 12
        }
    }

    block_19b8a:
    // @19b8a
    return
}
fn func_110(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val
    let target_node_idx
    let base_ptr_offset
    let node_val_a
    let node_val_b
    let final_result

    // Verify memory bounds for r1 relative to constant offset 544
    if (0xFEFD0000 >=u r1 - 544) {
        // @19bb7: Calculate shift factor from memory mapped configuration at r10 + 0x60000
        bit_shift_val = 32 >>u (32 << u32[8 + r10 + 0x60000])

        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // @19c4b: Calculate target node index using bit_shift_val and global state ptr_0_56
            target_node_idx = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val))) + ptr_0_56)
            base_ptr_offset = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

            if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                // @19d2b: Retrieve node value A from memory offset 0x60000
                node_val_a = u64[32 >>u (32 << (32 >>u (32 << (3 << base_ptr_offset))) + ptr_0_56) + 0x60000]
                node_val_b = 32 >>u (32 << ptr_0_48->field_393216)

                if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                    // @19e0a: Retrieve node value B and compare with node_val_a
                    let val_compare = u64[32 >>u (32 << (32 >>u (32 << (3 << node_val_b))) + ptr_0_56) + 0x60000]
                    final_result = val_compare

                    if (val_compare <u node_val_a >=u 0) {
                        // @19ec1: Update memory fields if node_val_a is larger
                        target_node_idx->field_393216 = final_result
                        ptr_0_40->field_393216 = 0
                        u32[8 + ptr_0_40 + 0x60000] = 0
                        goto block_19ef5;
                    } else {
                        // @19eb8: Cap result to node_val_a
                        final_result = node_val_a
                    }
                } else {
                    // @19e02: Default shift index
                    node_val_b = 12
                }
            } else {
                // @19d23: Default pointer offset
                base_ptr_offset = 12
            }
        } else {
            // @19c43: Default bit shift
            bit_shift_val = 12
        }
    }

    block_19ef5:
    // @19ef5
    return
}
fn func_111(input_val: u64, offset: u64, base_ptr: u64, r10: u64, r11: u64, r12: u64) {
    // Check if (offset - 104) is within the valid addressable range of 0xFEFD0000
    if (0xFEFD0000 >=u offset - 104) {
        // @19f21
        // Store status code 3 at base_ptr + 0x60000
        u32[base_ptr + 0x60000] = 3
        // Clear status code at base_ptr + 0x6000C
        u32[12 + base_ptr + 0x60000] = 0
    }

    // @19f65
    return
}
fn func_112(input_len: u64, r1: u64, heap_base: u64, r10: u64, r11: u64, r12: u64) {
    // Check if input_len + 104 is within safe bounds relative to 0xFEFD0000
    if (0xFEFD0000 >=u r1 - 104) {
        // @19f8f
        // Clear status flags or metadata at heap_base + 0x60000
        u32[heap_base + 0x60000] = 0
        // Clear secondary field at heap_base + 0x60008
        u32[8 + heap_base + 0x60000] = 0
    }

    // @19fd2
    return
}
fn func_113(buffer_ptr: u64, size: u64, vm_memory_base: u64, source_ptr: u64, r11: u64, r12: u64) {
    // Check if buffer size satisfies memory alignment/bounds requirements
    if (0xFEFD0000 >=u size - 128) {
        // @19ffc: Initialize status fields in VM memory structure
        u32[vm_memory_base + 0x60000] = 3
        u32[0xC + vm_memory_base + 0x60000] = 3
        
        // Copy state word from source to memory destination
        u32[0x10 + vm_memory_base + 0x60000] = u32[source_ptr + 0x60000]
    }

    // @1a063
    return
}
fn func_116(r1: u64, r7: u64) {
    let base_ptr: ptr

    // @1a4ca
    // Store input value r7 into struct field at offset 104
    u64[r1 + 104] = r7
    // Calculate memory offset base for data structures
    base_ptr = r7 + 0x60000

    if (var_9 == 0) {
        // @1a4ef
    } else {
        // @1a4e1
        // Increment pointer if condition met
        r2 = base_ptr + 1
    }

    // @1a498
    // Retrieve value from struct field at offset 88
    r7 = u64[r1 + 88]
    return
}
fn func_117(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let shift_mask
    let target_offset
    let data_ptr

    // Check bounds against memory region starting at base 0x60000
    if (0xFEFD0000 >=u r1 - 368) {
        // @1bc3f
        shift_mask = 32 >>u (32 << u32[4 + r10 + 0x60000])

        // Verify index is within valid range [0, 13]
        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @1bcd3
            target_offset = 32 >>u (32 << (32 >>u (32 << (3 << shift_mask))) + ptr_0_56)
            data_ptr = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @1bda5
                // Perform memory read via calculated offset in region 0x60000
                target_offset->field_393216 = u64[32 >>u (32 << (32 >>u (32 << (3 << data_ptr))) + ptr_0_56) + 0x60000]
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0 // Clear word at offset 8
                goto block_1be42;
            } else {
                // @1bd9d
                data_ptr = 12
            }
        } else {
            // @1bccb
            shift_mask = 12
        }
    }

    block_1be42:
    // @1be42
    return
}
fn func_118(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bitmask_offset

    if (0xFEFD0000 >=u r1 - 416) {
        // Calculate shift for bitmask based on memory word at 0x60000 + r10
        let bit_shift = 32 >>u (32 << u32[r10 + 0x60000])

        if (u32[r10 + 0x60000] <=u 13 >=u 0) {
            let var_27 = 32
            let var_38 = 32

            // Check if specific bit at calculated memory address is set
            if (!(u64[var_38 >>u (var_38 << (var_27 >>u (var_27 << (3 << bit_shift))) + ptr_0_56) + 0x60000] <u 0)) {
                goto block_1c0cc;
            } else {
                goto block_1bfb9;
            }
        }
    }

    block_1bfb9:
    // Calculate memory offset using ptr_0_48 base
    bitmask_offset = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

    if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
        // Write value from offset 8 to calculated memory pointer
        u64[32 >>u (32 << (32 >>u (32 << (3 << bitmask_offset))) + ptr_0_56) + 0x60000] = u32[8 + ptr_0_48 + 0x60000]
        goto block_1c0cc;
    } else {
        bitmask_offset = 12
    }

    bitmask_offset = 12

    // Write value from offset 8 using fallback offset constant 12
    u64[32 >>u (32 << (32 >>u (32 << (3 << bitmask_offset))) + ptr_0_56) + 0x60000] = u32[8 + ptr_0_48 + 0x60000]

    block_1c0cc:
    // Clear fields at ptr_0_40
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0

    return
}
fn func_119(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let shift_val_secondary

    // Bounds check against address space offset 416
    if (0xFEFD0000 >=u r1 - 416) {
        // @1c108
        // Calculate bit-shift for memory alignment based on index at r10+0x60000
        let shift_val_primary = 32 >>u (32 << u32[r10 + 0x60000])

        if (u32[r10 + 0x60000] <=u 13 >=u 0) {
            // @1c18e
            let bit_mask_a = 32
            let bit_mask_b = 32

            // Validate data structure state before assignment
            if (u64[bit_mask_b >>u (bit_mask_b << (bit_mask_a >>u (bit_mask_a << (3 << shift_val_primary))) + ptr_0_56) + 0x60000] <u 0 <u 0) {
                // @1c362
                goto block_1c367;
            } else {
                // @1c237
                goto block_1c254;
            }
        }
    }

    block_1c254:
    // @1c254
    // Recalculate shift using offset ptr_0_48
    shift_val_secondary = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

    if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
        // @1c2d8
        // Store value from r10 based calculation into memory slot
        u64[32 >>u (32 << (32 >>u (32 << (3 << shift_val_secondary))) + ptr_0_56) + 0x60000] = u32[8 + ptr_0_48 + 0x60000]
        goto block_1c367;
    } else {
        // @1c2d0
        shift_val_secondary = 12
    }

    // @1c2d0
    shift_val_secondary = 12

    // @1c2d8
    // Fallback store operation
    u64[32 >>u (32 << (32 >>u (32 << (3 << shift_val_secondary))) + ptr_0_56) + 0x60000] = u32[8 + ptr_0_48 + 0x60000]

    block_1c367:
    // @1c367
    // Reset state pointers and memory headers
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0

    // @1c23c
    return
}
fn func_120(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let offset_304
    let target_node_ptr
    let offset_432

    // Check bounds for PVM memory access relative to r1
    if (0xFEFD0000 >=u r1 - 536) {
        // Calculate hash index from memory at offset 0x60000
        let hash_idx_root = 32 >>u (32 << u32[r10 + 0x60000])

        if (u32[r10 + 0x60000] <=u 13 >=u 0) {
            let shift_base = 32
            let shift_var = 32

            // Validate tree node existence via bitwise index calculation
            if (!(u64[shift_var >>u (shift_var << (shift_base >>u (shift_base << (3 << hash_idx_root))) + ptr_0_56) + 0x60000] <u 0)) {
                goto block_1c6d1;
            } else {
                goto block_1c4ed;
            }
        }
    }

    block_1c4ed:
    // Fallback logic for secondary node lookup
    offset_304 = 32 >>u (32 << u32[8 + ptr_0_48 + 0x60000])

    if (u32[8 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
        target_node_ptr = 32 >>u (32 << (32 >>u (32 << (3 << offset_304))) + ptr_0_56)
        offset_432 = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

        if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
            target_node_ptr->field_393216 = u64[32 >>u (32 << (32 >>u (32 << (3 << offset_432))) + ptr_0_56) + 0x60000]
            goto block_1c6d1;
        } else {
            offset_432 = 12
        }
    } else {
        offset_304 = 12
    }

    offset_304 = 12

    target_node_ptr = 32 >>u (32 << (32 >>u (32 << (3 << offset_304))) + ptr_0_56)
    offset_432 = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

    if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
        target_node_ptr->field_393216 = u64[32 >>u (32 << (32 >>u (32 << (3 << offset_432))) + ptr_0_56) + 0x60000]
        goto block_1c6d1;
    } else {
        offset_432 = 12
    }

    offset_432 = 12

    target_node_ptr->field_393216 = u64[32 >>u (32 << (32 >>u (32 << (3 << offset_432))) + ptr_0_56) + 0x60000]

    block_1c6d1:
    // Zero out result fields in PVM memory structure
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0

    return
}
fn func_121(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let base_ptr_304
    let base_ptr_360
    let base_ptr_432

    // Check bounds for memory address r1 relative to PVM heap/stack
    if (0xFEFD0000 >=u r1 - 536) {
        // @1c70d
        // Calculate shift amount based on memory access at global offset 0x60000
        let shift_val_128 = 32 >>u (32 << u32[r10 + 0x60000])

        if (u32[r10 + 0x60000] <=u 13 >=u 0) {
            // @1c793
            let const_32_var = 32
            let const_32_ref = 32

            // Validate memory range before access
            if (u64[const_32_ref >>u (const_32_ref << (const_32_var >>u (const_32_var << (3 << shift_val_128))) + ptr_0_56) + 0x60000] <u 0 <u 0) {
                // @1ca38
                goto block_1ca3d;
            } else {
                // @1c83c
                goto block_1c859;
            }
        }
    }

    block_1c859:
    // @1c859
    // Calculate page/block pointers using bitwise folding
    base_ptr_304 = 32 >>u (32 << u32[8 + ptr_0_48 + 0x60000])

    if (u32[8 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
        // @1c8dd
        base_ptr_360 = 32 >>u (32 << (32 >>u (32 << (3 << base_ptr_304))) + ptr_0_56)
        base_ptr_432 = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

        if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
            // @1c9bd
            base_ptr_360->field_393216 = u64[32 >>u (32 << (32 >>u (32 << (3 << base_ptr_432))) + ptr_0_56) + 0x60000]
            goto block_1ca3d;
        } else {
            // @1c9b5
            base_ptr_432 = 12
        }
    } else {
        // @1c8d5
        base_ptr_304 = 12
    }

    // @1c8d5 - Fallback assignment
    base_ptr_304 = 12

    // @1c8dd
    base_ptr_360 = 32 >>u (32 << (32 >>u (32 << (3 << base_ptr_304))) + ptr_0_56)
    base_ptr_432 = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

    if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
        // @1c9bd
        base_ptr_360->field_393216 = u64[32 >>u (32 << (32 >>u (32 << (3 << base_ptr_432))) + ptr_0_56) + 0x60000]
        goto block_1ca3d;
    } else {
        // @1c9b5
        base_ptr_432 = 12
    }

    // @1c9b5
    base_ptr_432 = 12

    // @1c9bd
    base_ptr_360->field_393216 = u64[32 >>u (32 << (32 >>u (32 << (3 << base_ptr_432))) + ptr_0_56) + 0x60000]

    block_1ca3d:
    // @1ca3d - Final state cleanup
    ptr_0_40->field_393216 = 0
    u32[8 + ptr_0_40 + 0x60000] = 0

    // @1c841
    return
}
fn func_122(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let shift_mask_a
    let target_ptr
    let shift_mask_b

    // Validate memory boundary r1-392 against global constant 0xFEFD0000
    if (0xFEFD0000 >=u r1 - 392) {
        // @1ca79
        // Extract shift amount from memory at r10 + 0x60004
        shift_mask_a = 32 >>u (32 << u32[4 + r10 + 0x60000])

        // Check if shift amount is within valid range [0, 13]
        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @1cb0d
            target_ptr = 32 >>u (32 << (32 >>u (32 << (3 << shift_mask_a))) + ptr_0_56)
            shift_mask_b = 32 >>u (32 << ptr_0_48->field_393216)

            // Validate secondary shift constraint
            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @1cbdf
                // Perform word-aligned memory access with calculated offset in memory segment 0x60000
                let bit_shift_val = u64[32 >>u (32 << (32 >>u (32 << (3 << shift_mask_b))) + ptr_0_56) + 0x60000]
                let bit_count = 63 & u32[8 + ptr_0_48 + 0x60000]
                
                // Rotate bits based on extracted configuration field
                target_ptr->field_393216 = bit_count >>u bit_shift_val | bit_count - 64 << bit_shift_val
                
                // Clear state flags
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_1ccbd;
            } else {
                // @1cbd7
                shift_mask_b = 12
            }
        } else {
            // @1cb05
            shift_mask_a = 12
        }
    }

    block_1ccbd:
    // @1ccbd
    return
}
fn func_123(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let shift_op_base
    let target_node_ptr
    let state_val_ptr
    let shift_amount

    if (0xFEFD0000 >=u r1 - 392) {
        // @1ccea
        // Normalize bit shift for PVM word-alignment
        shift_op_base = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @1cd7e
            // Calculate node pointer using shifting arithmetic
            target_node_ptr = 32 >>u (32 << (32 >>u (32 << (3 << shift_op_base))) + ptr_0_56)
            state_val_ptr = u32[8 + ptr_0_48 + 0x60000]
            shift_amount = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @1ce6c
                // Perform word-masked memory read and bit rotation
                let bit_rotated_val = 63 & u64[32 >>u (32 << (32 >>u (32 << (3 << shift_amount))) + ptr_0_56) + 0x60000]
                // Apply rotation bits based on shift and state pointers
                target_node_ptr->field_393216 = bit_rotated_val >>u state_val_ptr | bit_rotated_val - 64 << state_val_ptr
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_1cf2e;
            } else {
                // @1ce64
                shift_amount = 12
            }
        } else {
            // @1cd76
            shift_op_base = 12
        }
    }

    block_1cf2e:
    // @1cf2e
    return
}
fn func_124(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val_a
    let bit_shift_val_b
    let bit_shift_val_c

    // Bounds check on input r1 against memory offset 0xFEFD0000
    if (0xFEFD0000 >=u r1 - 448) {
        // @1cf5b: calculate initial shift value based on memory at r10 + 0x60000
        bit_shift_val_a = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @1cfef: update shifts using counter/index ptr_0_56
            bit_shift_val_b = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val_a))) + ptr_0_56)
            bit_shift_val_c = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @1d0c1: perform circular rotation/shift operations on buffer data
                let rotation_mask = 32 >>u (32 << u64[32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val_c))) + ptr_0_56) + 0x60000])
                let bit_packed_val = 31 & u32[8 + ptr_0_48 + 0x60000]
                // Apply bitwise rotation (rbit)
                bit_shift_val_b->field_393216 = 32 >>u (32 << (bit_packed_val >>u rotation_mask | bit_packed_val - 32 << rotation_mask))
                // Clear memory fields at current pointer
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_1d1f1;
            } else {
                // @1d0b9: default shift constant for out-of-bounds index
                bit_shift_val_c = 12
            }
        } else {
            // @1cfe7: default shift constant for out-of-bounds index
            bit_shift_val_a = 12
        }
    }

    block_1d1f1:
    // @1d1f1
    return
}
fn func_125(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_mask_shift_val
    let target_buffer_ptr
    let bit_offset_val
    let shift_width_val

    if (0xFEFD0000 >=u r1 - 448) {
        // @1d21e: Calculate initial shift based on memory-mapped input at r10+0x60004
        bit_mask_shift_val = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @1d2b2: Calculate pointers and offsets for bit manipulation
            target_buffer_ptr = 32 >>u (32 << (32 >>u (32 << (3 << bit_mask_shift_val))) + ptr_0_56)
            bit_offset_val = u32[8 + ptr_0_48 + 0x60000]
            shift_width_val = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @1d39e: Perform bitwise rotate/shift on memory block
                let extracted_bits = 31 & 32 >>u (32 << u64[32 >>u (32 << (32 >>u (32 << (3 << shift_width_val))) + ptr_0_56) + 0x60000])
                target_buffer_ptr->field_393216 = 32 >>u (32 << (extracted_bits >>u bit_offset_val | extracted_bits - 32 << bit_offset_val))
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_1d4b4;
            } else {
                // @1d396: Fallback for out-of-bounds shift width
                shift_width_val = 12
            }
        } else {
            // @1d2aa: Fallback for out-of-bounds memory input
            bit_mask_shift_val = 12
        }
    }

    block_1d4b4:
    // @1d4b4
    return
}
fn func_126(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let loop_shift_base
    let target_node_ptr
    let node_offset_idx
    let node_data_ptr
    let node_shifter_val

    if (0xFEFD0000 >=u r1 - 512) {
        // @1d4e1: Calculate initial shift base from memory at r10 + 0x60000 + 8
        loop_shift_base = 32 >>u (32 << u32[8 + r10 + 0x60000])

        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // @1d575: Calculate target node relative to ptr_0_56
            target_node_ptr = 32 >>u (32 << (32 >>u (32 << (3 << loop_shift_base))) + ptr_0_56)
            node_offset_idx = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

            if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                // @1d655: Fetch data from node at calculated address
                node_data_ptr = u64[32 >>u (32 << (32 >>u (32 << (3 << node_offset_idx))) + ptr_0_56) + 0x60000]
                node_shifter_val = 32 >>u (32 << ptr_0_48->field_393216)

                if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                    // @1d734: Perform rotational bitwise transformation on memory data
                    let rot_val = 63 & u64[32 >>u (32 << (32 >>u (32 << (3 << node_shifter_val))) + ptr_0_56) + 0x60000]
                    target_node_ptr->field_393216 = rot_val - 64 >>u node_data_ptr | rot_val << node_data_ptr
                    ptr_0_40->field_393216 = 0
                    u32[8 + ptr_0_40 + 0x60000] = 0
                    goto block_1d7f6;
                } else {
                    // @1d72c: Clamp node_shifter_val to 12
                    node_shifter_val = 12
                }
            } else {
                // @1d64d: Clamp node_offset_idx to 12
                node_offset_idx = 12
            }
        } else {
            // @1d56d: Clamp loop_shift_base to 12
            loop_shift_base = 12
        }
    }

    block_1d7f6:
    // @1d7f6
    return
}
fn func_127(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let shift_val_idx1
    let state_ptr_192
    let shift_val_idx2
    let state_ptr_344
    let shift_val_idx3

    // Check bounds: r1 is likely an offset into memory accessed via r10 base
    if (0xFEFD0000 >=u r1 - 584) {
        // @1d823: Calculate shift index from memory at offset 0x60000
        shift_val_idx1 = 32 >>u (32 << u32[8 + r10 + 0x60000])

        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // @1d8b7: Derive temporary states using bitwise rotations
            state_ptr_192 = 32 >>u (32 << (32 >>u (32 << (3 << shift_val_idx1))) + ptr_0_56)
            shift_val_idx2 = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

            if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                // @1d997: Update state pointers based on memory lookups
                state_ptr_344 = 32 >>u (32 << u64[32 >>u (32 << (32 >>u (32 << (3 << shift_val_idx2))) + ptr_0_56) + 0x60000])
                shift_val_idx3 = 32 >>u (32 << ptr_0_48->field_393216)

                if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                    // @1da91: Perform bitwise rotation/slicing on derived memory state
                    let bit_slice = 31 & 32 >>u (32 << u64[32 >>u (32 << (32 >>u (32 << (3 << shift_val_idx3))) + ptr_0_56) + 0x60000])
                    state_ptr_192->field_393216 = 32 >>u (32 << (bit_slice - 32 >>u state_ptr_344 | bit_slice << state_ptr_344))
                    ptr_0_40->field_393216 = 0
                    u32[8 + ptr_0_40 + 0x60000] = 0 // Reset memory control word
                    goto block_1dba7;
                } else {
                    // @1da89: Clamp index to bounds
                    shift_val_idx3 = 12
                }
            } else {
                // @1d98f: Clamp index to bounds
                shift_val_idx2 = 12
            }
        } else {
            // @1d8af: Clamp index to bounds
            shift_val_idx1 = 12
        }
    }

    block_1dba7:
    // @1dba7
    return
}
fn func_128(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_idx_const_1
    let state_ptr_1
    let bit_idx_const_2
    let lookup_val
    let bit_idx_const_3

    if (0xFEFD0000 >=u r1 - 512) {
        // Calculate bit shift index from memory offset 0x60008 relative to r10
        bit_idx_const_1 = 32 >>u (32 << u32[8 + r10 + 0x60000])

        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // Apply bitwise transformations on pointer offsets
            state_ptr_1 = 32 >>u (32 << (32 >>u (32 << (3 << bit_idx_const_1))) + ptr_0_56)
            bit_idx_const_2 = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

            if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                // Fetch 64-bit value from calculated memory location
                lookup_val = u64[32 >>u (32 << (32 >>u (32 << (3 << bit_idx_const_2))) + ptr_0_56) + 0x60000]
                bit_idx_const_3 = 32 >>u (32 << ptr_0_48->field_393216)

                if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                    // Extract byte and perform bit rotation
                    let rotated_val = 63 & u64[32 >>u (32 << (32 >>u (32 << (3 << bit_idx_const_3))) + ptr_0_56) + 0x60000]
                    state_ptr_1->field_393216 = rotated_val >>u lookup_val | rotated_val - 64 << lookup_val
                    ptr_0_40->field_393216 = 0
                    u32[8 + ptr_0_40 + 0x60000] = 0
                    goto block_1dee9;
                } else {
                    bit_idx_const_3 = 12
                }
            } else {
                bit_idx_const_2 = 12
            }
        } else {
            bit_idx_const_1 = 12
        }
    }

    block_1dee9:
    return
}
fn func_129(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val
    let target_node_ptr
    let offset_idx
    let data_val_a
    let data_val_b

    // Bounds check against heap/memory range
    if (0xFEFD0000 >=u r1 - 584) {
        // @1df16: Normalize input index from memory mapped region
        bit_shift_val = 32 >>u (32 << u32[8 + r10 + 0x60000])

        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // @1dfaa
            target_node_ptr = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val))) + ptr_0_56)
            offset_idx = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

            if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                // @1e08a: Load value using calculated shift offset
                data_val_a = 32 >>u (32 << u64[32 >>u (32 << (32 >>u (32 << (3 << offset_idx))) + ptr_0_56) + 0x60000])
                data_val_b = 32 >>u (32 << ptr_0_48->field_393216)

                if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                    // @1e184: Perform bitwise rotation/transform
                    let transformed_val = 31 & 32 >>u (32 << u64[32 >>u (32 << (32 >>u (32 << (3 << data_val_b))) + ptr_0_56) + 0x60000])
                    target_node_ptr->field_393216 = 32 >>u (32 << (transformed_val >>u data_val_a | transformed_val - 32 << data_val_a))
                    ptr_0_40->field_393216 = 0
                    u32[8 + ptr_0_40 + 0x60000] = 0
                    goto block_1e29a;
                } else {
                    // @1e17c: Fallback default index
                    data_val_b = 12
                }
            } else {
                // @1e082: Fallback default index
                offset_idx = 12
            }
        } else {
            // @1dfa2: Fallback default index
            bit_shift_val = 12
        }
    }

    block_1e29a:
    // @1e29a
    return
}
fn func_130(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_mask_shift
    let target_node_ptr
    let offset_shift

    // Check if input parameter r1 resides within the valid memory boundary
    if (0xFEFD0000 >=u r1 - 416) {
        // Calculate shift based on u32 word at specific memory offset + 0x60000
        bit_mask_shift = 32 >>u (32 << u32[4 + r10 + 0x60000])

        // Validate range of the derived index
        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // Compute pointers for target structure manipulation using bitwise arithmetic
            target_node_ptr = 32 >>u (32 << (32 >>u (32 << (3 << bit_mask_shift))) + ptr_0_56)
            offset_shift = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // Update target field based on comparison result between structured data fields
                target_node_ptr->field_393216 = 32 >>u (32 << (u32[8 + ptr_0_48 + 0x60000] <u u64[32 >>u (32 << (32 >>u (32 << (3 << offset_shift))) + ptr_0_56) + 0x60000]))
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_1e518;
            } else {
                // Fallback index assignment
                offset_shift = 12
            }
        } else {
            // Fallback shift value assignment
            bit_mask_shift = 12
        }
    }

    block_1e518:
    // Exit function after structural updates
    return
}
fn func_131(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val
    let target_struct_ptr
    let offset_idx

    // Check bounds for r1 against heap-based memory pointer
    if (0xFEFD0000 >=u r1 - 416) {
        // @1e545
        // Extract shift value from structured memory at r10 + 0x60000 base
        bit_shift_val = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @1e5d9
            // Calculate dereference pointers using nested bitwise normalization
            target_struct_ptr = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val))) + ptr_0_56)
            offset_idx = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @1e6ab
                // Update target field via signed comparison of memory-mapped registers
                target_struct_ptr->field_393216 = 32 >>u (32 << (u32[8 + ptr_0_48 + 0x60000] <s u64[32 >>u (32 << (32 >>u (32 << (3 << offset_idx))) + ptr_0_56) + 0x60000]))
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_1e796;
            } else {
                // @1e6a3
                offset_idx = 12
            }
        } else {
            // @1e5d1
            bit_shift_val = 12
        }
    }

    block_1e796:
    // @1e796
    return
}
fn func_132(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bitmask_shift_val
    let target_struct_ptr
    let index_val

    // Check if r1 (likely an offset) is within bounds of memory region starting at 0x60000
    if (0xFEFD0000 >=u r1 - 416) {
        // @1e7c3
        // Calculate shift factor based on value at memory offset + 0x60000
        bitmask_shift_val = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @1e857
            // Calculate struct pointer and lookup index using bit manipulation
            target_struct_ptr = 32 >>u (32 << (32 >>u (32 << (3 << bitmask_shift_val))) + ptr_0_56)
            index_val = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @1e929
                // Perform boolean comparison and store result into specific memory field
                target_struct_ptr->field_393216 = 32 >>u (32 << (u64[32 >>u (32 << (32 >>u (32 << (3 << index_val))) + ptr_0_56) + 0x60000] <u u32[8 + ptr_0_48 + 0x60000]))
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_1ea14;
            } else {
                // @1e921
                index_val = 12
            }
        } else {
            // @1e84f
            bitmask_shift_val = 12
        }
    }

    block_1ea14:
    // @1ea14
    return
}
fn func_133(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val_1
    let bit_shift_val_2
    let bit_shift_val_3

    // Check if input r1 is within bounds of memory region
    if (0xFEFD0000 >=u r1 - 416) {
        // @1ea41
        // Calculate shift based on u32 value at memory offset 0x60004 + r10
        bit_shift_val_1 = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @1ead5
            bit_shift_val_2 = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val_1))) + ptr_0_56)
            bit_shift_val_3 = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @1eba7
                // Perform comparison and update struct field via computed memory index
                bit_shift_val_2->field_393216 = 32 >>u (32 << (u64[32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val_3))) + ptr_0_56) + 0x60000] <s u32[8 + ptr_0_48 + 0x60000]))
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_1ec92;
            } else {
                // @1eb9f
                bit_shift_val_3 = 12
            }
        } else {
            // @1eacd
            bit_shift_val_1 = 12
        }
    }

    block_1ec92:
    // @1ec92
    return
}
fn func_134(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bitshift_val_a
    let bitshift_val_b
    let bitshift_val_c
    let target_mem_val
    let bitshift_val_d

    // Check bounds against memory offset 0xFEFD0000
    if (0xFEFD0000 >=u r1 - 536) {
        // @1ecbf: Extract bitfield from r10 memory area
        bitshift_val_a = 32 >>u (32 << u32[8 + r10 + 0x60000])

        // Ensure index is within valid range [0, 13]
        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // @1ed53: Calculate bitmask index using ptr_0_56 (shared global state)
            bitshift_val_b = 32 >>u (32 << (32 >>u (32 << (3 << bitshift_val_a))) + ptr_0_56)
            bitshift_val_c = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

            if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                // @1ee33: Load 64-bit value from computed memory address
                target_mem_val = u64[32 >>u (32 << (32 >>u (32 << (3 << bitshift_val_c))) + ptr_0_56) + 0x60000]
                bitshift_val_d = 32 >>u (32 << ptr_0_48->field_393216)

                if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                    // @1ef12: Compare and assign result to memory field
                    bitshift_val_b->field_393216 = 32 >>u (32 << (u64[32 >>u (32 << (32 >>u (32 << (3 << bitshift_val_d))) + ptr_0_56) + 0x60000] <u target_mem_val))
                    ptr_0_40->field_393216 = 0
                    u32[8 + ptr_0_40 + 0x60000] = 0
                    goto block_1efe1;
                } else {
                    // @1ef0a: Default case for invalid indices
                    bitshift_val_d = 12
                }
            } else {
                // @1ee2b: Default case for invalid indices
                bitshift_val_c = 12
            }
        } else {
            // @1ed4b: Default case for invalid indices
            bitshift_val_a = 12
        }
    }

    block_1efe1:
    // @1efe1: Function exit
    return
}
fn func_135(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_idx_a        // Variable for controlling bit shift logic
    let bit_shift_idx_b        // Intermediate storage for complex bitwise calculation
    let target_memory_val      // Holds value retrieved from memory for comparison
    let bit_shift_idx_c        // Index used for finalizing field update
    let bit_shift_idx_d        // Temporary storage for result of bitwise logic

    if (0xFEFD0000 >=u r1 - 536) {
        // @1f00e: Calculate initial bit shift index from memory offset
        bit_shift_idx_a = 32 >>u (32 << u32[8 + r10 + 0x60000])

        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // @1f0a2: Compute base pointer offset using barrel shifting trick
            bit_shift_idx_b = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_idx_a))) + ptr_0_56)
            bit_shift_idx_c = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

            if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                // @1f182: Load external value from memory based on calculated offset
                target_memory_val = u64[32 >>u (32 << (32 >>u (32 << (3 << bit_shift_idx_c))) + ptr_0_56) + 0x60000]
                bit_shift_idx_d = 32 >>u (32 << ptr_0_48->field_393216)

                if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                    // @1f261: Execute state update based on memory comparison result
                    bit_shift_idx_b->field_393216 = 32 >>u (32 << (u64[32 >>u (32 << (32 >>u (32 << (3 << bit_shift_idx_d))) + ptr_0_56) + 0x60000] <s target_memory_val))
                    ptr_0_40->field_393216 = 0
                    u32[8 + ptr_0_40 + 0x60000] = 0 // Reset status word at offset
                    goto block_1f330;
                } else {
                    // @1f259: Error path: set default flag
                    bit_shift_idx_d = 12
                }
            } else {
                // @1f17a: Error path: invalid shift index
                bit_shift_idx_c = 12
            }
        } else {
            // @1f09a: Error path: index out of bounds
            bit_shift_idx_a = 12
        }
    }

    block_1f330:
    // @1f330: Exit function without status
    return
}
fn func_136(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let scratch_pad_136
    let target_buffer_192
    let shift_index_256

    // Bounds check on input r1 relative to constant offset 448
    if (0xFEFD0000 >=u r1 - 448) {
        // @1f35d: Normalize U32 shift value from memory [r10 + 0x60004]
        scratch_pad_136 = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @1f3f1: Compute base index using register-buffered offsets
            target_buffer_192 = 32 >>u (32 << (32 >>u (32 << (3 << scratch_pad_136))) + ptr_0_56)
            shift_index_256 = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @1f4c3: Update field at target buffer with complex bit-shifted memory reference
                target_buffer_192->field_393216 = 32 >>u (32 << (u32[8 + ptr_0_48 + 0x60000] << (32 >>u (32 << u64[32 >>u (32 << (32 >>u (32 << (3 << shift_index_256))) + ptr_0_56) + 0x60000]))))
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_1f5dd;
            } else {
                // @1f4bb: Clamp shift index for out-of-bounds failure
                shift_index_256 = 12
            }
        } else {
            // @1f3e9: Clamp scratch pad for out-of-bounds failure
            scratch_pad_136 = 12
        }
    }

    block_1f5dd:
    // @1f5dd: Execution complete
    return
}
fn func_137(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val
    let target_ptr
    let offset_val

    // Check if r1 is within the offset memory range (r1 - 448 <= 0xFEFD0000)
    if (0xFEFD0000 >=u r1 - 448) {
        // @1f60a
        // Extract 32-bit value from memory region and calculate shift
        bit_shift_val = 32 >>u (32 << u32[4 + r10 + 0x60000])

        // Verify index range for look-up table at r10 offset
        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @1f69e
            target_ptr = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val))) + ptr_0_56)
            offset_val = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @1f770
                // Perform complex bitwise-based pointer arithmetic and memory update
                target_ptr->field_393216 = 32 >>u (32 << (u32[8 + ptr_0_48 + 0x60000] >>u (32 >>u (32 << u64[32 >>u (32 << (32 >>u (32 << (3 << offset_val))) + ptr_0_56) + 0x60000]))))
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_1f88a;
            } else {
                // @1f768
                offset_val = 12
            }
        } else {
            // @1f696
            bit_shift_val = 12
        }
    }

    block_1f88a:
    // @1f88a
    return
}
fn func_138(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val
    let target_ptr
    let offset_val

    // Check bounds against memory region relative to R1
    if (0xFEFD0000 >=u r1 - 448) {
        // Calculate bit shift based on stored preference at memory offset
        bit_shift_val = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // Compute pointer targeting memory based on operational parameters and bit_shift_val
            target_ptr = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val))) + ptr_0_56)
            offset_val = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // Perform complex bitwise extraction and store result in target memory field
                target_ptr->field_393216 = 32 >>u (32 << (32 >>u (32 << (31 & u32[8 + ptr_0_48 + 0x60000])))) >>s u64[32 >>u (32 << (32 >>u (32 << (3 << offset_val))) + ptr_0_56) + 0x60000]
                // Reset state variables
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_1fb3b;
            } else {
                // Set default offset if threshold exceeded
                offset_val = 12
            }
        } else {
            // Set default bit shift if threshold exceeded
            bit_shift_val = 12
        }
    }

    block_1fb3b:
    // Exit function
    return
}
fn func_139(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let shift_mask_val
    let offset_ptr
    let state_val
    let index_val

    if (0xFEFD0000 >=u r1 - 456) {
        // @1fb68: Extract bit shift factor from memory mapped registers
        shift_mask_val = 32 >>u (32 << u32[4 + r10 + 0x60000])

        // Condition checks if current state index is within bounds [0, 13]
        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @1fbfc: Calculate pointer offset using bitwise shift manipulation
            offset_ptr = 32 >>u (32 << (32 >>u (32 << (3 << shift_mask_val))) + ptr_0_56)
            state_val = u32[8 + ptr_0_48 + 0x60000]
            index_val = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @1fce8: Perform indirect memory write based on calculated index and state
                offset_ptr->field_393216 = 32 >>u (32 << (32 >>u (32 << (31 & u64[32 >>u (32 << (32 >>u (32 << (3 << index_val))) + ptr_0_56) + 0x60000])) << state_val))
                // Clear target structure fields at ptr_0_40
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_1fdf6;
            } else {
                // @1fce0: Default case index constant
                index_val = 12
            }
        } else {
            // @1fbf4: Default case shift constant
            shift_mask_val = 12
        }
    }

    block_1fdf6:
    // @1fdf6
    return
}
fn func_140(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let shift_op_base
    let shift_val_res
    let mem_offset_idx
    let bit_mask_op

    // Check if r1 is within valid range relative to memory offset
    if (0xFEFD0000 >=u r1 - 456) {
        // @1fe23
        // Calculate initial shift based on memory at r10 + 0x60004
        shift_op_base = 32 >>u (32 << u32[4 + r10 + 0x60000])

        // Verify bounds for the bit-shift operation
        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @1feb7
            // Calculate complex shift/mask value using local scratch references (ptr_0_56, ptr_0_48)
            shift_val_res = 32 >>u (32 << (32 >>u (32 << (3 << shift_op_base))) + ptr_0_56)
            mem_offset_idx = u32[8 + ptr_0_48 + 0x60000]
            bit_mask_op = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @1ffa3
                // Update memory fields for the target structure using bitwise masking and shifts
                shift_val_res->field_393216 = 32 >>u (32 << (32 >>u (32 << (31 & u64[32 >>u (32 << (32 >>u (32 << (3 << bit_mask_op))) + ptr_0_56) + 0x60000])) >>u mem_offset_idx))
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_200b1;
            } else {
                // @1ff9b
                bit_mask_op = 12
            }
        } else {
            // @1feaf
            shift_op_base = 12
        }
    }

    block_200b1:
    // @200b1
    return
}
fn func_141(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val
    let target_buffer
    let offset_val
    let mask_shift

    // Check bounds: r1 - 416 must be <= 0xFEFD0000
    if (0xFEFD0000 >=u r1 - 416) {
        // @200de: Calculate bit shift based on state in r10 + offset
        bit_shift_val = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @20172: Complex bitwise calculation for buffer pointer
            target_buffer = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val))) + ptr_0_56)
            offset_val = u32[8 + ptr_0_48 + 0x60000]
            mask_shift = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @20260: Apply bitwise mask and shift to update target field
                target_buffer->field_393216 = (31 & u64[32 >>u (32 << (32 >>u (32 << (3 << mask_shift))) + ptr_0_56) + 0x60000]) >>s offset_val
                // Reset associated state fields
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_2032e;
            } else {
                // @20258: Fallback index
                mask_shift = 12
            }
        } else {
            // @2016a: Fallback index
            bit_shift_val = 12
        }
    }

    block_2032e:
    // @2032e
    return
}
fn func_142(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let index_shift         // ptr_0_136
    let target_data_ptr     // ptr_0_192
    let bit_mask_val        // ptr_0_256

    // Verify bounds against memory window at 0xFEFD0000
    if (0xFEFD0000 >=u r1 - 432) {
        // Obfuscated shift sequence used to mask/calculate index from memory at offset 0x60004
        index_shift = 32 >>u (32 << u32[4 + r10 + 0x60000])

        // Verify index is within safe range [0, 13]
        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // Apply complex bitwise shift transformation using global state offsets
            target_data_ptr = 32 >>u (32 << (32 >>u (32 << (3 << index_shift))) + ptr_0_56)
            bit_mask_val = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // Perform memory write: mask and shift target field based on state
                target_data_ptr->field_393216 = 32 >>u (32 << (32 >>u (32 << (63 & u32[8 + ptr_0_48 + 0x60000])))) << u64[32 >>u (32 << (32 >>u (32 << (3 << bit_mask_val))) + ptr_0_56) + 0x60000]
                
                // Reset transaction counters
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_205cb;
            } else {
                index_shift = 12
                bit_mask_val = 12
            }
        } else {
            index_shift = 12
        }
    }

    block_205cb:
    // Exit function
    return
}
fn func_143(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let shift_mask_val
    let offset_target
    let bit_index_val

    if (0xFEFD0000 >=u r1 - 432) {
        // @205f8: Calculate initial shift mask based on memory at r10+0x60004
        shift_mask_val = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @2068c: Calculate target memory pointer using shifting logic
            offset_target = 32 >>u (32 << (32 >>u (32 << (3 << shift_mask_val))) + ptr_0_56)
            bit_index_val = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @2075e: Perform bitwise masking and memory update
                offset_target->field_393216 = 32 >>u (32 << (32 >>u (32 << (63 & u32[8 + ptr_0_48 + 0x60000])))) >>u u64[32 >>u (32 << (32 >>u (32 << (3 << bit_index_val))) + ptr_0_56) + 0x60000]
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_20868;
            } else {
                // @20756: Default fallback for bit index
                bit_index_val = 12
            }
        } else {
            // @20684: Default fallback for shift mask
            shift_mask_val = 12
        }
    }

    block_20868:
    // @20868
    return
}
fn func_144(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_mask_idx
    let buffer_ptr
    let shift_val

    if (0xFEFD0000 >=u r1 - 432) {
        // @20895: Normalize shift index from memory offset
        bit_mask_idx = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @20929: Calculate base pointers for data manipulation
            buffer_ptr = 32 >>u (32 << (32 >>u (32 << (3 << bit_mask_idx))) + ptr_0_56)
            shift_val = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @209fb: Update memory structure field using bitwise overflow protection
                buffer_ptr->field_393216 = 32 >>u (32 << (32 >>u (32 << (63 & u32[8 + ptr_0_48 + 0x60000])))) >>s u64[32 >>u (32 << (32 >>u (32 << (3 << shift_val))) + ptr_0_56) + 0x60000]
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_20b05;
            } else {
                // @209f3: Fallback state
                shift_val = 12
            }
        } else {
            // @20921: Error condition handling for index out of bounds
            bit_mask_idx = 12
        }
    }

    block_20b05:
    // @20b05: Exit procedure
    return
}
fn func_145(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val
    let target_mem_ptr
    let stored_offset_val
    let normalized_shift

    // Validate memory bounds against input parameter r1
    if (0xFEFD0000 >=u r1 - 392) {
        // @20b32 - Calculate initial bit mask using offset from r10 based memory buffer
        bit_shift_val = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @20bc6 - Calculate memory destination addresses and shift parameters
            target_mem_ptr = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val))) + ptr_0_56)
            stored_offset_val = u32[8 + ptr_0_48 + 0x60000]
            normalized_shift = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @20cb4 - Update memory field with bit-shifted value from computed offset
                target_mem_ptr->field_393216 = u64[32 >>u (32 << (32 >>u (32 << (3 << normalized_shift))) + ptr_0_56) + 0x60000] << stored_offset_val
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_20d60;
            } else {
                // @20cac - Hardcoded fallback shift
                normalized_shift = 12
            }
        } else {
            // @20bbe - Hardcoded fallback shift
            bit_shift_val = 12
        }
    }

    block_20d60:
    // @20d60 - Function exit
    return
}
fn func_146(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val
    let target_mem_ptr
    let offset_val
    let bit_mask_len

    if (0xFEFD0000 >=u r1 - 392) {
        // @20d8d: Initialize shift amount from memory at r10 + 0x60004
        bit_shift_val = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // @20e21: Calculate pointer and offset for bit manipulation
            target_mem_ptr = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val))) + ptr_0_56)
            offset_val = u32[8 + ptr_0_48 + 0x60000]
            bit_mask_len = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // @20f0f: Perform bitwise shift and mask operation on memory
                target_mem_ptr->field_393216 = u64[32 >>u (32 << (32 >>u (32 << (3 << bit_mask_len))) + ptr_0_56) + 0x60000] >>u offset_val
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_20fbb;
            } else {
                // @20f07: Clamp mask length to 12
                bit_mask_len = 12
            }
        } else {
            // @20e19: Clamp shift value to 12
            bit_shift_val = 12
        }
    }

    block_20fbb:
    // @20fbb
    return
}
fn func_147(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let shift_op_1
    let target_mem_ptr
    let offset_val
    let shift_op_2

    if (0xFEFD0000 >=u r1 - 408) {
        // Calculate shift based on stack/memory state at 0x60000 base
        shift_op_1 = 32 >>u (32 << u32[4 + r10 + 0x60000])

        if (u32[4 + r10 + 0x60000] <=u 13 >=u 0) {
            // Memory address calculation involving ptr_0_56 (likely a base register)
            target_mem_ptr = 32 >>u (32 << (32 >>u (32 << (3 << shift_op_1))) + ptr_0_56)
            offset_val = u32[8 + ptr_0_48 + 0x60000]
            shift_op_2 = 32 >>u (32 << ptr_0_48->field_393216)

            if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                // Perform bitwise shift on target field using internal memory read
                target_mem_ptr->field_393216 = u64[32 >>u (32 << (32 >>u (32 << (3 << shift_op_2))) + ptr_0_56) + 0x60000] >>s offset_val
                ptr_0_40->field_393216 = 0
                u32[8 + ptr_0_40 + 0x60000] = 0
                goto block_2122a;
            } else {
                // Clamp shift_op_2 to 12 if out of expected range
                shift_op_2 = 12
            }
        } else {
            // Clamp shift_op_1 to 12 if out of expected range
            shift_op_1 = 12
        }
    }

    block_2122a:
    // Exit function
    return
}
fn func_148(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let loop_shift_val
    let data_entry_idx
    let data_offset_idx
    let buffer_val_ptr
    let state_shift_val

    if (0xFEFD0000 >=u r1 - 592) {
        // @21257: Calculate initial shift based on memory input
        loop_shift_val = 32 >>u (32 << u32[8 + r10 + 0x60000])

        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // @212eb: Compute intermediate shift for data indexing
            data_entry_idx = 32 >>u (32 << (32 >>u (32 << (3 << loop_shift_val))) + ptr_0_56)
            data_offset_idx = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

            if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                // @213cb: Retrieve buffer pointer using calculated offset
                buffer_val_ptr = 32 >>u (32 << u64[32 >>u (32 << (32 >>u (32 << (3 << data_offset_idx))) + ptr_0_56) + 0x60000])
                state_shift_val = 32 >>u (32 << ptr_0_48->field_393216)

                if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                    // @214c5: Apply bitwise mutation to state field
                    data_entry_idx->field_393216 = 32 >>u (32 << (32 >>u (32 << (31 & u64[32 >>u (32 << (32 >>u (32 << (3 << state_shift_val))) + ptr_0_56) + 0x60000])) << buffer_val_ptr))
                    ptr_0_40->field_393216 = 0
                    u32[8 + ptr_0_40 + 0x60000] = 0
                    goto block_215d3;
                } else {
                    // @214bd: Reset shift if bounds exceeded
                    state_shift_val = 12
                }
            } else {
                // @213c3: Reset shift if bounds exceeded
                data_offset_idx = 12
            }
        } else {
            // @212e3: Reset shift if bounds exceeded
            loop_shift_val = 12
        }
    }

    block_215d3:
    // @215d3
    return
}
fn func_149(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let shift_val_a
    let bitmask_res_b
    let shift_val_c
    let bitmask_res_d
    let shift_val_e

    // Bounds check relative to memory segment 0xFEFD0000
    if (0xFEFD0000 >=u r1 - 592) {
        // @21600: Load and normalize shift factor from heap at 0x60008
        shift_val_a = 32 >>u (32 << u32[8 + r10 + 0x60000])

        // Verify shift factor is within allowed range [0, 13]
        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // @21694: Calculate intermediate shift bases
            bitmask_res_b = 32 >>u (32 << (32 >>u (32 << (3 << shift_val_a))) + ptr_0_56)
            shift_val_c = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

            if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                // @21774: Access heap memory using calculated offsets
                bitmask_res_d = 32 >>u (32 << u64[32 >>u (32 << (32 >>u (32 << (3 << shift_val_c))) + ptr_0_56) + 0x60000])
                shift_val_e = 32 >>u (32 << ptr_0_48->field_393216)

                if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                    // @2186e: Apply bitwise mutation to field_393216 (offset 0x60000)
                    bitmask_res_b->field_393216 = 32 >>u (32 << (32 >>u (32 << (31 & u64[32 >>u (32 << (32 >>u (32 << (3 << shift_val_e))) + ptr_0_56) + 0x60000])) >>u bitmask_res_d)))
                    ptr_0_40->field_393216 = 0
                    u32[8 + ptr_0_40 + 0x60000] = 0
                    goto block_2197c;
                } else {
                    // @21866: Default error/invalid state flag
                    shift_val_e = 12
                }
            } else {
                // @2176c: Default error/invalid state flag
                shift_val_c = 12
            }
        } else {
            // @2168c: Default error/invalid state flag
            shift_val_a = 12
        }
    }

    block_2197c:
    // @2197c
    return
}
fn func_150(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let offset_shift_a
    let offset_shift_b
    let bit_mask_idx
    let data_val
    let offset_shift_c

    // Validate bounds: r1 - 552 must be within the heap memory range 0xFEFD0000
    if (0xFEFD0000 >=u r1 - 552) {
        // @219a9
        // Extract shift value from heap offset 0x60008, normalize to 32-bit range
        offset_shift_a = 32 >>u (32 << u32[8 + r10 + 0x60000])

        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // @21a3d
            offset_shift_b = 32 >>u (32 << (32 >>u (32 << (3 << offset_shift_a))) + ptr_0_56)
            bit_mask_idx = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

            if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                // @21b1d
                // Fetch 64-bit value from calculated heap address
                data_val = u64[32 >>u (32 << (32 >>u (32 << (3 << bit_mask_idx))) + ptr_0_56) + 0x60000]
                offset_shift_c = 32 >>u (32 << ptr_0_48->field_393216)

                if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                    // @21c10
                    // Apply masking (31 & val) and bitwise shift based on retrieved data_val
                    offset_shift_b->field_393216 = (31 & u64[32 >>u (32 << (32 >>u (32 << (3 << offset_shift_c))) + ptr_0_56) + 0x60000]) >>s data_val
                    ptr_0_40->field_393216 = 0
                    u32[8 + ptr_0_40 + 0x60000] = 0
                    goto block_21cde;
                } else {
                    // @21c08
                    offset_shift_c = 12
                }
            } else {
                // @21b15
                bit_mask_idx = 12
            }
        } else {
            // @21a35
            offset_shift_a = 12
        }
    }

    block_21cde:
    // @21cde
    return
}
fn func_151(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_idx_a
    let mem_offset_b
    let mem_offset_c
    let target_val
    let bit_shift_idx_d

    // Validate memory bounds for r1 relative to 0xFEFD0000 limit
    if (0xFEFD0000 >=u r1 - 512) {
        // @21d0b
        bit_shift_idx_a = 32 >>u (32 << u32[8 + r10 + 0x60000])

        // Ensure index is within range [0, 13]
        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // @21d9f
            mem_offset_b = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_idx_a))) + ptr_0_56)
            mem_offset_c = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

            if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                // @21e7f
                // Fetch 64-bit value using calculated memory offset
                target_val = u64[32 >>u (32 << (32 >>u (32 << (3 << mem_offset_c))) + ptr_0_56) + 0x60000]
                bit_shift_idx_d = 32 >>u (32 << ptr_0_48->field_393216)

                if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                    // @21f5e
                    // Perform bitwise shift and store result in structure field
                    mem_offset_b->field_393216 = u64[32 >>u (32 << (32 >>u (32 << (3 << bit_shift_idx_d))) + ptr_0_56) + 0x60000] << target_val
                    ptr_0_40->field_393216 = 0
                    u32[8 + ptr_0_40 + 0x60000] = 0
                    goto block_2200a;
                } else {
                    // @21f56: Fallback index
                    bit_shift_idx_d = 12
                }
            } else {
                // @21e77: Fallback index
                mem_offset_c = 12
            }
        } else {
            // @21d97: Fallback index
            bit_shift_idx_a = 12
        }
    }

    block_2200a:
    // @2200a
    return
}
fn func_152(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val_a
    let bit_shift_val_b
    let bit_shift_val_c
    let data_offset_idx
    let bit_shift_val_d

    // Validate bounds for stack pointer calculation
    if (0xFEFD0000 >=u r1 - 512) {
        // @22037
        bit_shift_val_a = 32 >>u (32 << u32[8 + r10 + 0x60000]) // Extract shift factor from memory

        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // @220cb
            bit_shift_val_b = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val_a))) + ptr_0_56)
            bit_shift_val_c = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

            if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                // @221ab
                data_offset_idx = u64[32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val_c))) + ptr_0_56) + 0x60000]
                bit_shift_val_d = 32 >>u (32 << ptr_0_48->field_393216)

                if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                    // @2228a
                    // Perform bit-shifted store from calculated memory offset
                    bit_shift_val_b->field_393216 = u64[32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val_d))) + ptr_0_56) + 0x60000] >>u data_offset_idx
                    ptr_0_40->field_393216 = 0
                    u32[8 + ptr_0_40 + 0x60000] = 0 // Clear status word at memory offset
                    goto block_22336;
                } else {
                    // @22282
                    bit_shift_val_d = 12
                }
            } else {
                // @221a3
                bit_shift_val_c = 12
            }
        } else {
            // @220c3
            bit_shift_val_a = 12
        }
    }

    block_22336:
    // @22336
    return
}
fn func_153(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    let bit_shift_val_1
    let bit_shift_val_2
    let lookup_idx
    let mem_offset_val
    let bit_shift_val_3

    // Check if r1 - 512 is within the valid memory boundary 0xFEFD0000
    if (0xFEFD0000 >=u r1 - 512) {
        // @22363
        // Calculate shift amount from memory at r10 + 0x60008
        bit_shift_val_1 = 32 >>u (32 << u32[8 + r10 + 0x60000])

        if (u32[8 + r10 + 0x60000] <=u 13 >=u 0) {
            // @223f7
            // Complex bitwise logic to calculate offset for ptr_0_192
            bit_shift_val_2 = 32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val_1))) + ptr_0_56)
            lookup_idx = 32 >>u (32 << u32[4 + ptr_0_48 + 0x60000])

            if (u32[4 + ptr_0_48 + 0x60000] <=u 13 >=u 0) {
                // @224d7
                // Retrieve u64 value from calculated memory location
                mem_offset_val = u64[32 >>u (32 << (32 >>u (32 << (3 << lookup_idx))) + ptr_0_56) + 0x60000]
                bit_shift_val_3 = 32 >>u (32 << ptr_0_48->field_393216)

                if (ptr_0_48->field_393216 <=u 13 >=u 0) {
                    // @225b6
                    // Perform bitwise right shift calculation and update field
                    bit_shift_val_2->field_393216 = u64[32 >>u (32 << (32 >>u (32 << (3 << bit_shift_val_3))) + ptr_0_56) + 0x60000] >>s mem_offset_val
                    ptr_0_40->field_393216 = 0
                    u32[8 + ptr_0_40 + 0x60000] = 0
                    goto block_22662;
                } else {
                    // @225ae
                    bit_shift_val_3 = 12
                }
            } else {
                // @224cf
                lookup_idx = 12
            }
        } else {
            // @223ef
            bit_shift_val_1 = 12
        }
    }

    block_22662:
    // @22662
    return
}
fn func_154(r1: u64) {
    // @226fe
    // Fetch a byte from a memory-mapped lookup table and store it in status slots
    let status_byte = u8[u64[r1 + 48] + 0x60000]
    u64[r1 + 104] = status_byte
    u64[r1 + 112] = status_byte
    let is_negative = status_byte <u 0
    u64[r1 + 120] = is_negative

    if (status_byte <u 0) {
        // @22749
        goto block_2274e;
    } else {
        // @2272b
    }

    // @22744

    block_22730:
    // @22730
    r9 = u64[r1 + 8]
    return

    block_2274e:
    // @2274e
    // Calculate memory offset and load parameters
    let entry_ptr = 4 + u64[r1 + 88]
    u64[r1 + 128] = entry_ptr
    let entry_val = u32[entry_ptr + 0x60000]
    u64[r1 + 136] = entry_val
    let data_offset = u64[r1 + 72]
    u64[r1 + 144] = data_offset
    let data_mask = 255 & data_offset
    u64[r1 + 152] = data_mask
    // Masking operation via bitwise shifting
    let bit_mask_1 = 32 >>u (32 << data_mask)
    u64[r1 + 160] = bit_mask_1
    u64[r1 + 168] = entry_val
    u64[r1 + 176] = bit_mask_1
    let combined_offset = bit_mask_1 + entry_val
    u64[r1 + 184] = combined_offset
    let bit_mask_2 = 32 >>u (32 << combined_offset)
    u64[r1 + 192] = bit_mask_2
    u64[r1 + 200] = bit_mask_2
    let is_within_limit = bit_mask_2 <u 0x1000
    u64[r1 + 208] = is_within_limit
    u64[r1 + 216] = is_within_limit
    u64[r1 + 224] = is_within_limit
    let is_invalid = is_within_limit <u 0
    u64[r1 + 232] = is_invalid
    u64[r1 + 240] = bit_mask_2

    if (is_within_limit >=u 0) {
        // @2281d
        goto block_22822;
    } else {
        // @22814
        u64[r1 + 240] = 0x1000
    }

    // @22814
    u64[r1 + 240] = 0x1000

    // @2281d

    block_22822:
    while (cond_3 >=u 0) {
        // @22822
        let loop_idx_1 = u64[r1 + 256]
        u64[r1 + 264] = loop_idx_1
        let limit_1 = u64[r1 + 240]
        u64[r1 + 272] = limit_1
        let cond_3 = limit_1 <u loop_idx_1
        u64[r1 + 280] = cond_3
        u64[r1 + 288] = cond_3
        u64[r1 + 296] = cond_3
        let cond_4 = cond_3 <u 0
        u64[r1 + 304] = cond_4

        // @2287d
        let base_ptr = u32[u64[r1 + 88] + 0x60000]
        u64[r1 + 312] = base_ptr
        let struct_ptr = 4 + base_ptr
        u64[r1 + 320] = struct_ptr
        let offset_val = u32[struct_ptr + 0x60000]
        u64[r1 + 328] = offset_val
        let current_ptr = u64[r1 + 256]
        u64[r1 + 336] = current_ptr
        u64[r1 + 344] = offset_val
        let total_addr = offset_val + current_ptr
        u64[r1 + 352] = total_addr
        let masked_addr = 32 >>u (32 << total_addr)
        u64[r1 + 360] = masked_addr
        // Write byte to memory-mapped region
        u8[masked_addr + 0x60000] = u64[r1 + 248]
        u64[r1 + 376] = 8 >>u u64[r1 + 248]
        let next_idx = u64[r1 + 256]
        u64[r1 + 384] = next_idx
        let incremented_idx = 1 + next_idx
        u64[r1 + 392] = incremented_idx
        u64[r1 + 400] = 32 >>u (32 << incremented_idx)
    }

    // @22921

    // @22926

    // @22878

    while (cond_5 >=u 0) {
        // @2292b
        let entry_ptr_2 = 12 + u64[r1 + 88]
        u64[r1 + 424] = entry_ptr_2
        let loop_limit_2 = u32[entry_ptr_2 + 0x60000]
        u64[r1 + 432] = loop_limit_2
        let current_val = u64[r1 + 416]
        u64[r1 + 440] = current_val
        u64[r1 + 448] = loop_limit_2
        let cond_5 = loop_limit_2 <u current_val
        u64[r1 + 456] = cond_5
        u64[r1 + 464] = cond_5
        u64[r1 + 472] = cond_5
        let cond_6 = cond_5 <u 0
        u64[r1 + 480] = cond_6

        // @229a0
        let base_ptr_2 = 8 + u64[r1 + 88]
        u64[r1 + 488] = base_ptr_2
        let offset_ptr_2 = u32[base_ptr_2 + 0x60000]
        u64[r1 + 496] = offset_ptr_2
        let final_ptr = 4 + offset_ptr_2
        u64[r1 + 504] = final_ptr
        let final_offset = u32[final_ptr + 0x60000]
        u64[r1 + 512] = final_offset
        let loop_idx_2 = u64[r1 + 416]
        u64[r1 + 520] = loop_idx_2
        u64[r1 + 528] = final_offset
        let addr_calc = final_offset + loop_idx_2
        u64[r1 + 536] = addr_calc
        let masked_addr_2 = 32 >>u (32 << addr_calc)
        u64[r1 + 544] = masked_addr_2
        // Write byte to memory-mapped region
        u8[masked_addr_2 + 0x60000] = u64[r1 + 408]
        u64[r1 + 560] = 8 >>u u64[r1 + 408]
        let next_idx_2 = u64[r1 + 416]
        u64[r1 + 568] = next_idx_2
        let inc_idx_2 = 1 + next_idx_2
        u64[r1 + 576] = inc_idx_2
        u64[r1 + 584] = 32 >>u (32 << inc_idx_2)
    }

    // @22a52

    // @22a57

    // @2299b
    goto block_22730;
}
fn func_155(r1: u64) {
    // @22b20
    let data_ptr = u32[36] // Entry point to metadata table
    u64[r1 + 152] = data_ptr
    let flags = u8[data_ptr + 0x60000] // Load status/flags from memory mapped region
    u64[r1 + 160] = flags
    u64[r1 + 168] = flags
    let cond_0 = flags <u 0 // Always false for u8
    u64[r1 + 176] = cond_0

    if (flags <u 0) {
        // @22c2b
        goto block_22c30;
    } else {
        // @22b5f
    }

    // @22b7c
    u32[u64[r1 + 40] + 0x60000] = 3 // Set operation status to 3
    let next_ptr = 1 + u64[r1 + 152]
    u64[r1 + 200] = next_ptr
    let next_flags = u8[next_ptr + 0x60000]
    u64[r1 + 208] = next_flags
    u64[r1 + 216] = next_flags
    let cond_1 = next_flags <u 0
    u64[r1 + 224] = cond_1
    u64[r1 + 232] = 1 // Init state update flag

    if (next_flags >=u 0) {
        // @22bd7
        let status_field_ptr = 12 + u64[r1 + 40]
        u64[r1 + 240] = status_field_ptr
        u32[status_field_ptr + 0x60000] = u64[r1 + 232]
        let item_ptr = 4 + u64[r1 + 152]
        u64[r1 + 256] = item_ptr
        let item_val = u32[item_ptr + 0x60000]
        u64[r1 + 264] = item_val
        let target_field_ptr = 16 + u64[r1 + 40]
        u64[r1 + 272] = target_field_ptr
        u32[target_field_ptr + 0x60000] = item_val
        goto block_22c30;
    } else {
        // @22bcf
        u64[r1 + 232] = 2 // Update state code
    }

    // @22bcf
    u64[r1 + 232] = 2

    // @22bd7
    var_32 = 12 + u64[r1 + 40]
    u64[r1 + 240] = var_32
    u32[var_32 + 0x60000] = u64[r1 + 232]
    var_36 = 4 + u64[r1 + 152]
    u64[r1 + 256] = var_36
    var_37 = u32[var_36 + 0x60000]
    u64[r1 + 264] = var_37
    var_40 = 16 + u64[r1 + 40]
    u64[r1 + 272] = var_40
    u32[var_40 + 0x60000] = var_37

    block_22c30:
    // @22c30

    // @22b64
    r7 = u64[r1 + 184] // Return value captured from context
    return
}
fn func_156(r1: u64) {
    // @22cfa
    let config_ptr = u32[36] // Load configuration pointer from static address 36
    u64[r1 + 152] = config_ptr
    let flags_byte = u8[config_ptr + 0x60000] // Read flag at memory offset 0x60000
    u64[r1 + 160] = flags_byte
    u64[r1 + 168] = flags_byte
    let cond_0 = flags_byte <u 0 // Always false for u8
    u64[r1 + 176] = cond_0

    if (flags_byte <u 0) {
        // @22e05
        goto block_22e0a;
    } else {
        // @22d39
    }

    // @22d56
    u32[u64[r1 + 40] + 0x60000] = 3 // Set status_code to 3 at runtime base + offset
    let next_ptr = 1 + u64[r1 + 152]
    u64[r1 + 200] = next_ptr
    let next_flags = u8[next_ptr + 0x60000]
    u64[r1 + 208] = next_flags
    u64[r1 + 216] = next_flags
    let cond_1 = next_flags <u 0 // Always false for u8
    u64[r1 + 224] = cond_1
    u64[r1 + 232] = 1 // Initialize state_var to 1

    if (next_flags >=u 0) {
        // @22db1
        let target_reg_1 = 12 + u64[r1 + 40]
        u64[r1 + 240] = target_reg_1
        u32[target_reg_1 + 0x60000] = u64[r1 + 232]
        let data_ptr = 4 + u64[r1 + 152]
        u64[r1 + 256] = data_ptr
        let data_val = u32[data_ptr + 0x60000]
        u64[r1 + 264] = data_val
        let target_reg_2 = 16 + u64[r1 + 40]
        u64[r1 + 272] = target_reg_2
        u32[target_reg_2 + 0x60000] = data_val
        goto block_22e0a;
    } else {
        // @22da9
        u64[r1 + 232] = 2
    }

    // @22da9
    u64[r1 + 232] = 2

    // @22db1
    target_reg_1 = 12 + u64[r1 + 40]
    u64[r1 + 240] = target_reg_1
    u32[target_reg_1 + 0x60000] = u64[r1 + 232]
    data_ptr = 4 + u64[r1 + 152]
    u64[r1 + 256] = data_ptr
    data_val = u32[data_ptr + 0x60000]
    u64[r1 + 264] = data_val
    target_reg_2 = 16 + u64[r1 + 40]
    u64[r1 + 272] = target_reg_2
    u32[target_reg_2 + 0x60000] = data_val

    block_22e0a:
    // @22e0a

    // @22d3e
    r7 = u64[r1 + 184]
    return
}
fn func_157(r1: u64) {
    // @22e94
    let current_base_ptr = u32[36] // Load base pointer from internal storage
    u64[r1 + 112] = current_base_ptr
    let flags_byte = u8[current_base_ptr + 0x60000] // Read flag at offset from PVM state
    u64[r1 + 120] = flags_byte
    u64[r1 + 128] = flags_byte
    let cond_0 = flags_byte <u 0 // Check if byte is negative (unsigned check technically always false)
    u64[r1 + 136] = cond_0

    if (flags_byte <u 0) {
        // @22f99
        goto block_22f9e;
    } else {
        // @22ecf
    }

    // @22eec
    u32[u64[r1 + 40] + 0x60000] = 3 // Set status descriptor to 3
    let next_ptr = 1 + u64[r1 + 112] // Pointer arithmetic for next structure element
    u64[r1 + 160] = next_ptr
    let next_flags = u8[next_ptr + 0x60000]
    u64[r1 + 168] = next_flags
    u64[r1 + 176] = next_flags
    let cond_1 = next_flags <u 0
    u64[r1 + 184] = cond_1
    u64[r1 + 192] = 1 // Initialize iteration flag

    if (next_flags >=u 0) {
        // @22f46
        let dest_ptr = 12 + u64[r1 + 40]
        u64[r1 + 200] = dest_ptr
        u32[dest_ptr + 0x60000] = u64[r1 + 192]
        let src_ptr = 4 + u64[r1 + 112]
        u64[r1 + 216] = src_ptr
        let val_temp = u32[src_ptr + 0x60000]
        u64[r1 + 224] = val_temp
        let target_ptr = 16 + u64[r1 + 40]
        u64[r1 + 232] = target_ptr
        u32[target_ptr + 0x60000] = val_temp
        goto block_22f9e;
    } else {
        // @22f3e
        u64[r1 + 192] = 2
    }

    // @22f3e
    u64[r1 + 192] = 2 // Fallthrough to update flag when condition is negative

    // @22f46
    let var_32 = 12 + u64[r1 + 40]
    u64[r1 + 200] = var_32
    u32[var_32 + 0x60000] = u64[r1 + 192]
    let var_36 = 4 + u64[r1 + 112]
    u64[r1 + 216] = var_36
    let var_37 = u32[var_36 + 0x60000]
    u64[r1 + 224] = var_37
    let var_40 = 16 + u64[r1 + 40]
    u64[r1 + 232] = var_40
    u32[var_40 + 0x60000] = var_37

    block_22f9e:
    // @22f9e

    // @22ed4
    r7 = u64[r1 + 144] // Return value from stack/context
    return
}
fn func_158(r1: u64) {
    // @2302a
    let base_addr = u32[36] // Load base pointer/offset from memory index 36
    u64[r1 + 112] = base_addr
    let byte_val_0 = u8[base_addr + 0x60000] // Read byte at base offset + 0x60000 range
    u64[r1 + 120] = byte_val_0
    u64[r1 + 128] = byte_val_0
    let cond_0 = byte_val_0 <u 0 // Always false for unsigned 8-bit
    u64[r1 + 136] = cond_0

    if (byte_val_0 <u 0) {
        // @2312f
        goto block_23134;
    } else {
        // @23065
    }

    // @23082
    u32[u64[r1 + 40] + 0x60000] = 3 // Set status/flag to 3
    let next_ptr = 1 + u64[r1 + 112]
    u64[r1 + 160] = next_ptr
    let byte_val_1 = u8[next_ptr + 0x60000]
    u64[r1 + 168] = byte_val_1
    u64[r1 + 176] = byte_val_1
    let cond_1 = byte_val_1 <u 0
    u64[r1 + 184] = cond_1
    u64[r1 + 192] = 1

    if (byte_val_1 >=u 0) {
        // @230dc
        let target_ptr = 12 + u64[r1 + 40]
        u64[r1 + 200] = target_ptr
        u32[target_ptr + 0x60000] = u64[r1 + 192]
        let data_offset = 4 + u64[r1 + 112]
        u64[r1 + 216] = data_offset
        let data_val = u32[data_offset + 0x60000]
        u64[r1 + 224] = data_val
        let dest_ptr = 16 + u64[r1 + 40]
        u64[r1 + 232] = dest_ptr
        u32[dest_ptr + 0x60000] = data_val
        goto block_23134;
    } else {
        // @230d4
        u64[r1 + 192] = 2
    }

    // @230d4
    u64[r1 + 192] = 2

    // @230dc
    var_32 = 12 + u64[r1 + 40]
    u64[r1 + 200] = var_32
    u32[var_32 + 0x60000] = u64[r1 + 192]
    var_36 = 4 + u64[r1 + 112]
    u64[r1 + 216] = var_36
    var_37 = u32[var_36 + 0x60000]
    u64[r1 + 224] = var_37
    var_40 = 16 + u64[r1 + 40]
    u64[r1 + 232] = var_40
    u32[var_40 + 0x60000] = var_37

    block_23134:
    // @23134

    // @2306a
    r7 = u64[r1 + 144]
    return
}
fn func_159(r1: u64) {
    // @23299
    let base_addr = u32[36] // Load base address from static memory index 36
    u64[r1 + 240] = base_addr
    let val_at_base = u8[base_addr + 0x60000] // Read byte from interpreted heap offset
    u64[r1 + 248] = val_at_base
    u64[r1 + 256] = val_at_base
    let cond_0 = val_at_base <u 0 // Always false for u8
    u64[r1 + 264] = cond_0

    if (val_at_base <u 0) {
        // @233a4
        goto block_233a9;
    } else {
        // @232d8
    }

    // @232f5
    u32[u64[r1 + 40] + 0x60000] = 3 // Set status/type flag
    let next_addr = 1 + u64[r1 + 240] // Pointer increment
    u64[r1 + 288] = next_addr
    let next_val = u8[next_addr + 0x60000]
    u64[r1 + 296] = next_val
    u64[r1 + 304] = next_val
    let cond_1 = next_val <u 0
    u64[r1 + 312] = cond_1
    u64[r1 + 320] = 1

    if (next_val >=u 0) {
        // @23350
        let target_ptr_1 = 12 + u64[r1 + 40]
        u64[r1 + 328] = target_ptr_1
        u32[target_ptr_1 + 0x60000] = u64[r1 + 320]
        let data_ptr = 4 + u64[r1 + 240]
        u64[r1 + 344] = data_ptr
        let data_val = u32[data_ptr + 0x60000]
        u64[r1 + 352] = data_val
        let target_ptr_2 = 16 + u64[r1 + 40]
        u64[r1 + 360] = target_ptr_2
        u32[target_ptr_2 + 0x60000] = data_val
        goto block_233a9;
    } else {
        // @23348
        u64[r1 + 320] = 2
    }

    // @23348
    u64[r1 + 320] = 2

    // @23350
    let var_32 = 12 + u64[r1 + 40]
    u64[r1 + 328] = var_32
    u32[var_32 + 0x60000] = u64[r1 + 320]
    let var_36 = 4 + u64[r1 + 240]
    u64[r1 + 344] = var_36
    let var_37 = u32[var_36 + 0x60000]
    u64[r1 + 352] = var_37
    let var_40 = 16 + u64[r1 + 40]
    u64[r1 + 360] = var_40
    u32[var_40 + 0x60000] = var_37

    block_233a9:
    // @233a9

    // @232dd
    r7 = u64[r1 + 272]
    return
}
fn func_160(r1: u64) {
    // @2350f
    let data_ptr = u32[36] // Load base pointer/offset from memory index 36
    u64[r1 + 240] = data_ptr
    let status_byte = u8[data_ptr + 0x60000] // Read byte from memory offset 0x60000
    u64[r1 + 248] = status_byte
    u64[r1 + 256] = status_byte
    let cond_0 = status_byte <u 0 // Always false for u8
    u64[r1 + 264] = cond_0

    if (status_byte <u 0) {
        // @2361a
        goto block_2361f;
    } else {
        // @2354e
    }

    // @2356b
    u32[u64[r1 + 40] + 0x60000] = 3 // Set state to 3 in memory
    let next_data_ptr = 1 + u64[r1 + 240]
    u64[r1 + 288] = next_data_ptr
    let next_status_byte = u8[next_data_ptr + 0x60000]
    u64[r1 + 296] = next_status_byte
    u64[r1 + 304] = next_status_byte
    let cond_1 = next_status_byte <u 0
    u64[r1 + 312] = cond_1
    u64[r1 + 320] = 1

    if (next_status_byte >=u 0) {
        // @235c6
        let dest_offset_1 = 12 + u64[r1 + 40]
        u64[r1 + 328] = dest_offset_1
        u32[dest_offset_1 + 0x60000] = u64[r1 + 320]
        let source_offset = 4 + u64[r1 + 240]
        u64[r1 + 344] = source_offset
        let loaded_val = u32[source_offset + 0x60000]
        u64[r1 + 352] = loaded_val
        let dest_offset_2 = 16 + u64[r1 + 40]
        u64[r1 + 360] = dest_offset_2
        u32[dest_offset_2 + 0x60000] = loaded_val
        goto block_2361f;
    } else {
        // @235be
        u64[r1 + 320] = 2
    }

    // @235be
    u64[r1 + 320] = 2

    // @235c6
    let dest_offset_1 = 12 + u64[r1 + 40]
    u64[r1 + 328] = dest_offset_1
    u32[dest_offset_1 + 0x60000] = u64[r1 + 320]
    let source_offset = 4 + u64[r1 + 240]
    u64[r1 + 344] = source_offset
    let loaded_val = u32[source_offset + 0x60000]
    u64[r1 + 352] = loaded_val
    let dest_offset_2 = 16 + u64[r1 + 40]
    u64[r1 + 360] = dest_offset_2
    u32[dest_offset_2 + 0x60000] = loaded_val

    block_2361f:
    // @2361f

    // @23553
    r7 = u64[r1 + 272]
    return
}
fn func_161(r1: u64) {
    // @237ab
    let current_offset = u32[36] // Load base pointer offset from memory
    u64[r1 + 264] = current_offset
    let val_at_offset = u8[current_offset + 0x60000] // Const-offset memory access
    u64[r1 + 272] = val_at_offset
    u64[r1 + 280] = val_at_offset
    let cond_0 = val_at_offset <u 0 // Always false for u8
    u64[r1 + 288] = cond_0

    if (val_at_offset <u 0) {
        // @238b6
        goto block_238bb;
    } else {
        // @237ea
    }

    // @23807
    u32[u64[r1 + 40] + 0x60000] = 3 // Initialize state-related memory
    let next_offset = 1 + u64[r1 + 264]
    u64[r1 + 312] = next_offset
    let next_val_at_offset = u8[next_offset + 0x60000]
    u64[r1 + 320] = next_val_at_offset
    u64[r1 + 328] = next_val_at_offset
    let cond_1 = next_val_at_offset <u 0
    u64[r1 + 336] = cond_1
    u64[r1 + 344] = 1 // Default branch identifier

    if (next_val_at_offset >=u 0) {
        // @23862
        let state_reg_ptr = 12 + u64[r1 + 40]
        u64[r1 + 352] = state_reg_ptr
        u32[state_reg_ptr + 0x60000] = u64[r1 + 344]
        let struct_ptr = 4 + u64[r1 + 264]
        u64[r1 + 368] = struct_ptr
        let struct_val = u32[struct_ptr + 0x60000]
        u64[r1 + 376] = struct_val
        let target_ptr = 16 + u64[r1 + 40]
        u64[r1 + 384] = target_ptr
        u32[target_ptr + 0x60000] = struct_val
        goto block_238bb;
    } else {
        // @2385a
        u64[r1 + 344] = 2 // Update branch identifier if condition unmet
    }

    // @2385a
    u64[r1 + 344] = 2

    // @23862
    // Re-setting state registers from path @2385a
    let state_reg_ptr = 12 + u64[r1 + 40]
    u64[r1 + 352] = state_reg_ptr
    u32[state_reg_ptr + 0x60000] = u64[r1 + 344]
    let struct_ptr = 4 + u64[r1 + 264]
    u64[r1 + 368] = struct_ptr
    let struct_val = u32[struct_ptr + 0x60000]
    u64[r1 + 376] = struct_val
    let target_ptr = 16 + u64[r1 + 40]
    u64[r1 + 384] = target_ptr
    u32[target_ptr + 0x60000] = struct_val

    block_238bb:
    // @238bb

    // @237ef
    r7 = u64[r1 + 296]
    return
}
fn func_162(r1: u64) {
    // @23a11
    let base_ptr = u32[36] // Global base pointer/offset
    u64[r1 + 232] = base_ptr
    let state_val = u8[base_ptr + 0x60000] // Read state byte from memory mapped region
    u64[r1 + 240] = state_val
    u64[r1 + 248] = state_val
    let cond_0 = state_val <u 0 // Always false for u8
    u64[r1 + 256] = cond_0

    if (state_val <u 0) {
        // @23b1c
        goto block_23b21;
    } else {
        // @23a50
    }

    // @23a6d
    u32[u64[r1 + 40] + 0x60000] = 3 // Write metadata to object at r1+40
    let next_ptr = 1 + u64[r1 + 232]
    u64[r1 + 280] = next_ptr
    let next_state_val = u8[next_ptr + 0x60000]
    u64[r1 + 288] = next_state_val
    u64[r1 + 296] = next_state_val
    let cond_1 = next_state_val <u 0
    u64[r1 + 304] = cond_1
    u64[r1 + 312] = 1 // Default status code

    if (next_state_val >=u 0) {
        // @23ac8
        let target_field_ptr = 12 + u64[r1 + 40]
        u64[r1 + 320] = target_field_ptr
        u32[target_field_ptr + 0x60000] = u64[r1 + 312]
        let source_ptr = 4 + u64[r1 + 232]
        u64[r1 + 336] = source_ptr
        let val_to_copy = u32[source_ptr + 0x60000]
        u64[r1 + 344] = val_to_copy
        let dest_field_ptr = 16 + u64[r1 + 40]
        u64[r1 + 352] = dest_field_ptr
        u32[dest_field_ptr + 0x60000] = val_to_copy
        goto block_23b21;
    } else {
        // @23ac0
        u64[r1 + 312] = 2 // Error/Alternative status code
    }

    // @23ac0
    u64[r1 + 312] = 2

    // @23ac8
    let var_32 = 12 + u64[r1 + 40]
    u64[r1 + 320] = var_32
    u32[var_32 + 0x60000] = u64[r1 + 312]
    let var_36 = 4 + u64[r1 + 232]
    u64[r1 + 336] = var_36
    let var_37 = u32[var_36 + 0x60000]
    u64[r1 + 344] = var_37
    let var_40 = 16 + u64[r1 + 40]
    u64[r1 + 352] = var_40
    u32[var_40 + 0x60000] = var_37

    block_23b21:
    // @23b21

    // @23a55
    r7 = u64[r1 + 264]
    return
}
fn func_163(r1: u64) {
    // @23d02
    // Load state pointer from memory offset 36
    let state_ptr = u32[36]
    u64[r1 + 312] = state_ptr
    // Retrieve byte at manual memory offset (base 0x60000)
    let state_val = u8[state_ptr + 0x60000]
    u64[r1 + 320] = state_val
    u64[r1 + 328] = state_val
    let cond_0 = state_val <u 0
    u64[r1 + 336] = cond_0

    if (state_val <u 0) {
        // @23e0d
        goto block_23e12;
    } else {
        // @23d41
    }

    // @23d5e
    // Set status flag at context pointer 40
    u32[u64[r1 + 40] + 0x60000] = 3
    let next_state_ptr = 1 + u64[r1 + 312]
    u64[r1 + 360] = next_state_ptr
    let next_val = u8[next_state_ptr + 0x60000]
    u64[r1 + 368] = next_val
    u64[r1 + 376] = next_val
    let cond_1 = next_val <u 0
    u64[r1 + 384] = cond_1
    // Initialize error/status register
    u64[r1 + 392] = 1

    if (next_val >=u 0) {
        // @23db9
        let result_dest_ptr = 12 + u64[r1 + 40]
        u64[r1 + 400] = result_dest_ptr
        u32[result_dest_ptr + 0x60000] = u64[r1 + 392]
        let data_src_ptr = 4 + u64[r1 + 312]
        u64[r1 + 416] = data_src_ptr
        let data_val = u32[data_src_ptr + 0x60000]
        u64[r1 + 424] = data_val
        let copy_dest_ptr = 16 + u64[r1 + 40]
        u64[r1 + 432] = copy_dest_ptr
        u32[copy_dest_ptr + 0x60000] = data_val
        goto block_23e12;
    } else {
        // @23db1
        u64[r1 + 392] = 2
    }

    // @23db1
    u64[r1 + 392] = 2

    // @23db9
    var_32 = 12 + u64[r1 + 40]
    u64[r1 + 400] = var_32
    u32[var_32 + 0x60000] = u64[r1 + 392]
    var_36 = 4 + u64[r1 + 312]
    u64[r1 + 416] = var_36
    var_37 = u32[var_36 + 0x60000]
    u64[r1 + 424] = var_37
    var_40 = 16 + u64[r1 + 40]
    u64[r1 + 432] = var_40
    u32[var_40 + 0x60000] = var_37

    block_23e12:
    // @23e12

    // @23d46
    r7 = u64[r1 + 344]
    return
}
fn func_164(r1: u64) {
    // @23ff4: Initialize state with memory offset from global const [36]
    let base_offset = u32[36]
    u64[r1 + 312] = base_offset
    let byte_val = u8[base_offset + 0x60000] // Read byte from memory segment at 0x60000
    u64[r1 + 320] = byte_val
    u64[r1 + 328] = byte_val
    let cond_0 = byte_val <u 0 // Always false for u8
    u64[r1 + 336] = cond_0

    if (byte_val <u 0) {
        // @240ff
        goto block_24104;
    } else {
        // @24033
    }

    // @24050
    u32[u64[r1 + 40] + 0x60000] = 3 // Write literal 3 to computed memory address
    let next_offset = 1 + u64[r1 + 312]
    u64[r1 + 360] = next_offset
    let next_byte_val = u8[next_offset + 0x60000] 
    u64[r1 + 368] = next_byte_val
    u64[r1 + 376] = next_byte_val
    let cond_1 = next_byte_val <u 0
    u64[r1 + 384] = cond_1
    u64[r1 + 392] = 1 // status_flag initialized to 1

    if (next_byte_val >=u 0) {
        // @240ab: Conditional path (always taken if byte >= 0)
        let target_ptr = 12 + u64[r1 + 40]
        u64[r1 + 400] = target_ptr
        u32[target_ptr + 0x60000] = u64[r1 + 392]
        let derived_ptr = 4 + u64[r1 + 312]
        u64[r1 + 416] = derived_ptr
        let struct_val = u32[derived_ptr + 0x60000]
        u64[r1 + 424] = struct_val
        let dest_ptr = 16 + u64[r1 + 40]
        u64[r1 + 432] = dest_ptr
        u32[dest_ptr + 0x60000] = struct_val
        goto block_24104;
    } else {
        // @240a3: Fallthrough if byte < 0
        u64[r1 + 392] = 2
    }

    // @240a3
    u64[r1 + 392] = 2

    // @240ab: Redundant assignment to variables for flow consistency
    var_32 = 12 + u64[r1 + 40]
    u64[r1 + 400] = var_32
    u32[var_32 + 0x60000] = u64[r1 + 392]
    var_36 = 4 + u64[r1 + 312]
    u64[r1 + 416] = var_36
    var_37 = u32[var_36 + 0x60000]
    u64[r1 + 424] = var_37
    var_40 = 16 + u64[r1 + 40]
    u64[r1 + 432] = var_40
    u32[var_40 + 0x60000] = var_37

    block_24104:
    // @24104

    // @24038: Load result from internal struct and return
    r7 = u64[r1 + 344]
    return
}
fn func_165(r1: u64) {
    // @242a0
    let config_ptr = u32[36] // Load base pointer from global offset 36
    u64[r1 + 272] = config_ptr
    let state_val = u8[config_ptr + 0x60000] // Read state byte from memory map
    u64[r1 + 280] = state_val
    u64[r1 + 288] = state_val
    let cond_0 = state_val <u 0 // Always false for u8
    u64[r1 + 296] = cond_0

    if (state_val <u 0) {
        // @243ab
        goto block_243b0;
    } else {
        // @242df
    }

    // @242fc
    u32[u64[r1 + 40] + 0x60000] = 3 // Write marker 3 to context ptr offset
    let next_ptr = 1 + u64[r1 + 272]
    u64[r1 + 320] = next_ptr
    let next_val = u8[next_ptr + 0x60000]
    u64[r1 + 328] = next_val
    u64[r1 + 336] = next_val
    let cond_1 = next_val <u 0
    u64[r1 + 344] = cond_1
    u64[r1 + 352] = 1 // Initialize state_flag

    if (next_val >=u 0) {
        // @24357
        let dst_ptr_1 = 12 + u64[r1 + 40]
        u64[r1 + 360] = dst_ptr_1
        u32[dst_ptr_1 + 0x60000] = u64[r1 + 352]
        let src_ptr_1 = 4 + u64[r1 + 272]
        u64[r1 + 376] = src_ptr_1
        let loaded_val = u32[src_ptr_1 + 0x60000]
        u64[r1 + 384] = loaded_val
        let dst_ptr_2 = 16 + u64[r1 + 40]
        u64[r1 + 392] = dst_ptr_2
        u32[dst_ptr_2 + 0x60000] = loaded_val
        goto block_243b0;
    } else {
        // @2434f
        u64[r1 + 352] = 2 // Update state_flag on error case
    }

    // @2434f
    u64[r1 + 352] = 2

    // @24357
    let var_32 = 12 + u64[r1 + 40]
    u64[r1 + 360] = var_32
    u32[var_32 + 0x60000] = u64[r1 + 352]
    let var_36 = 4 + u64[r1 + 272]
    u64[r1 + 376] = var_36
    let var_37 = u32[var_36 + 0x60000]
    u64[r1 + 384] = var_37
    let var_40 = 16 + u64[r1 + 40]
    u64[r1 + 392] = var_40
    u32[var_40 + 0x60000] = var_37

    block_243b0:
    // @243b0

    // @242e4
    r7 = u64[r1 + 304] // Return value from context
    return
}
fn func_166(r1: u64) {
    // @2454e
    let current_base = u32[36] // Load base address from PVM register memory
    u64[r1 + 272] = current_base
    let byte_val = u8[current_base + 0x60000] // Read byte from offset in linear memory
    u64[r1 + 280] = byte_val
    u64[r1 + 288] = byte_val
    let cond_0 = byte_val <u 0 // Boundary check: currently always false for u8
    u64[r1 + 296] = cond_0

    if (byte_val <u 0) {
        // @24659
        goto block_2465e;
    } else {
        // @2458d
    }

    // @245aa
    u32[u64[r1 + 40] + 0x60000] = 3 // Set status flag to 3
    let next_ptr = 1 + u64[r1 + 272] // Increment base pointer
    u64[r1 + 320] = next_ptr
    let next_byte_val = u8[next_ptr + 0x60000]
    u64[r1 + 328] = next_byte_val
    u64[r1 + 336] = next_byte_val
    let cond_1 = next_byte_val <u 0
    u64[r1 + 344] = cond_1
    u64[r1 + 352] = 1 // Default status code

    if (next_byte_val >=u 0) {
        // @24605
        let offset_12 = 12 + u64[r1 + 40]
        u64[r1 + 360] = offset_12
        u32[offset_12 + 0x60000] = u64[r1 + 352]
        let struct_ptr = 4 + u64[r1 + 272]
        u64[r1 + 376] = struct_ptr
        let val_at_ptr = u32[struct_ptr + 0x60000]
        u64[r1 + 384] = val_at_ptr
        let offset_16 = 16 + u64[r1 + 40]
        u64[r1 + 392] = offset_16
        u32[offset_16 + 0x60000] = val_at_ptr
        goto block_2465e;
    } else {
        // @245fd
        u64[r1 + 352] = 2
    }

    // @245fd
    u64[r1 + 352] = 2

    // @24605
    let var_32 = 12 + u64[r1 + 40]
    u64[r1 + 360] = var_32
    u32[var_32 + 0x60000] = u64[r1 + 352]
    let var_36 = 4 + u64[r1 + 272]
    u64[r1 + 376] = var_36
    let var_37 = u32[var_36 + 0x60000]
    u64[r1 + 384] = var_37
    let var_40 = 16 + u64[r1 + 40]
    u64[r1 + 392] = var_40
    u32[var_40 + 0x60000] = var_37

    block_2465e:
    // @2465e

    // @24592
    r7 = u64[r1 + 304]
    return
}
fn func_167(r1: u64) {
    // @248dc
    let entry_ptr = u32[36] // Load base pointer/offset from memory address 36
    u64[r1 + 400] = entry_ptr
    let state_val = u8[entry_ptr + 0x60000] // Read state byte from the PVM data segment (offset 0x60000)
    u64[r1 + 408] = state_val
    u64[r1 + 416] = state_val
    let cond_0 = state_val <u 0 // Always false for u8; checks if value is negative
    u64[r1 + 424] = cond_0

    if (state_val <u 0) {
        // @249e7
        goto block_249ec;
    } else {
        // @2491b
    }

    // @24938
    u32[u64[r1 + 40] + 0x60000] = 3 // Write constant 3 to control memory at specified offset
    let next_ptr = 1 + u64[r1 + 400]
    u64[r1 + 448] = next_ptr
    let next_state_val = u8[next_ptr + 0x60000]
    u64[r1 + 456] = next_state_val
    u64[r1 + 464] = next_state_val
    let cond_1 = next_state_val <u 0
    u64[r1 + 472] = cond_1
    u64[r1 + 480] = 1 // Initialize sub-status flag to 1

    if (next_state_val >=u 0) {
        // @24993
        let target_ptr = 12 + u64[r1 + 40]
        u64[r1 + 488] = target_ptr
        u32[target_ptr + 0x60000] = u64[r1 + 480]
        let data_idx_ptr = 4 + u64[r1 + 400]
        u64[r1 + 504] = data_idx_ptr
        let data_val = u32[data_idx_ptr + 0x60000]
        u64[r1 + 512] = data_val
        let write_ptr = 16 + u64[r1 + 40]
        u64[r1 + 520] = write_ptr
        u32[write_ptr + 0x60000] = data_val
        goto block_249ec;
    } else {
        // @2498b
        u64[r1 + 480] = 2 // Switch sub-status flag to 2
    }

    // @2498b
    u64[r1 + 480] = 2

    // @24993
    let var_32 = 12 + u64[r1 + 40]
    u64[r1 + 488] = var_32
    u32[var_32 + 0x60000] = u64[r1 + 480]
    let var_36 = 4 + u64[r1 + 400]
    u64[r1 + 504] = var_36
    let var_37 = u32[var_36 + 0x60000]
    u64[r1 + 512] = var_37
    let var_40 = 16 + u64[r1 + 40]
    u64[r1 + 520] = var_40
    u32[var_40 + 0x60000] = var_37

    block_249ec:
    // @249ec

    // @24920
    r7 = u64[r1 + 432] // Load return value from context structure
    return
}
fn func_168(r1: u64) {
    // @24c6b
    let state_ptr = u32[36] // Load base pointer/offset from memory address 36
    u64[r1 + 400] = state_ptr
    let flags = u8[state_ptr + 0x60000] // Read flags from state memory segment
    u64[r1 + 408] = flags
    u64[r1 + 416] = flags
    let cond_0 = flags <u 0 // Always false for u8
    u64[r1 + 424] = cond_0

    if (flags <u 0) {
        // @24d76
        goto block_24d7b;
    } else {
        // @24caa
    }

    // @24cc7
    u32[u64[r1 + 40] + 0x60000] = 3 // Update result status in output buffer
    let next_ptr = 1 + u64[r1 + 400]
    u64[r1 + 448] = next_ptr
    let next_flags = u8[next_ptr + 0x60000]
    u64[r1 + 456] = next_flags
    u64[r1 + 464] = next_flags
    let cond_1 = next_flags <u 0 // Always false for u8
    u64[r1 + 472] = cond_1
    u64[r1 + 480] = 1

    if (next_flags >=u 0) {
        // @24d22
        let out_data_ptr = 12 + u64[r1 + 40]
        u64[r1 + 488] = out_data_ptr
        u32[out_data_ptr + 0x60000] = u64[r1 + 480]
        let input_val_ptr = 4 + u64[r1 + 400]
        u64[r1 + 504] = input_val_ptr
        let input_val = u32[input_val_ptr + 0x60000]
        u64[r1 + 512] = input_val
        let out_val_ptr = 16 + u64[r1 + 40]
        u64[r1 + 520] = out_val_ptr
        u32[out_val_ptr + 0x60000] = input_val
        goto block_24d7b;
    } else {
        // @24d1a
        u64[r1 + 480] = 2
    }

    // @24d1a
    u64[r1 + 480] = 2

    // @24d22
    var_32 = 12 + u64[r1 + 40]
    u64[r1 + 488] = var_32
    u32[var_32 + 0x60000] = u64[r1 + 480]
    var_36 = 4 + u64[r1 + 400]
    u64[r1 + 504] = var_36
    var_37 = u32[var_36 + 0x60000]
    u64[r1 + 512] = var_37
    var_40 = 16 + u64[r1 + 40]
    u64[r1 + 520] = var_40
    u32[var_40 + 0x60000] = var_37

    block_24d7b:
    // @24d7b

    // @24caf
    r7 = u64[r1 + 432]
    return
}
fn func_169(r1: u64) {
    // @25020: Load global/stored index and initialize control state
    let base_addr = u32[36]
    u64[r1 + 424] = base_addr
    let byte_val_0 = u8[base_addr + 0x60000] // Read byte from memory offset
    u64[r1 + 432] = byte_val_0
    u64[r1 + 440] = byte_val_0
    let cond_0 = byte_val_0 <u 0 // Always false for u8
    u64[r1 + 448] = cond_0

    if (byte_val_0 <u 0) {
        // @2512b
        goto block_25130;
    } else {
        // @2505f
    }

    // @2507c: Update status flag at memory + 0x60000
    u32[u64[r1 + 40] + 0x60000] = 3
    let next_addr = 1 + u64[r1 + 424]
    u64[r1 + 472] = next_addr
    let byte_val_1 = u8[next_addr + 0x60000]
    u64[r1 + 480] = byte_val_1
    u64[r1 + 488] = byte_val_1
    let cond_1 = byte_val_1 <u 0
    u64[r1 + 496] = cond_1
    u64[r1 + 504] = 1 // Set default state to 1

    if (byte_val_1 >=u 0) {
        // @250d7: Update pointer offsets and write state
        let target_ptr_a = 12 + u64[r1 + 40]
        u64[r1 + 512] = target_ptr_a
        u32[target_ptr_a + 0x60000] = u64[r1 + 504]
        let data_ptr = 4 + u64[r1 + 424]
        u64[r1 + 528] = data_ptr
        let loaded_val = u32[data_ptr + 0x60000]
        u64[r1 + 536] = loaded_val
        let target_ptr_b = 16 + u64[r1 + 40]
        u64[r1 + 544] = target_ptr_b
        u32[target_ptr_b + 0x60000] = loaded_val
        goto block_25130;
    } else {
        // @250cf: Handle exception state
        u64[r1 + 504] = 2
    }

    // @250cf: Fallback state update
    u64[r1 + 504] = 2

    // @250d7: Re-executed state sync
    var_32 = 12 + u64[r1 + 40]
    u64[r1 + 512] = var_32
    u32[var_32 + 0x60000] = u64[r1 + 504]
    var_36 = 4 + u64[r1 + 424]
    u64[r1 + 528] = var_36
    var_37 = u32[var_36 + 0x60000]
    u64[r1 + 536] = var_37
    var_40 = 16 + u64[r1 + 40]
    u64[r1 + 544] = var_40
    u32[var_40 + 0x60000] = var_37

    block_25130:
    // @25130 

    // @25064: Return result stored in context
    r7 = u64[r1 + 456]
    return
}
fn func_170(r1: u64) {
    // @2539f
    let current_base = u32[36] // Load base address/index from memory
    u64[r1 + 392] = current_base
    let entry_val = u8[current_base + 0x60000] // Access data at base + offset
    u64[r1 + 400] = entry_val
    u64[r1 + 408] = entry_val
    let cond_0 = entry_val <u 0 // Always false for u8
    u64[r1 + 416] = cond_0

    if (entry_val <u 0) {
        // @254aa
        goto block_254af;
    } else {
        // @253de
    }

    // @253fb
    u32[u64[r1 + 40] + 0x60000] = 3
    let next_ptr = 1 + u64[r1 + 392]
    u64[r1 + 440] = next_ptr
    let next_val = u8[next_ptr + 0x60000]
    u64[r1 + 448] = next_val
    u64[r1 + 456] = next_val
    let cond_1 = next_val <u 0
    u64[r1 + 464] = cond_1
    u64[r1 + 472] = 1 // status_flag initialized to 1

    if (next_val >=u 0) {
        // @25456
        let offset_12 = 12 + u64[r1 + 40]
        u64[r1 + 480] = offset_12
        u32[offset_12 + 0x60000] = u64[r1 + 472]
        let offset_4 = 4 + u64[r1 + 392]
        u64[r1 + 496] = offset_4
        let data_val = u32[offset_4 + 0x60000]
        u64[r1 + 504] = data_val
        let offset_16 = 16 + u64[r1 + 40]
        u64[r1 + 512] = offset_16
        u32[offset_16 + 0x60000] = data_val
        goto block_254af;
    } else {
        // @2544e
        u64[r1 + 472] = 2
    }

    // @2544e
    u64[r1 + 472] = 2 // status_flag set to 2

    // @25456
    var_32 = 12 + u64[r1 + 40]
    u64[r1 + 480] = var_32
    u32[var_32 + 0x60000] = u64[r1 + 472]
    var_36 = 4 + u64[r1 + 392]
    u64[r1 + 496] = var_36
    var_37 = u32[var_36 + 0x60000]
    u64[r1 + 504] = var_37
    var_40 = 16 + u64[r1 + 40]
    u64[r1 + 512] = var_40
    u32[var_40 + 0x60000] = var_37

    block_254af:
    // @254af

    // @253e3
    r7 = u64[r1 + 424]
    return
}
fn func_171(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    if (0xFEFD0000 >=u r1 - 312) {
        // @254c9: Load shared configuration base pointer
        let global_config_offset = u32[0x339DC]
    }

    // Iterate using loop_counter, constrained by bitwise masking and shifting operations
    for (let loop_counter = 0x39DC; shift_reg_a >>u (shift_reg_a << (255 & global_config_offset)) <u loop_counter >=u 0; loop_counter = 32 >>u (32 << 1 + loop_counter)) {
        // @25507: Initialize bit-shift constant
        let shift_reg_a = 32

        // @2557c: Define bit-shift constants for memory indexing
        let shift_reg_b = 32
        let shift_reg_c = 32

        // @25586: Access memory indexed by calculated bit-shift offset and compare against threshold
        if (u8[shift_reg_b >>u (shift_reg_b << u32[0x339D4] + loop_counter) + 0x60000] >=u shift_reg_c >>u (shift_reg_c << (255 & global_config_offset)) <u 0) {
            // @25682
            goto block_25687;
        } else {
            break
        }
    }

    // @25646

    // @25655: Calculate result value using bitwise shifts relative to loop_counter
    let result_val = 32 >>u (32 << loop_counter - 8)

    // @2564b

    // @25650

    // @25577

    // @254f0
    return
}
fn func_172(r1: u64) {
    // @25736
    let data_offset = 4 + u64[r1 + 40] // Base offset from PVM storage header
    u64[r1 + 104] = data_offset
    let data_len = u32[data_offset + 0x60000] // Load length from memory map
    u64[r1 + 112] = data_len
    u64[r1 + 120] = data_len
    let base_ptr = u64[r1 + 64] // Target base address
    u64[r1 + 128] = base_ptr
    let end_ptr = base_ptr + data_len // Calculate boundary
    u64[r1 + 136] = end_ptr
    let bitmask = 32 >>u (32 << end_ptr) // Calculate alignment mask
    u64[r1 + 144] = bitmask
    u8[bitmask + 0x60000] = u64[r1 + 48] // Store byte header at masked address
    let meta_offset = 12 + u64[r1 + 40] // Secondary metadata offset
    u64[r1 + 160] = meta_offset
    u32[meta_offset + 0x60000] = u64[r1 + 88] // Store descriptor value

    // @257a8
    r9 = u64[r1 + 8]
    return
}
fn func_174(r1: u64, r7: u64, r12: u64) {
    let memory_base_addr
    let config_offset_a: u64
    let config_offset_b: u64

    // @260db
    u64[r1 + 248] = r7 // Update state pointer in structure at r1
    config_offset_a = u64[r1 + 40]
    config_offset_b = u64[r1 + 216]

    if (0xFEFD0000 >=u r1 - 976) {
        // @258f9
        memory_base_addr = r7 // Initialize base address with r7
    }

    while (loop_counter <=u 0x2710 >=u 0) {
        // @259bd
        let shifted_val_a = 32 >>u (32 << 0x2710 %u loop_counter)
        let shifted_val_b = 32 >>u (32 << 0x2710 /u loop_counter)
        // Perform bitwise OR operation on memory based on calculated offsets from PVM constant pool
        u64[32 >>u (32 << (32 >>u (32 << (1 << (32 >>u (32 << 4 - bit_shift_idx))))) + memory_base_addr) + 0x60000] = 32 << u32[32 >>u (32 << 0x3C7C + (32 >>u (32 << (2 << (32 >>u (32 << 100 %u shifted_val_a)))))) + 0x60000] | u32[32 >>u (32 << 0x3C7C + (32 >>u (32 << (2 << (32 >>u (32 << 100 /u shifted_val_a)))))) + 0x60000]
    }

    // @25bd4

    // @25bd9

    if (loop_counter <=u 100 <u 0) {
        // @25d24
        goto block_25d29;
    } else {
        // @259b8
    }
    // @25bde
    // Store result of shift and memory look-up to specialized memory region
    u32[32 >>u (32 << (32 >>u (32 << (1 << (32 >>u (32 << 2 - index_ptr_a))))) + memory_base_addr) + 0x60000] = u32[32 >>u (32 << 0x3C7C + (32 >>u (32 << (2 << (32 >>u (32 << 100 /u loop_counter)))))) + 0x60000]
    let residual_val = 32 >>u (32 << 100 /u loop_counter)

    // @25d24

    block_25d29:
    if (iteration_idx <=u 10 <u 0) {
        // @25e6e
        u16[32 >>u (32 << (32 >>u (32 << (1 << (32 >>u (32 << 1 - index_ptr_b))))) + memory_base_addr) + 0x60000] = 32 >>u (32 << 48 + iteration_idx)
        goto block_25f2e;
    } else {
        // @25d72
    }
    // @25d77
    u32[32 >>u (32 << (32 >>u (32 << (1 << (32 >>u (32 << 2 - index_ptr_b))))) + memory_base_addr) + 0x60000] = u32[32 >>u (32 << 0x3C7C + (32 >>u (32 << (2 << iteration_idx)))) + 0x60000]

    // @25e6e
    u16[32 >>u (32 << (32 >>u (32 << (1 << (32 >>u (32 << 1 - index_ptr_b))))) + memory_base_addr) + 0x60000] = 32 >>u (32 << 48 + iteration_idx)

    block_25f2e:
    // @25f2e

    // @2591a
    return
}
fn func_175(storage_base_ptr: u64, value_to_store: u64) {
    // @27375
    // Store input value at offset 312 relative to the provided base pointer
    u64[storage_base_ptr + 312] = value_to_store
    return
}
fn func_177(r1: u64, r7: u64) {
    // @273e3
    u64[r1 + 80] = r7
    // Read value from base pointer at offset 0x60000
    let data_val = u32[u64[r1 + 40] + 0x60000]
    u64[r1 + 88] = data_val
    u32[r7 + 0x60000] = data_val
    // Calculate pointer to secondary data field
    let ptr_data_2 = 4 + u64[r1 + 40]
    u64[r1 + 104] = ptr_data_2
    u64[r1 + 112] = u32[ptr_data_2 + 0x60000]
    let param_n = u64[r1 + 48]
    u64[r1 + 120] = param_n
    let is_n_pos = param_n >s 0
    u64[r1 + 128] = is_n_pos
    u64[r1 + 136] = is_n_pos
    u64[r1 + 144] = is_n_pos
    let cond_1 = is_n_pos <u 0
    u64[r1 + 152] = cond_1

    if (is_n_pos <u 0) {
        // @274fe
        let val_n = u64[r1 + 48]
        u64[r1 + 248] = val_n
        let val_limit = u64[r1 + 72]
        u64[r1 + 256] = val_limit
        let is_limit_lt_n = val_limit <s val_n
        u64[r1 + 264] = is_limit_lt_n
        u64[r1 + 272] = is_limit_lt_n
        u64[r1 + 280] = is_limit_lt_n
        let cond_5 = is_limit_lt_n <u 0
        u64[r1 + 288] = cond_5
        u64[r1 + 296] = u64[r1 + 72]

        if (is_limit_lt_n >=u 0) {
            // @2755c
            goto block_27561;
        } else {
            // @27554
            u64[r1 + 296] = u64[r1 + 48]
        }
    } else {
        // @27456
    }

    // @27473
    let val_n_2 = u64[r1 + 48]
    u64[r1 + 168] = val_n_2
    let val_limit_2 = u64[r1 + 72]
    u64[r1 + 176] = val_limit_2
    let sum_limit_n = val_limit_2 + val_n_2
    u64[r1 + 184] = sum_limit_n
    // Bitwise shift construct: used for mask/size logic
    let shift_mask = 32 >>u (32 << sum_limit_n)
    u64[r1 + 192] = shift_mask
    u64[r1 + 200] = shift_mask
    let is_mask_neg = shift_mask <s 0
    u64[r1 + 208] = is_mask_neg
    u64[r1 + 216] = is_mask_neg
    u64[r1 + 224] = is_mask_neg
    let cond_3 = is_mask_neg <u 0
    u64[r1 + 232] = cond_3
    u64[r1 + 240] = 0

    if (is_mask_neg >=u 0) {
        // @274f9
        goto block_27561;
    } else {
        // @274f0
        u64[r1 + 240] = u64[r1 + 192]
    }

    // @274f0
    u64[r1 + 240] = u64[r1 + 192]

    // @274f9

    block_27561:
    // @27561
    let val_data_2 = u64[r1 + 112]
    u64[r1 + 312] = val_data_2
    let val_offset = u64[r1 + 304]
    u64[r1 + 320] = val_offset
    let new_addr = val_offset + val_data_2
    u64[r1 + 328] = new_addr
    let mask_2 = 32 >>u (32 << new_addr)
    u64[r1 + 336] = mask_2
    let ptr_target = 4 + u64[r1 + 80]
    u64[r1 + 344] = ptr_target
    u32[ptr_target + 0x60000] = mask_2
    let val_m = u64[r1 + 56]
    u64[r1 + 360] = val_m
    let is_m_pos = val_m >s 0
    u64[r1 + 368] = is_m_pos
    u64[r1 + 376] = is_m_pos
    u64[r1 + 384] = is_m_pos
    let cond_7 = is_m_pos <u 0
    u64[r1 + 392] = cond_7

    if (is_m_pos <u 0) {
        // @27682
        let val_m_inner = u64[r1 + 56]
        u64[r1 + 480] = val_m_inner
        let val_limit_inner = u64[r1 + 72]
        u64[r1 + 488] = val_limit_inner
        let is_limit_lt_m = val_limit_inner <s val_m_inner
        u64[r1 + 496] = is_limit_lt_m
        u64[r1 + 504] = is_limit_lt_m
        u64[r1 + 512] = is_limit_lt_m
        let cond_11 = is_limit_lt_m <u 0
        u64[r1 + 520] = cond_11
        u64[r1 + 528] = u64[r1 + 72]

        if (is_limit_lt_m >=u 0) {
            // @276e0
            goto block_276e5;
        } else {
            // @276d8
            u64[r1 + 528] = u64[r1 + 56]
        }
    } else {
        // @275f2
        goto block_275f7;
    }

    // @275f2

    block_275f7:
    // @275f7
    let val_m_3 = u64[r1 + 56]
    u64[r1 + 400] = val_m_3
    let val_lim_3 = u64[r1 + 72]
    u64[r1 + 408] = val_lim_3
    let sum_m_lim = val_lim_3 + val_m_3
    u64[r1 + 416] = sum_m_lim
    let mask_3 = 32 >>u (32 << sum_m_lim)
    u64[r1 + 424] = mask_3
    u64[r1 + 432] = mask_3
    let is_mask3_neg = mask_3 <s 0
    u64[r1 + 440] = is_mask3_neg
    u64[r1 + 448] = is_mask3_neg
    u64[r1 + 456] = is_mask3_neg
    let cond_9 = is_mask3_neg <u 0
    u64[r1 + 464] = cond_9
    u64[r1 + 472] = 0

    if (is_mask3_neg >=u 0) {
        // @2767d
        goto block_276e5;
    } else {
        // @27674
        u64[r1 + 472] = u64[r1 + 424]
    }

    // @27674
    u64[r1 + 472] = u64[r1 + 424]

    // @2767d

    // @27682
    let val_m_4 = u64[r1 + 56]
    u64[r1 + 480] = val_m_4
    let val_lim_4 = u64[r1 + 72]
    u64[r1 + 488] = val_lim_4
    let is_lim_lt_m4 = val_lim_4 <s val_m_4
    u64[r1 + 496] = is_lim_lt_m4
    u64[r1 + 504] = is_lim_lt_m4
    u64[r1 + 512] = is_lim_lt_m4
    let cond_11_copy = is_lim_lt_m4 <u 0
    u64[r1 + 520] = cond_11_copy
    u64[r1 + 528] = u64[r1 + 72]

    if (is_lim_lt_m4 >=u 0) {
        // @276e0
        goto block_276e5;
    } else {
        // @276d8
        u64[r1 + 528] = u64[r1 + 56]
    }

    // @276d8
    u64[r1 + 528] = u64[r1 + 56]

    // @276e0

    block_276e5:
    // @276e5
    let var_134 = u64[r1 + 304]
    u64[r1 + 544] = var_134
    let var_136 = u64[r1 + 536]
    u64[r1 + 552] = var_136
    let is_136_lt_134 = var_136 <s var_134
    u64[r1 + 560] = is_136_lt_134
    u64[r1 + 568] = is_136_lt_134
    u64[r1 + 576] = is_136_lt_134
    let cond_13 = is_136_lt_134 <u 0
    u64[r1 + 584] = cond_13
    u64[r1 + 592] = u64[r1 + 304]

    if (is_136_lt_134 >=u 0) {
        // @27747
        let v_150 = u64[r1 + 592]
        u64[r1 + 600] = v_150
        let v_152 = u64[r1 + 304]
        u64[r1 + 608] = v_152
        let diff_offset = v_152 - v_150
        u64[r1 + 616] = diff_offset
        let mask_final = 32 >>u (32 << diff_offset)
        u64[r1 + 624] = mask_final
        let ptr_final = 8 + u64[r1 + 80]
        u64[r1 + 632] = ptr_final
        u32[ptr_final + 0x60000] = mask_final
        goto block_2745b;
    } else {
        // @2773e
        u64[r1 + 592] = u64[r1 + 536]
    }

    // @2773e
    u64[r1 + 592] = u64[r1 + 536]

    // @27747
    let v_150_2 = u64[r1 + 592]
    u64[r1 + 600] = v_150_2
    let v_152_2 = u64[r1 + 304]
    u64[r1 + 608] = v_152_2
    let diff_offset_2 = v_152_2 - v_150_2
    u64[r1 + 616] = diff_offset_2
    let mask_final_2 = 32 >>u (32 << diff_offset_2)
    u64[r1 + 624] = mask_final_2
    let ptr_final_2 = 8 + u64[r1 + 80]
    u64[r1 + 632] = ptr_final_2
    u32[ptr_final_2 + 0x60000] = mask_final_2

    block_2745b:
    // @2745b
    r7 = u64[r1 + 160]
    return
}
fn func_178(r1: u64, r7: u64) {
    // @27822
    // Storing input value r7 into the field at offset 96 of the struct/object at r1
    u64[r1 + 96] = r7

    // @277f4
    // Retrieving the value from offset 72 of the struct/object at r1 into r7 (return value)
    r7 = u64[r1 + 72]
    return
}
fn func_179(r1: u64, r7: u64) {
    // @278b8
    // Store input address/value r7 into object at offset 80
    u64[r1 + 80] = r7
    let data_offset = 4 + r7
    u64[r1 + 88] = data_offset
    // Fetch a 32-bit value from memory relative to data_offset and a static base
    let extracted_val = u32[data_offset + 0x60000]
    u64[r1 + 96] = extracted_val
    // Store a single byte from the extracted memory location
    u64[r1 + 104] = u8[extracted_val + 0x60000]
    // Signal or store state in global memory at address 28
    u32[28] = 1
    // Read state from global memory 0x33A00 and store it in the object and stack/base
    let global_state = u32[0x33A00]
    u64[r1 + 112] = global_state
    u64[r1 - 8] = global_state

    if (ptr_4_4 == 1) {
        // @27919
        // Trigger indirect call with constant argument
        r0 = 1230
        call_indirect(r8)
    }
}
fn func_180(r1: u64, r7: u64) {
    // @27a8a
    u64[r1 + 320] = r7 // Store input r7 to state offset 320
    u32[r7 + 0x60000] = u64[r1 + 168] // Store initial state struct value to memory base 0x60000
    let next_ptr = 4 + r7
    u64[r1 + 336] = next_ptr
    u32[next_ptr + 0x60000] = 1 // Initialize flag/counter in memory

    // @279e0
    let state_ptr_a = 4 + u64[r1 + 40]
    u64[r1 + 232] = state_ptr_a
    let val_a = u32[state_ptr_a + 0x60000]
    u64[r1 + 240] = val_a
    let state_ptr_b = 4 + u64[r1 + 224]
    u64[r1 + 248] = state_ptr_b
    let val_b = u32[state_ptr_b + 0x60000]
    u64[r1 + 256] = val_b
    u64[r1 + 264] = val_a
    u64[r1 + 272] = val_b
    let sum_vals = val_b + val_a
    u64[r1 + 280] = sum_vals
    let bitwise_result = 32 >>u (32 << sum_vals) // Perform bitwise shift normalization
    u64[r1 + 288] = bitwise_result
    let update_ptr = 4 + u64[r1 + 40]
    u64[r1 + 296] = update_ptr
    u32[update_ptr + 0x60000] = bitwise_result
    u64[r1 + 312] = u32[u64[r1 + 224] + 0x60000] // Load value from memoized address

    // @279c8
    r7 = u64[r1 + 216]
    return
}
fn func_181(state_base_ptr: u64, value_to_store: u64) {
    // @27be1
    // Store value_to_store in the state structure at offset 496
    u64[state_base_ptr + 496] = value_to_store
    return
}
fn func_183(r1: u64, r7: u64) {
    // @28030
    u64[r1 + 120] = r7                                         // Store input param into state struct
    let data_offset = 4 + u64[r1 + 40]                         // Calculate base offset from state field
    u64[r1 + 128] = data_offset
    let val_at_offset = u32[data_offset + 0x60000]             // Read u32 value at calculated memory address
    u64[r1 + 136] = val_at_offset
    u64[r1 + 144] = val_at_offset
    let base_val = u64[r1 + 48]                                // Load base value from state
    u64[r1 + 152] = base_val
    let sum_val = base_val + val_at_offset                     // Calculate sum for shift operation
    u64[r1 + 160] = sum_val
    let shift_mask = 32 >>u (32 << sum_val)                    // Compute mask via bitwise arithmetic
    u64[r1 + 168] = shift_mask
    let target_addr = 4 + u64[r1 + 40]                         // Re-calculate target store address
    u64[r1 + 176] = target_addr
    u32[target_addr + 0x60000] = shift_mask                    // Update memory with computed mask

    // @280a0
    r7 = u64[r1 + 192]                                         // Return value from state field
    return
}
fn func_185(r1: u64, r7: u64) {
    let memory_base_ptr: ptr

    // @2821d
    u64[r1 + 216] = r7 // Store input r7 into tracked offset 216
    memory_base_ptr = r7 + 0x60000 // Calculate memory mapped base address

    if (var_9 == 0) {
        // @28245
        // Write r7 into the first slot of the buffer located at [r1 + 48]
        u32[u64[r1 + 48] + 0x60000] = u64[r1 + 216]
        
        let slot_2_ptr = 4 + u64[r1 + 48]
        u64[r1 + 240] = slot_2_ptr
        u32[slot_2_ptr + 0x60000] = u64[r1 + 216]
        
        let slot_3_ptr = 8 + u64[r1 + 48]
        u64[r1 + 256] = slot_3_ptr
        u32[slot_3_ptr + 0x60000] = u64[r1 + 208]
        
        let slot_4_ptr = 12 + u64[r1 + 48]
        u64[r1 + 272] = slot_4_ptr
        u32[slot_4_ptr + 0x60000] = u64[r1 + 40]
    } else {
        // @28237
        r2 = memory_base_ptr + 1 // Increment memory pointer
    }

    // @28191
    r7 = u64[r1 + 152] // Return context-specific value from offset 152
    return
}
fn func_188(r1: u64, r7: u64) {
    let base_memory_ptr: ptr

    // @28524
    // Store input value r7 into struct field at offset 128
    u64[r1 + 128] = r7
    // Calculate base memory pointer with a fixed offset of 0x60000
    base_memory_ptr = r7 + 0x60000

    if (var_9 == 0) {
        // @2854b
    } else {
        // @2853d
        // Increment memory pointer if condition is met
        r2 = base_memory_ptr + 1
    }

    // @284ce
    // Load result from struct field at offset 88 into return register
    r7 = u64[r1 + 88]
    return
}
fn func_190(state_ptr: u64, struct_base_ptr: u64) {
    // @293f6
    // Store current base address in state
    u64[state_ptr + 2072] = struct_base_ptr
    // Offset 0x60000 acts as a memory-mapped I/O or state buffer
    u32[struct_base_ptr + 0x60000] = u64[state_ptr + 288]
    
    let offset_1 = 4 + struct_base_ptr
    u64[state_ptr + 2088] = offset_1
    u32[offset_1 + 0x60000] = u64[state_ptr + 432]
    
    let offset_2 = 8 + struct_base_ptr
    u64[state_ptr + 2104] = offset_2
    u32[offset_2 + 0x60000] = u64[state_ptr + 728]
    
    let offset_3 = 12 + struct_base_ptr
    u64[state_ptr + 2120] = offset_3
    u32[offset_3 + 0x60000] = u64[state_ptr + 1400]

    // @28755
    // Return the value located at offset 296 from the state
    r7 = u64[state_ptr + 296]
    return
}
fn func_191(state_ptr: u64, value_to_store: u64) {
    // @29f17
    // Store input value into the state structure at offset 144
    u64[state_ptr + 144] = value_to_store
    return
}
fn func_193(state_ptr: u64, value_to_store: u64) {
    // @2a497
    // Store value in the state struct at offset 896
    u64[state_ptr + 896] = value_to_store

    // @29ed1
    // Retrieve value from the state struct at offset 128 and return it
    r7 = u64[state_ptr + 128]
    return
}
fn func_195(r1: u64) {
    // @2a8a8
    let offset_ptr = 4 + u64[r1 + 40]
    u64[r1 + 56] = offset_ptr
    let val_a = u32[offset_ptr + 0x60000] // Load from state memory at offset
    u64[r1 + 64] = val_a
    let base_ptr_offset = u32[u64[r1 + 40] + 0x60000]
    u64[r1 + 72] = base_ptr_offset
    let next_ptr = 4 + base_ptr_offset
    u64[r1 + 80] = next_ptr
    let val_b = u32[next_ptr + 0x60000]
    u64[r1 + 88] = val_b
    u64[r1 + 96] = val_a
    u64[r1 + 104] = val_b
    let sum_ab = val_b + val_a
    u64[r1 + 112] = sum_ab
    let shift_1 = 32 >>u (32 << sum_ab) // Bitwise normalization/masking
    u64[r1 + 120] = shift_1
    let byte_1 = u8[shift_1 + 0x60000]
    u64[r1 + 128] = byte_1
    u64[r1 + 136] = val_a
    let inc_a = 1 + val_a
    u64[r1 + 144] = inc_a
    let shift_2 = 32 >>u (32 << inc_a)
    u64[r1 + 152] = shift_2
    u64[r1 + 160] = shift_2
    u64[r1 + 168] = val_b
    let sum_b2 = val_b + shift_2
    u64[r1 + 176] = sum_b2
    let shift_3 = 32 >>u (32 << sum_b2)
    u64[r1 + 184] = shift_3
    let byte_2 = u8[shift_3 + 0x60000]
    u64[r1 + 192] = byte_2
    u64[r1 + 200] = byte_2
    let bit_shl_1 = 8 << byte_2
    u64[r1 + 208] = bit_shl_1
    let shift_4 = 32 >>u (32 << bit_shl_1)
    u64[r1 + 216] = shift_4
    u64[r1 + 224] = byte_1
    u64[r1 + 232] = shift_4
    let or_res_1 = shift_4 | byte_1
    u64[r1 + 240] = or_res_1
    let final_shift_1 = 32 >>u (32 << or_res_1)
    u64[r1 + 248] = final_shift_1
    u64[r1 + 256] = val_a
    let inc_a2 = 2 + val_a
    u64[r1 + 264] = inc_a2
    let shift_5 = 32 >>u (32 << inc_a2)
    u64[r1 + 272] = shift_5
    u64[r1 + 280] = shift_5
    u64[r1 + 288] = val_b
    let sum_b3 = val_b + shift_5
    u64[r1 + 296] = sum_b3
    let shift_6 = 32 >>u (32 << sum_b3)
    u64[r1 + 304] = shift_6
    let byte_3 = u8[shift_6 + 0x60000]
    u64[r1 + 312] = byte_3
    u64[r1 + 320] = byte_3
    let bit_shl_2 = 16 << byte_3
    u64[r1 + 328] = bit_shl_2
    let shift_7 = 32 >>u (32 << bit_shl_2)
    u64[r1 + 336] = shift_7
    u64[r1 + 344] = final_shift_1
    u64[r1 + 352] = shift_7
    let or_res_2 = shift_7 | final_shift_1
    u64[r1 + 360] = or_res_2
    u64[r1 + 368] = 32 >>u (32 << or_res_2)
    u64[r1 + 376] = val_a
    let inc_a3 = 3 + val_a
    u64[r1 + 384] = inc_a3
    let shift_8 = 32 >>u (32 << inc_a3)
    u64[r1 + 392] = shift_8
    let write_ptr = 4 + u64[r1 + 40]
    u64[r1 + 400] = write_ptr
    u32[write_ptr + 0x60000] = shift_8 // Commit result back to state memory

    // @2aaea
    r7 = u64[r1 + 416]
    return
}
fn func_196(storage_ptr: u64, value: u64) {
    // @2ad54
    // Store the provided value at the memory location defined by constant offset 184
    u64[storage_ptr + 184] = value
    return
}
fn func_198(state_ptr: u64, value_to_store: u64) {
    // @2b510: Store value_to_store at offset 1296 from the base state pointer
    u64[state_ptr + 1296] = value_to_store
    return
}
fn func_203(state_ptr: u64, value_to_write: u64) {
    // @2c756
    // Store value in instance state at offset 3720 and update memory-mapped buffer at 0x60000
    u64[state_ptr + 3720] = value_to_write
    u32[u64[state_ptr + 3664] + 0x60000] = value_to_write

    // @2aca7
    // Retrieve and return pointer stored at state_ptr + 112 as the result
    r7 = u64[state_ptr + 112]
    return
}
fn func_204(stack_base: u64, stack_limit: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    // Check if the provided memory address (stack_limit - 72 bytes) is within the valid PVM memory bounds
    if (0xFEFD0000 >=u stack_limit - 72) {
        // @2c9df
        return
    }
}
fn func_205(ctx_ptr: u64) {
    // @2c9fb
    // Retrieve the value stored at offset 64 (8 words) from the context pointer
    r7 = u64[ctx_ptr + 64]
    return
}
fn func_207(r1: u64) {
    // @2eef5
    let data_base_addr = 4 + u64[r1 + 40]
    u64[r1 + 104] = data_base_addr
    let item_size = u32[data_base_addr + 0x60000] // Read size from memory-mapped table
    u64[r1 + 112] = item_size
    let shift_factor = u64[r1 + 64]
    u64[r1 + 120] = shift_factor
    let bit_mask = 2 << shift_factor // Calculate size-based power-of-two mask
    u64[r1 + 128] = bit_mask
    let alignment_offset = 32 >>u (32 << bit_mask) // Bitwise alignment calculation
    u64[r1 + 136] = alignment_offset
    u64[r1 + 144] = item_size
    u64[r1 + 152] = alignment_offset
    let combined_offset = alignment_offset + item_size
    u64[r1 + 160] = combined_offset
    let next_alignment = 32 >>u (32 << combined_offset)
    u64[r1 + 168] = next_alignment
    u32[next_alignment + 0x60000] = u64[r1 + 48] // Store result in memory table
    let metadata_ptr = 12 + u64[r1 + 40]
    u64[r1 + 184] = metadata_ptr
    u32[metadata_ptr + 0x60000] = u64[r1 + 88] // Update metadata entry

    // @2ef90
    r9 = u64[r1 + 8]
    return
}
fn func_211(r1: u64, r7: u64) {
    // @30e4d
    // Store input r7 at offset 2744 (likely a storage or context struct)
    u64[r1 + 2744] = r7
    
    // Calculate offsets relative to the base pointer at 2704
    let struct_base = u64[r1 + 2704]
    
    let offset_8 = 8 + struct_base
    u64[r1 + 2752] = offset_8
    u32[offset_8 + 0x60000] = r7 // Write r7 to memory pool at offset_8
    
    let offset_12 = 12 + struct_base
    u64[r1 + 2768] = offset_12
    u32[offset_12 + 0x60000] = 4 // Initialize field with constant 4
    
    let offset_16 = 16 + struct_base
    u64[r1 + 2784] = offset_16
    u32[offset_16 + 0x60000] = 0 // Clear field
    
    let offset_20 = 20 + struct_base
    u64[r1 + 2800] = offset_20
    u32[offset_20 + 0x60000] = 0 // Clear field

    // @2fabc
    // Update return register with value from memory at offset 96
    r7 = u64[r1 + 96]
    return
}
fn func_212(r1: u64, r7: u64, r11: u64, r12: u64) {
    let base_ptr_r7
    let base_ptr_var46
    let target_ptr_a: ptr
    let target_ptr_b: ptr
    let current_var46: u64

    // @313ea
    u64[r1 + 544] = r7 // Update function state/context
    current_var46 = u64[r1 + 256]

    if (0xFEFD0000 >=u r1 - 184) {
        // @257d1
        base_ptr_r7 = r7
        base_ptr_var46 = current_var46
        let loop_limit = u32[12 + current_var46 + 0x60000] // Load loop bound from memory region

        if (u32[12 + current_var46 + 0x60000] <s u32[8 + r7 + 0x60000] <u 0) {
            // @25878
            goto block_2587d;
        } else {
            // @2585d
            goto block_25876;
        }
    }

    block_25876:
    // @25876
    return

    block_2587d:
    // @2587d
    target_ptr_a = u32[4 + base_ptr_r7 + 0x60000] + 0x60000 // Calculate address with offset
    target_ptr_b = u32[4 + base_ptr_var46 + 0x60000] + 0x60000

    if (loop_limit == 0) {
        // @258df
        goto block_25862;
    } else {
        // @258cc
        r2 = target_ptr_a + 1 // Advance iteration pointer
        r3 = target_ptr_b + 1
    }

    while (loop_limit - 1 != 0) {
        // @258cc
        r2 = target_ptr_a + 1
        r3 = target_ptr_b + 1
    }

    // @258df

    block_25862:
    // @25862
    return
}
fn func_213(input_ptr: u64, msg_len: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    // Check if remaining buffer length (msg_len - 56) is within the safe memory range (0xFEFD0000)
    if (0xFEFD0000 >=u msg_len - 56) {
        // @32176
    }

    // @32190
    return
}
fn func_214(r0: u64, r1: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    // Check if (r1 - 48) is within the bounds of the allowed memory region/input range ending at 0xFEFD0000
    if (0xFEFD0000 >=u r1 - 48) {
        // @321ba
    }

    // @321d4
    return
}
fn func_215(input_buffer_ptr: u64, buffer_size: u64, r9: u64, r10: u64, r11: u64, r12: u64) {
    // Check if buffer_size - 40 is within the valid memory boundary (0xFEFD0000)
    if (0xFEFD0000 >=u buffer_size - 40) {
        // @321fb
    }

    // @32212
    return
}
fn func_216(r1: u64, r7: u64) {
    // @323c6
    u64[r1 + 184] = r7 // Store input r7 at offset 184
    let base_ptr = u64[r1 + 144] // Base pointer for data structure offsets

    let var_2 = 8 + base_ptr
    u64[r1 + 192] = var_2
    u32[var_2 + 0x60000] = r7 // Write input r7 into memory buffer

    let var_6 = 12 + base_ptr
    u64[r1 + 208] = var_6
    u32[var_6 + 0x60000] = 4 // Write constant 4 to buffer

    let var_10 = 16 + base_ptr
    u64[r1 + 224] = var_10
    u32[var_10 + 0x60000] = 0 // Initialize field to 0

    let var_14 = 20 + base_ptr
    u64[r1 + 240] = var_14
    u32[var_14 + 0x60000] = 0 // Initialize field to 0

    // @32437
    r9 = u64[r1 + 8] // Load return value from context
    return
}
